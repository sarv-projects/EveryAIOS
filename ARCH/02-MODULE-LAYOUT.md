# 02 — Module Layout (Rust crates + TS packages)

## 2.1 The workspace

```
desktop_app/
├── ARCH/                        ← this architecture series
├── crates/                      ← Rust workspace (new — everyaios-core)
│   ├── everyaios-core/                ← the orchestrator binary (tauri-free; testable headless) + `MemoryService` (memory/* dispatch) + `GuardService` (guard/* dispatch — tickets/policy/estop/profile)
│   ├── everyaios-cdp/                 ← CDP client + generated protocol types (BrowserOS browseros-cdp design)
│   ├── everyaios-browser/             ← snapshot/refs/diff engine, input, observer, markdown walker
│   ├── everyaios-script/              ← rquickjs sandbox (the `run` tool) + InnerCallHook audit
│   ├── everyaios-guard/               ← regex interceptors (P7.4 blocklist+prescan+red-team+url-floor+tickets), injection defense (P7.6), path floors + profiles + loop guard + manifests + config scan (P7.7), J21 permissions policy + decision package (P7.5)
│   ├── everyaios-audit/               ← NDJSON ingest, recording index, token estimators, replay store, session log (ContextInjection + Trajectory query), Merkle chain (P7.7), session repair (P7.7)
│   ├── everyaios-mcp/                 ← official MCP Rust SDK server (Streamable HTTP + stdio, stateless 2026-07-28) + tool catalog
│   ├── everyaios-vault/               ← SQLCipher key-ring store (per-provider key pools, OAuth tokens)
│   ├── everyaios-office/              ← office engine (docx block-patch, IronCalc calc library, pptx part-editor, pdf suite, LibreOffice oracle) — P4
│   ├── everyaios-storage/             ← storage intelligence (work-stealing walker, arena snapshots, 7-stage dedup, FTS5 search) — P4.8
│   ├── everyaios-memory/              ← memory algorithms (fusion, ACT-R, taste, compaction, graph, paging, ghost, reference, FSRS, intent classifier, repo summary, reinforce queue) — P5
│   ├── everyaios-eval/               ← verified-completion eval subsystem (status taxonomy, task manifest, deterministic verifier, evidence bundle, loop report, adversarial suite, retrieval scoring) — P8.0
│   ├── everyaios-blueprint/          ← orchestration core (spec-per-task, verify-gated blueprint tasks, agent-frontmatter, multi-agent topologies, automation shapes, .md blueprint parser + registry, DAG state machine, checkpoint/resume, plan cache, sub-agent runtime, iteration budgets + loop detector) — P6
│   ├── everyaios-codeintel/          ← code intelligence (LSP framing + core types, SCIP-style symbol index, tree-sitter-fed repo map + PageRank budget fit) — P7.1
│   ├── everyaios-acp/                ← ACP harness bridge (ACP v1 wire types, newline-delimited JSON-RPC framing, `AcpSession` client lifecycle, `LaunchRegistry` agent catalog) — F12/J17
│   └── everyaios-ipc/                 ← JSON-RPC over stdio (sidecar contract) + tauri command glue
├── packages/                    ← TS workspace (NEW — reuses @personal-ai/core-* from APP/ as deps)
│   └── coordinator/             ← the Bun-compiled sidecar (agent loop, engine stages, hub, memory)
└── ui/                          ← React SPA (webview frontend)
```

**Reuse rule:** `@personal-ai/core-*` packages are consumed **as published workspace deps** (pnpm workspace pointing at `APP/packages/*` or a shared publish), not copied. Desktop-specific additions go in `packages/coordinator/`.

## 2.2 Rust crate responsibilities (all new — source-pattern maps)

| Crate | Owns | Pattern source | Key rules |
|---|---|---|---|
| everyaios-cdp | WebSocket CDP client, discovery (loopback-only), per-target sessions, protocol types, **E15 Electron discovery (`probe_electron`/`discover_electron_apps`/`electron_from_json` — `Browser: Electron/…` detection + loopback guard)** | BrowserOS browseros-cdp (doc 33 §5) + agent-browser (doc 63 §4.1) | loopback-only discovery hosts; sha2-verified protocol file |
| everyaios-browser | a11y snapshot render, ref minting `[ref=eN]`, line-diff + URL-change short-circuit, input dispatch, iframe stitching, content-markdown walker, **E16 slim snapshot mode (`SnapshotMode::Slim` + `collapse_text` + `SLIM_DEPTH_CAP`, ≤40% of full-snapshot tokens)**; **E16 WebMCP handshake + HTTP transport (`webmcp.rs` `WebMcpTool`/`WebMcpResult`/`WebMcpRegistry`/`WebMcpExecutor` + `webmcp_http.rs` `McpHttpServer`/`handle_mcp_request`/`parse_http_request` — GET `/mcp` manifest, POST JSON-RPC `tools/list`/`tools/call` over std `TcpListener`)**; **E17 multi-protocol action parsing (`protocol.rs` — `parse_action` for native/Anthropic-CUA/OpenAI-CUA/UI-TARS → `ParsedAction` → `ActKind`)**; **E15 Electron CDP automation (`electron.rs` — `ElectronHandle::attach(port)` → snapshot/click/fill/read/screenshot over the CDP stack)** | BrowserOS browseros-core (doc 33 §5) + chrome-devtools-mcp `SlimMcpResponse`/`webmcp.ts` (doc 63 §4.2) + skyvern `parse_actions.py` (doc 63 §4.3) | refs scoped to (document_id, url); depth caps 1..=100; stable-capture attempts |
| everyaios-script | rquickjs async runtime, `browser` SDK surface, ownership filtering, limits | BrowserOS run tool (doc 33 §6.3) | 64MB heap / 512KB stack / 30s / 2MB return; per-primitive InnerCallHook (authorize + record + on_page_created) |
| everyaios-guard | P7.4: 40+ pattern blocklist (8 categories), pre-exec scan (shell/path/URL), URL floors, red-team corpus gate (35/35 blocked), authorization tickets; P7.6: injection scan + `<user_document>` wrap + tool-result sanitize + estop; P7.7: path floor (canonicalize/symlink/`..`/fuzz), profile-gated hooks, SHA-256 loop guard, AgentShield config scan, Ed25519 signed manifests; **P7.5/J21: `permissions::PermissionsPolicy` (`parse` TOML rules → `Rule::{AlwaysAsk,AlwaysAllow,Block,AskIfGt,AskIfNewDomain,AskIfDestructive}` + `evaluate(Operation)` + `min_confidence_for_auto`/`user_feedback_learning`) + `decision::DecisionPackage` (goal/diff/risk/paths/script-lines/env/network/`WebActionKind`) + `ticket::GuardReceipt` (self-hashed approve/reject audit receipt)**; **P7.8 (doc 64): `sandbox` module — `SandboxProfile` builder (no_new_privs → Landlock/App-Sandbox path allowlist → seccomp-bpf policy groups; Renderer/Worker/Network profiles), capability-string + path-seal doctrine, arg-filtered seccomp w/ crash-on-violation, FS syscall broker for script-eval (chromium syscall-broker pattern), path-floor 6-axis grants (recursive/temporary/read/write/create/stat — chromium BrokerFilePermission)** | doc 03 §8, doc 06, doc 26, doc 46, doc 52 §2, doc 53 §3, doc 64 §2/§3/§5 | every generated shell string scanned pre-exec; destructive = human click always; red-team corpus blocked (deny-filter — hard floors are structural, not regex); 0 path escapes (path-floor/canonicalization); sandbox profiles fail-closed (deny by default); policy is user-owned (`~/.everyaios/permissions.toml`), never model-owned |
| everyaios-audit | recording ingest (NDJSON + headers + sticky has_gap), recording index (dedupe, one-tx commit), per-dispatch token estimates, session efficiency projections; durable session log (idempotency classes + `ContextInjection` event + Trajectory (J5) `context_injections`/`list_session_ids`); P7.7: Merkle hash-chain (tamper-evident, `verify`→first-bad-row), 7-phase session repair (`validate_session` → RecoveryAction) | BrowserOS recordings/replay/recording_index + session_efficiency_stats (doc 33 §9); doc 46 OpenFang | append-only; 7-day replay retention; insert-once projections; chain head committed externally |
| everyaios-mcp | official `modelcontextprotocol/rust-sdk` server: one endpoint `http://127.0.0.1:9200/mcp` + stdio; **stateless per MCP 2026-07-28** (no initialize handshake, no Mcp-Session-Id — every request self-contained via `_meta`); 37-tool catalog (34 core + 3 `file_ops`); tool annotations (readOnly/openWorld); **`Mcp-Method`/`Mcp-Name` HTTP headers** (required per SEP-2243) | BrowserOS browseros-mcp (doc 33 §6) + MCP 2026-07-28 (doc 34 §2) | stateless streamable HTTP |
| everyaios-vault | SQLCipher token store: per-provider key pools, OAuth token rows, TTL/rotation metadata | BrowserOS oauth_tokens schema (doc 33 §7.4) + doc 19 §7 | keys never enter LLM context; per-key usage/cooldown state |
| everyaios-ipc | stdio JSON-RPC framing, typed messages, streaming | — | versioned contract (01 §1.4) |
| everyaios-office | docx block-patch engine (anchored block tree, byte-preserving ZIP part-patch), IronCalc xlsx truth recalc (**ironcalc 0.8.3 as a Rust library** — not a sidecar binary), deterministic workbook DSL planner, pptx part-editor (text runs / add-remove slides), pdf suite (form-fill, text-replace, re-author, redaction — lopdf), LibreOffice conformance oracle harness; **P4.7b D-gaps (doc 63 §3): xlsx chart read + author (`chart.rs` `extract_chart_series` + `build_chart_part`/`chart_rel_fragment`/`chart_content_type_override`), docx track-changes/comments read + author (`track.rs` `extract_tracked_changes`/`extract_comments`/`add_comment`/`emit_tracked_change`/`TrackAuthor`), pptx transitions (`transition.rs`) + per-shape `p:anim` (`anim.rs` `build_timing_xml`), pptx speaker notes + sync validation + rehearsal timing (`notes.rs` `extract_notes_text`/`build_speaker_notes`/`validate_slides_notes_sync`/`plan_rehearsal`), pdf annotations (`annot.rs` `add_text_annotation`/`add_highlight_annotation`), CSL citations + library + docx insert (`citation.rs` `render_citation`/`render_reference`/`render_bibliography`/`ReferenceLibrary`/`insert_citation_into_docx`)** | GenOffice (doc 28) + IronCalc + own thin OOXML editors (ARCH/04 §4.5) | byte-stable round-trip via LibreOffice oracle; IronCalc never bypassed for numeric claims; snapshotBefore rollback on every edit |
| everyaios-storage | parallel work-stealing disk walker (crossbeam-deque + `ignore`, cycle/device-boundary-safe), immutable arena snapshots (arc_swap @~100ms, zstd save/load), squarified treemap layout + per-dir aggregation, 7-stage hash dedup (size → xxHash3 [twox-hash — xxhash-rust is BSL-1.0, doc 54 §1.2] → BLAKE3, hardlink-aware, optional reflink), large-file finder, SQLite FTS5 filename index + notify-debouncer incremental updates, optional OS-native search hooks (Everything/MFT, mdfind, Baloo), Guard-2-ticketed cleanup | eDirStat traversal.rs/arena.rs/coordinator.rs + fclones + UltraSearch patterns (doc 49) | cleanup never bypasses dual-guard (ARCH/06); scans/indexing idle + battery-aware (J16); snapshots immutable (zero-copy arena) |
| everyaios-memory | weighted RRF multi-signal fusion + dedupe + smart snippets + per-type budget caps + RAG chunk-min-size merge (Alg #18/#29); ACT-R activation/decay + importance floor + associative recall + spontaneous-recall query derivation (Alg #32); taste profile store + stable-prefix injection + shareable markdown (Alg #31); compaction pipeline — snip/soft/force ratios, findSafeSplitPoint, sliding window, summarize-fail-open, prefix_dirty flag, PRUNE_PROTECT erasure, **compaction-as-lifecycle (`run_compaction_lifecycle`/`CompactionEvent`) + model-fallback chain (`compact_with_fallback`/`truncate_with_marker`) + turn-loop coordinator (`CompactionCoordinator` — per-turn `push_turn` → action, `maybe_compact` drives the lifecycle, events → turn items)** (Alg #21); **FSRS-6 scheduler (`fsrs.rs` — `Fsrs`/`MemoryState`/`NextStates`, 0/17/19/21-param construction, forgetting curve + next-interval/next-states + workload `simulate`)** (C13); **intent classifier + execution plan (`classify.rs` — Vane `(needs_research, needs_tools, needs_widgets, rewrite_query)` signals + `plan_execution`/`parallel_groups` parallel batches)**; **hierarchical repo summarization (`summary.rs` — summarize-file → directory → index → answer, no embeddings)**; **reinforce queue + extraction (`reinforce.rs` — `ReviewQueue` ingest/due/review on FSRS + `extract_candidates`/`split_sentences` deterministic fact extraction)**; **BM25 keyword signal (`bm25.rs` — pure Okapi BM25 over in-memory docs, one fused retrieval signal)**; **context planner (`planner.rs` — C7: decides prompt contents from retrieved signals with token budget + precedence)**; **Janus structural passes (`janus.rs` — dedup exact/near-dup + regex collapse + AST prune, the pre-injection reduction pipeline)**; **RTK output compression (`rtk.rs` — P5.7 per-command ls/ps/git/du parsers, measured 60–90% reduction)**; **Cognee-style graph API (`cognee.rs` — memory-ontology CRUD + query surface over the graph store)**; **usage ledger (`usage.rs` — P8 per-key/per-session token + cache hit/miss + cost at configured prices)**; graph store — entity/episodic nodes + typed edges + temporal edge-versioning + spreading activation + depth-cap query (Alg #6/#30); Letta-style 3-surface paging — core/archival/recall + queued writes + overflow eviction (Alg #20); ghost-context index — atomic tombstone + re-path (ARCH/07 §7.5.1); pass-by-reference handles + bounded ≤2K previews (C10) | mem0 fusion + NOOA nooa-memory + Command Code taste-1 (pattern-only) + Reasonix/BrowserOS/opencode compaction + Graphiti + Letta (docs 07/31/33/34/37/39/46) + **fsrs-rs (doc 63 §2.2)** | retrieval signals (FTS5/vec/graph) are caller-supplied; LadybugDB C++ FFI deferred (Rust-native graph store, same schema); taste store dir-scoped (global `~/.everyaios/taste/` vs per-repo `.everyaios-taste/`); all pure logic — no IO beyond taste markdown save/load |
| everyaios-eval | **P8.0 verified-completion eval**: six-way `CompletionStatus` taxonomy (score + status, never one blended number), typed `TaskManifest` (goal + `OutcomeCheck` exists/hash/contains + `Constraint` + `Budgets` + `EvidenceRequirement`), deterministic verifier SDK (`run_outcome_check`/`verify`/`verify_with_policy`) that never trusts the agent's "finished" claim; **evidence bundle + persistent store** (`EvidenceBundle`/`ArtifactHash`/`ApprovalEvent` + explicit `missing()` + `store.rs` `EvidenceStore` JSON persistence keyed by task id); **evidence-first loop report** (`LoopReport`/`Finding` with impact/expected-output/scoped-repair/acceptance-checks); **30-task adversarial suite** (`builtin_suite()` + 7 `FaultKind`); **retrieval-eval scoring** (`score_retrieval` → 7 metrics); **sandbox runner** (`runner.rs` `SandboxRunner::run` — provision fixture → hash snapshot → inject fault → run agent → verify → evidence → reset; `Agent` trait is the harness seam); **corpus data** (`corpus.rs` `builtin_retrieval_corpus`/`builtin_retrieval_questions`/`builtin_retrieval_cases`/`builtin_fixtures`); **batch runs** (`batch.rs` `run_suite` over the whole suite + `run_retrieval_batch` over cases with an answering function, per-status/per-case distributions + aggregates) | doc 63 §2.3 (better-harness evidence-first + skyvern verification loop + codex attestation + openspec verify-gate) | policy violations are hard-fail (`FailedUnsafely`) regardless of outcome completion; uncheckable tasks → `Unverifiable`; missing evidence stays explicit |
| everyaios-blueprint | **P6 orchestration core**: spec-per-task (`TaskSpec` ↔ `spec.md`); verify-gated `Blueprint`/`BlueprintTask`/`VerifyBlock` with dependency-aware ready set + cycle detection + `verify_against()` (delegates to eval); agent-frontmatter (`parse_frontmatter` → `AgentConfig` + `PermissionMode→ApprovalMode` bridge); multi-agent `Topology` (group-chat/handoff/sequential/concurrent) with least-privilege `privileged_workers()`; `Automation` steps (`run_code`/`online_search`/email/calendar); **P6.1 engine** — `Blueprint` ↔ `.md` (`md.rs` `BlueprintDoc`/`to_markdown`/`from_markdown` + optional agent frontmatter) + `BlueprintRegistry::load_dir`/`agent_configs()` (`checkpoint.rs`); `TaskStatus::transition` DAG state machine + `topological_order`/`set_status`/`is_complete`; checkpoint/resume + circuit-break freeze (`Checkpoint`/`checkpoint_to`/`resume_from`); plan cache (`plan_cache.rs` `PlanCache` signature + cosine sim + version invalidation); **P6.2 sub-agent runtime** (`subagent.rs` `SubAgentRuntime` — fresh-context `SubAgentSpec` + `DELEGATE_BLOCKED_TOOLS`/`effective_tools` + summary-only `SubAgentResult` + depth/concurrent/total guards + `spawn_batch` + `AgentMessage`/`route_message`); **P6.3 iteration budgets + loop detector** (`iteration.rs` `IterationBudget` 500/50 + execute-code refund, `LoopDetector` FNV-1a window/3× repeat, `TimeoutPolicy` 900/1800, `CircuitBreak`/`McqOption`/`CircuitBreaker`) | codger/openspec (doc 63 §2.3/§4.18) + qwen-code frontmatter (§4.4) + agent-framework (§4.13) + khoj (§4.12) | the verifier decides "done", never the agent's claim; email/calendar steps surfaced for the approval gate; multi-agent only where eval proves it |
| everyaios-codeintel | **P7.1 code intelligence (I11 — doc 63 §2.1/§4.6/§4.8)**: LSP JSON-RPC framing (`encode_message`/`decode_messages` — Content-Length, partial-buffer-safe) + core type set (`Hover`/`Location`/`TextEdit`/`Diagnostic`/`CodeAction`/`InlayHint`/`WorkspaceEdit`) (`lsp.rs`) **+ LSP session runtime (`session.rs` `LspTransport` trait, `ProcessTransport::spawn` stdio spawn + `is_alive` keep-alive + `shutdown`, `LspSession` initialize/request/notify lifecycle)**; SCIP-style `SemanticIndex` + `symbol_where`/`symbol_callers`/`unused_exports` (`semantic.rs`) **+ SCIP protobuf wire reader (`scip.rs` `parse_document`/`to_semantic_index`)**; repo map — `extract_tags`/`page_rank`/`rank_tags`/`build_repo_map`/`fit_budget` binary-search budget fit (`repomap.rs`) **+ pluggable `TagSource` (`LexicalTagSource` default, `CompositeTagSource`)** | neovim `runtime/lua/vim/lsp/*` + crux `symbol_where` + aider `repomap.py` (doc 63) | guard-ticket exposure (permission gate on code-intel actions) is P7 harness wiring; tree-sitter is one pluggable `TagSource` |
| everyaios-acp | **F12/J17 ACP harness bridge (doc 45 §1, doc 57 §2 — the `ollama launch` pattern)**: ACP v1 wire types (`messages.rs` — camelCase keys/snake_case discriminators, `PROTOCOL_VERSION`, capabilities, `StopReason`, `ToolKind`, `SessionUpdate`, `PermissionRequest`) + newline-delimited JSON-RPC framing (`frame.rs`) + `AcpSession` client lifecycle (`client.rs` — `initialize` negotiation → `session/new` → `session/prompt` collecting `session/update` + answering `session/request_permission` via an `FnMut` policy seam → `session/cancel`; `AcpTransport` trait with mock + real `ProcessTransport` stdio spawn) + `LaunchRegistry` (`registry.rs` — **46-agent catalog seeded from the official ACP `registry.json` (38 agents) + ollama launch + Zed `/acp` ecosystem**: claude (`npx @agentclientprotocol/claude-agent-acp`), codex (`npx @agentclientprotocol/codex-acp`), cline (`npx cline --acp`), opencode (`opencode acp`), hermes (`hermes acp`), openclaw (`openclaw client acp`), copilot (`@github/copilot --acp`), gemini (`@google/gemini-cli --acp`), cursor (`cursor-agent acp`), devin (`devin acp`), kiro (`kiro-cli acp`), junie, grok (`agent stdio`), qwen-code, goose, aider (`uvx aider-chat`), kimi, kilo, qoder, poolside, cortex-code, factory-droid, + more; `AuthMode` badge subscription/api_key/local; `Distribution` binary/npx/uvx **with `args`**; fixed `env` overrides; `HarnessProtocol::{Inbuilt,Acp,ModelBackend}`; **default = inbuilt `everyaios`**) **+ F8 registry-fed discovery (`registry_index.rs` — typed `registry.json` parse (npx/uvx/binary + per-platform `BinaryTarget` archive/sha256), `Platform::current`, `install_plan`→`InstallSpec`, `merge_into` version-pinning + `-acp`/alias canonicalization, `RegistryPolicy` allow-list→allow/ask/block; `registry_client.rs` — `RegistryClient` pluggable `Fetch` + ureq, `refresh`/`load_cached`/`load_or_refresh` offline-fallback, cache `<data_dir>/agents/registry.json`+meta; `installer.rs` — `Installer` download (256MB ceiling) → sha256 verify → `.tar.gz`/`.tar`/`.zip` extract → `<data_dir>/agents/<id>/<version>/pkg` + install-state pointer, `Installer::installed` resolves the launch path, npx/uvx record the pin; `AcpSession::authenticate`/`logout` + `auth_required` (-32000 + message fallback) detection on `session/new`; `auth_methods` surfaced from `initialize`)** | doc 45 ACP spec + doc 57 ACP registry + doc 69 (official `registry.json` transcription) + Ollama `launch` + Hermes `copilot_acp_client.py` (doc 57 §2) | the inbuilt engine is the default picker entry (no subprocess); an `Ask` permission never auto-allows — it mints the ticket and denies the turn for re-prompt; protocol implemented in-house (no external `agent-client-protocol` SDK); install mutates only `<data_dir>/agents` and verifies sha256; **the install is now Guard-2-ticketed (request mints the ticket/decision package → commit consumes `use_ticket` + executes) and agent auth is surfaced at launch (`authRequired` + `authMethods`; url-type opens the browser, agent-type completes in the agent's flow)** — the remaining seam is the live two-CLI side-by-side run with real agent binaries |

## 2.3 TS coordinator responsibilities (reuses core-*)

| Domain | Package (exists) | New work |
|---|---|---|
| Agent loop (pi-style) | `core-engine` (stages, risk-compass) | length-guard (fail truncated tool calls), model-swap hook, cost ledger wiring |
| Blueprint/spec loader | `core-agents` (registry) | `.md` parser → AgentConfig[]; continuous re-write of status blocks |
| Memory + RAG | `core-memory`, `core-files` (7 algos, hybrid search, embeddings) | multi-signal retrieval fusion (mem0 pattern), procedural memory, Letta-style paging hooks |
| Connector hub | `core-connectors` (orchestrator, 27+ adapters, composio) | routing engine per doc 13; usage meters; Auth Bridge |
| Search/research | `core-search` (cascade, bm25, research-tiers) | deep-research tree runner (doc 07); **tiered cascade + SQLite result cache (G8, Algorithm #33, doc 52)** — cached instant tier → WebSurfx → SearXNG → fallback; parallel top-N fetch cascade |
| Automations | `core-automations` (workflow engine, crystallization) | scheduler UI, nudge sentinels |
| Providers/BYOK | `core-providers`, `core-ai` (clients, router, vault) | **key-ring client** (03): multiple keys/provider, fallback rotation |
| Security | `core-tools` (trust-ladder, permission-gate) | keep; GuardRail enforcement delegated to Rust everyaios-guard |

**Division of trust:** the sidecar proposes; the Rust core disposes. **Execution model:** the sidecar runs the reused `core-*` engine in-process (files, connectors, search — that's the asset being reused), but every **mutating** call must present a valid **everyaios-guard authorization ticket**: Rust performs the regex scan + path resolution + permission decision first and issues a short-lived ticket; the sidecar's tool runtime rejects un-ticketed mutations. The sidecar's own *unguarded* OS access is confined to its data dir (its stdio pipe is the only unrestricted handle). Browser control, script-eval, OAuth-token use, and shell outside the granted workspace always execute in Rust regardless of ticket.

## 2.4 The 37-tool browser catalog (everyaios-mcp + everyaios-browser)

`tabs · tab_groups · history · navigate · snapshot · diff · act · download · upload · read · grep · screenshot · pdf · wait · windows · evaluate · run` — same names/semantics as BrowserOS (doc 33 §6.1) so prompts and skills transfer. Annotations: read-only set on read tools; `run`/`evaluate` are open-world + always permission-checked.

## 2.5 Cross-cutting: config

- `everyaios.toml` (Rust core): ports, dirs (`~/.everyaios/`), retention, vault path, browser binary resolution.
- `agents/*.md` blueprints (sidecar): per-agent models, subagent limits, tools, permission policy — the "everything is a file" rule (v2.0 §7.6).
- `providers.toml` (key-ring): provider → key pool → routing weights (03).
- `.env` fallbacks: `ANTHROPIC_API_KEY` etc. as last-resort single-key fallback (pi pattern, doc 19 §1).
