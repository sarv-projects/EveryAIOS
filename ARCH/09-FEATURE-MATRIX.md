# 09 — Capability → Feature → Module Matrix (the complete derivation)

> Every capability from the research corpus (docs 01–85, 282 repos) + the v2.0 matrix + the user's explicit requirements. **No scope cuts.** Status legend: 🟢 = exists (reuse from @personal-ai/core-*) · 🟡 = new (build) · 🔵 = new-in-Rust (everyaios-*) · ⚪ = later/optional. Module refs: sidecar = packages/coordinator + core-*; Rust = crates/everyaios-*; UI = ui/.

## A. Model & BYOK layer

| # | Capability | Feature | Module | Status | Source |
|---|---|---|---|---|---|
| A1 | Multi-provider BYOK | ProviderAdapter: anthropic/openai/responses/azure/bedrock/gemini/openrouter/deepseek/openai-compat/ollama/llamafile | sidecar (core-providers) + Rust vault | 🟢+🔵 | doc 19 |
| A2 | **Multi-key per provider** | Key rings: add N keys/provider, priority+weight, per-key model filter, budgets, health | Rust everyaios-vault | 🔵 **NEW (user req)** | ARCH/03 + doc 19 §7, doc 53 §2, doc 41 cc-switch |
| A3 | **Auto-failover rotation** | 429/401/5xx → cooldown → immediate next key; max-switches; all-fail backoff | Rust everyaios-vault | 🔵 **NEW (user req)** | ARCH/03 + doc 19 §7, doc 53 §2, doc 41 cc-switch |
| A4 | OAuth subscriptions | chatgpt-pro (PKCE) / copilot·qwen (device-code), encrypted tokens, same fallback semantics | Rust everyaios-vault + sidecar | 🔵 (⚠️ chatgpt-pro calls the unofficial `chatgpt.com/backend-api/codex/v1` — kept user-driven, Hermes/OpenCode pattern; ToS-risk documented, doc 57 §3; the URL is registered in the broker's `DEFAULT_BASE_URLS` but **inert without an OAuth-acquired token** — the OAuth flow is `EVERYAIOS_OAUTH=1`-gated) | 33 §7.4, 13 §5.5 |
| A5 | Local models | Ollama managed + llamafile single-binary + MLX (Rapid-MLX, Mac); agent-native class (doc 61); hwfit before spawn. **UI:** Discover = live HF Hub (no hardcoded names) + My models + Hardware panel. **P27 landed 2026-08-23 (6/6, UI wiring follow-on):** Range downloader, store/`local://` broker, GGUF serve, process probe (`everyaios-core::models` — `hf`/`store`/`local_url`/`probe`/`ModelsRuntime`). **v3.46 (P39.4):** adaptive KV-cache type knob (llama.cpp `-ctk`/`-ctv`, verified in `common/arg.cpp`) on the GGUF runtime bind | Rust spawn + UI | 🟢+🔵 | 34 §2 + 33 §7.4 + 61 + 79 |
| A6 | Model catalog + hints | capabilities (tools/vision/ctx), router picks per task; **baseline = models.dev catalog (doc 66 §1 — MIT, vendored `models.json` snapshot; 186 prov / 364 models at snapshot, live count drifts)** with the **two-tier lab/provider schema + `base_model` override-only inheritance** (canonical model layer + per-host cost/limits — the model-family vs transport-provider adapter split); BYOK providers added as overrides; **agent-scoped model surface (doc 68 §4):** hosted agents (F12/J17) expose their own models via `available_commands`/config — the full catalog is surfaced only in the native-engine picker (intent-first + power-user drawer), never a global model grid | sidecar core-providers + new `everyaios-catalog` | 🟡 | doc 19 + core-providers pi.dev catalog (15 prov / 280 models) + 66 §1 + 68 §4 |
| A7 | Asymmetric tiering | planner_model / subagent_models / depth=2 / concurrency=6 / writers=3; **agent-scoped routing (doc 68 §4):** hosted agents route on their own model surface (agent picker → `available_commands`/config), the native engine routes on intent-first tiers (Fast/Quality/Private/Cheap); Rust tier core now supplies role config, mode packs, strategy vocabulary, shortest-path chains and escalation-by-floor. **Honesty (v3.39 → v3.55):** the 13-factor OmniRoute scorer stays vocabulary-only (never public); the *consensus* scorer (`routing::Scorer::score` + `RouteDecision`/`ProviderObservation`) is landed and `router.ts` ports it — the coordinator records live observations per turn (`observations.ts`) and feeds them into `selectModelForTask`; without observations `router.ts` falls back to capability-filter + cost-sort; `tier.rs` = vocabulary, not the scorer loop | Rust everyaios-vault::tier + sidecar integration still open (the 13-factor OmniRoute scorer stays vocabulary-only by design) | 🟢 (consensus scorer + RouteDecision landed in everyaios-core::routing; coordinator router.ts ports it and feeds live observations per turn — v3.55) | doc 16/05 + 65 §5 (career-ops weighted A-F scoring → routing scores) + 68 §4 + 59 |
| A8 | Local OpenAI-compatible server | expose engine on localhost for VS Code/Cursor reuse | Rust everyaios-mcp (additional endpoint) | ⚪ | v2.0 §P3 · **P9.5 post-v1 — zero work yet; 🟡 corrected to ⚪ 2026-08-18** |
| A9 | Cache-aware costs | cache_read/cache_write/$ per call, key-affinity; **per-model `input_cache_read`/`input_cache_write` pricing from the models.dev catalog (doc 66 §1.3)** feeding the 3-layer cache stack + routing | sidecar + Rust audit | 🔵 | doc 05 + ARCH/05 + 65 §1 (codeburn usage-parser registry + efficiency metrics) + 66 §1 |
| A10 | Image generation | text-to-image + image-to-image provider endpoint (GPT-Image-1 / DALL·E 3 / Flux / Stable Diffusion / any MCP image server); key-ring + failover (A2/A3); ref-handle results | sidecar core-providers + vault | ⚪ | doc 50 |
| A11 | Provider Record + alias layer (v3.56) | provider = first-class identity, not `if provider ==` branches: `ProviderRecord` (id/aliases/name/transport/openai_chat|anthropic_messages|codex_responses|bedrock_converse/auth/api_key|oauth_device_code|oauth_external|external_process|aws_sdk|vertex|keyless/api_key_env/base_url+env override/is_aggregator vs is_routing_aggregator/models_source/capabilities/source/health/config_hash) merged from **models.dev (canonical live DB) + provider overlays (Hermes `HERMES_OVERLAYS` pattern — transport/auth/base-URL/env metadata models.dev doesn't track) + user config + plugin profiles**; **alias normalization to canonical ids** (claude→anthropic, kimi/moonshot→kimi-for-coding, glm/z-ai→zai, nim/nvidia-nim→nvidia, dashscope/aliyun/qwen→alibaba, hf→huggingface, aws/amazon-bedrock→bedrock, ai-gateway→vercel, zen→opencode, lmstudio/ollama/llamacpp→local); **OpenAI-compatible = one transport, many profiles** (Baseten/Cerebras/DeepInfra/DeepSeek/Fireworks/Groq/OpenRouter/Together/xAI + user base-URL override — OpenCode pattern); **capability-probe verification** (advertised vs verified: `/v1/models` listing, tool-call round-trip, context probe → `capabilities_verified_at`; **2026-08-26 probe-safety policy: `CapabilityProbePolicy { MetadataOnly · SafeSynthetic · Billable }` — default MetadataOnly, SafeSynthetic only when the provider declares a safe no-op test, Billable only on explicit user action — autodiscovery never spends money or triggers an external op**); live models.dev fetch + cache + pin, never a frozen list | Rust everyaios-catalog (extend) + vault | 🟡 New | Hermes `hermes_cli/providers.py` (source-read 2026-08-26) + opencode.ai/docs/providers + doc 66 §1 |

## B. Agent orchestration

| # | Capability | Feature | Module | Status | Source |
|---|---|---|---|---|---|
| B1 | Agent loop (pi-style) | streaming, length-guard (fail truncated tool calls), model-swap hook, cost ledger | sidecar core-engine | 🟢 (length-guard landed 2026-08-29: broker `assemble_tool_calls` drops length-truncated tool calls; loop port stays ARCH/16) | doc 05/16 |
| B2 | Spec-driven blueprints | .md → agent registry; continuous plan rewrite; dependency resolution; resume-after-reboot; **engine landed (Rust):** `everyaios-blueprint` — `.md` blueprint parser + `BlueprintRegistry`, status-block plan rewrite (`set_status`), dependency resolution (`topological_order`), resume-after-reboot + circuit-break freeze (`checkpoint.rs`), DAG state machine (`TaskStatus::transition`), plan cache (`plan_cache.rs`); **durable execution reference (doc 67 §2.2 — durable-execution-the-hard-way):** checkpoint task state to the audit event log, replay/retry steps from last checkpoint on crash (still open) | Rust everyaios-blueprint + sidecar loader | 🔵 (core); 🟡 doc-67 replay | v2.0 §P2, doc 03 + 67 §2.2 |
| B3 | Sub-agents | role isolation, own context+workspace, DELEGATE_BLOCKED_TOOLS; **core landed (Rust):** `everyaios-blueprint::subagent` — fresh-context `SubAgentSpec` (spec + per-agent model + workspace), `DELEGATE_BLOCKED_TOOLS` + `effective_tools()`, summary-only `SubAgentResult`/`parent_sees_summary`, depth/concurrent/total guards (`no-recursive-spawn`), `spawn_batch` fan-out; live LLM spawn = coordinator wiring. **v3.39:** derived child permissions (default-deny task/todo) + inbuilt abort/termination events. **v3.40 Scout:** read-only research child; clone deps into path-floored cache, never write the workspace | Rust everyaios-blueprint::subagent + sidecar | 🔵 (core); 🟡 live spawn | doc 16 |
| B4 | Inter-agent messaging | peer-review, cross-check, request sub-routines; no recursive spawn; **core landed (Rust):** `AgentMessage`/`AgentMessageKind` + `SubAgentRuntime::route_message` endpoint validation | Rust everyaios-blueprint::subagent | 🔵 (core); 🟡 delivery | doc 03 |
| B5 | Grammar-enforced extraction | ```blocks → tool calls (weak models) | sidecar core-engine | 🟢 | v2.0 §P3 |
| B6 | Iteration budgets | per-agent turn caps; subagent caps (Hermes 500/50); **core landed (Rust):** `everyaios-blueprint::iteration` — `IterationBudget` (500/50 + execute-code refund), `LoopDetector` (hash last N, 3× repeat), `TimeoutPolicy` (900/1800), `CircuitBreak`/`McqOption` card shape + `CircuitBreaker::step`; **MCQ card render landed (v2 UI, 2026-08-16 — `mcq-interrupt-card.tsx` + `pushMcq`/`respondMcq`, live-wired for Guard-2 tickets); coordinator `chat/interrupt` emit LANDED (2026-08-18, Stage-0 seam)** — `everyaios-core::PlanService` per-plan breaker over `plan/*` JSON-RPC, `ChatRelay` `chat/interrupt`/`chat/plan_done` forwarding + `start_plan`/`respond_plan`, coordinator `plan.ts` live plan executor (LLM turns → `plan/step` → trip → `chat/interrupt` → await `plan/respond` → `chat/plan_done`), Tauri `plan_execute`/`plan_respond`, UI MCQ card submits the SELECTED option → `planRespond` (117 core / 78 coordinator tests) | Rust everyaios-blueprint::iteration + sidecar | 🔵 (core); 🟡 (card render), 🟡 (emit) | doc 16 + 65 §9 (loop-engineering budget-guard / early-exit) |
| B7 | Scheduled tasks | cron/interval/event/webhook + event triggers; heartbeat lease + missed-heartbeat reassignment + checkpoint resume (landed); **v3.53: BackgroundTaskRecord detached-work ledger (OpenClaw tasks pattern)** — queued→running→terminal{succeeded,failed,timed_out,cancelled,lost}; push completion; execution≠delivery (fenced retry, `blocked`); lost = no live authority + no durable evidence after grace (5-min class); 7-day retention; tasks list/show/cancel/retry/audit | core::scheduler_service | 🟢 | docs 33/67 + docs.openclaw.ai/automation/tasks (v3.53)l/event/webhook; nudge sentinels (suggest_schedule); **heartbeat automations (doc 67 §2 — Hatchet lease pattern):** a scheduled run reawakens the **same conversation with its context intact**; worker heartbeat + missed-heartbeat → task reassignment/resume from the last audit-event checkpoint (durable-execution-the-hard-way lessons → Rust port path); automation step binding now has a fail-closed Rust adapter seam | sidecar core-automations + UI + Rust everyaios-core | 🟢+🟡 | 33 §7 + 67 §2 | **✅ P6.4 landed (2026-08-18): `everyaios-core::scheduler_service`** — 5-field `CronExpr` + `TriggerSpec::{Cron,Interval,Event,Webhook}`, `EventKind` (Gartner: CI-fail/test-regression/repo-change/ticket-assign/telemetry-threshold) w/ filter+scope+rolling-1h frequency cap, battery suppression, HITL `Paused{resume_deadline}` state, retry backoff+jitter+clamp, **heartbeat lease (30s) + missed-heartbeat reassignment + checkpoint resume** (non-determinism guard = completed steps never re-executed), nudge sentinels (3-day rule → `NudgeSuggestion` cron); coordinator `scheduler.ts` (due-ticker, lease heartbeats, `Bun.serve` loopback webhook, session reawakening via `surface:"automation"`); tray "Run automations now" (`tick_scheduler`); Tauri `scheduler_*` commands; H14 UI (AutomationsPanel live + nudge cards in composer). 17 scheduler tests + relay dispatch + 5 coordinator executor tests |
| B8 | Crystallization | multi-step workflow detection/classification → deterministic skill source, skill registry, drift fallback; crate contract is model-free, host execution remains adapter-owned | Rust everyaios-blueprint::crystallize + everyaios-core automation runtime | 🔵 (core) | v2.0 §P7 + doc 63 |
| B9 | Custom Agent Builder (agent-authoring) | named agent = versioned bundle (I6 + persona-manifest): persona/system-prompt + engine (inbuilt / ACP agent / model-only) + model/provider (optional = inherit chat-bar) + per-agent MCP subset + per-agent connector subset + skills + blueprints/workflows (B2/B7) + tool allow/deny (Guard scoping); templates + Default = inbuilt EveryAIOS + installed = ACP registry + custom = `~/.everyaios/agents/`; **v3.45 Dynamic Chief:** any registered agent (inbuilt · ACP-installed · model-only) can be the `primary_chief` (session top brain); subagents may be other agents | Rust `everyaios-acp` (LaunchRegistry) + `everyaios-guard` (scoped grants) + `everyaios-blueprint` | 🟡 | user directive 2026-08-17 | **✅ P31 + P38 (2026-08-24): `everyaios-agents` bundle/registry/templates/scopes/workflows + 4-step builder UI + agent_registry_* Tauri commands (agent.toml save/list/get/remove/duplicate/disable over `~/.everyaios/agents/`); `primary_chief` slot + ChiefAdapter/GovernedSession + dispatcher (inbuilt | ACP) — chief picker + Work-survives-Chief. UI wiring complete; live agent runs = coordinator seam.** |

## C. Memory & context

| # | Capability | Feature | Module | Status | Source |
|---|---|---|---|---|---|
| C1 | C-series memory/context plane + 34-algorithm index | polarized, compass, phantom thread, anticipation, spreading activation, KG+conflict, decay, retrieval/fusion/rerank, paging, compaction, ACT-R, graph/provenance, reference handles, FSRS, and the remaining indexed algorithms | Rust-native memory/core services | 🟢 | 07, v2.0 §3 |
| C2 | Multi-tier memory | sensory/working/episodic/semantic/procedural + Letta paging; **v3.39:** branch/lineage memory + `maintain()` tools | sidecar | 🟢+🟡 | 07, 34 §2 |
| C3 | Multi-signal retrieval | FTS5+vec+graph+temporal fusion, budgets, snippets; **v3.39:** abortable fusion | sidecar (new fusion) | 🟡 (P5.1 fusion core ✅) | 07, 34 §2, 32 | **✅ P5.1: `everyaios-memory::fusion` — weighted RRF (`rrf_fuse`), dedupe, smart snippets, per-type budget caps, chunk-min-size merge. Cross-encoder rerank (#19) still open.** |
| C4 | Vectorless default | FTS5/BM25 retrieval without embeddings | sidecar core-files | 🟢 | 32, 34 |
| C5 | Embeddings (optional) | on-device bge-micro/gte-small, int8/vec0 | sidecar core-files | 🟢 | v2.0 §P4 |
| C6 | Knowledge graph store | Rust-native embedded graph (LadybugDB = optional/deferred Kuzu-fork backend — Kuzu abandoned Oct 2025; C++, ACID, Cypher, vector+FTS built-in), temporal edges; **v3.39:** EXTRACTED vs INFERRED + source span on edges | Rust everyaios-memory | 🟡 (P5.2 ✅) | 07, 34 | **✅ P5.2: `graph` — Rust-native `GraphStore` (entity/epigraph nodes + typed supports/contradicts/derived-from edges, temporal `valid_from`/`valid_to` versioning, `spreading_activation` Alg #6, `query_depth` d=2/top-k=15). LadybugDB C++ FFI deferred (same schema, swap-in backend). v3.51: optional **lazy concept-graph mode** (LazyGraphRAG pattern — MSR 2025, search-verified) — query-time concept graph (NLP noun-phrase + co-occurrence, iterative-deepening search, `relevance_budget` knob) → indexing ≈ vector RAG, ~0.1% of full GraphRAG index cost; tracked as TODO P5.12.** |
| C7 | Memory injection | warm set 0ms TTFT, scope-leakage floors, budgets | sidecar + 05 | 🟢+🟡 | 07 + 65 §10 (claude-mem saved-vs-discovered token economics) |
| C8 | Sync/export/wipe | E2E-encrypted sync (opt-in), export, per-scope wipe | sidecar core-sync | 🟢 | v2.0 §P8 | **✅ P8.9 landed 2026-08-22: `everyaios-core::export` — `render_markdown_export`/`render_json_export`, `MemoryMirror` Obsidian `[[wiki-link]]` notes (view surface, not a second store), `WipeScope` + `wipe_facts`/`wipe_messages` per-scope wipe (no-silent-nuke). E2E sync now **landed, not just a seam**: `everyaios-core::sync` — ChaCha20-Poly1305 AEAD envelope (header-as-AAD), X25519 `KeyPair`/`KeyExchange` + confirm handshake, version vectors + tombstones, 3-way `reconcile` → `SyncDiff`, **F2 conflict policy landed 2026-08-25** (`ConflictPolicy` Manual/LWW/KeepBoth + `resolve_conflicts` + `reconcile_with_policy` + `SyncItem::ts_ms`, 6 tests — 25 total), `SyncBundle` export/import, `SyncTransport` seam (25 tests) — + `sync_transport.rs` live TCP transport (length-prefixed framing, ECDH handshake, `SyncServer` on 47615, `sync_with_peer`, LAN/Tailscale) + 8 Tauri commands + Settings → Connections UI.** |
| C9 | **Taste profile** | auto-learned coding-preference profile (style/patterns/frameworks/naming) with confidence scores 0–1; stored as shareable markdown (`~/.everyaios/taste/` + per-repo `.everyaios-taste/`); injected as a stable-prefix symbolic prior at generation; learns from accept/reject/edit via correction-detector + audit (Command Code taste-1 pattern — proprietary, pattern only) | Rust everyaios-memory | 🟡 (P5.6 ✅) | doc 37 | **✅ P5.6: `everyaios-memory::taste` — `TasteStore` (confidence-scored rules, `observe_accept/reject/edit`, `inject_stable_prefix`, markdown round-trip, dir save/load).** |
| C10 | **Pass-by-reference context** | files/datasets/tool results exposed as **live handles + bounded previews** (head/tail + type metadata); agent queries/slices them via sandboxed script-eval (E4) instead of serializing payloads into context (NOOA pattern — never serialize what you can reference) | Rust everyaios-memory + E4 script-eval | 🟡 (P5.8 ✅) | doc 39 | **✅ P5.8: `reference` — `RefHandle` (kind/size/row metadata) + `bounded_preview`/`make_ref_handle` (≤2K tokens; 10MB→≤2K test). E4 script-eval query path = follow-up.** |
| C11 | Temporal knowledge graph | Graphiti-pattern bi-temporal entity/fact tracking with validity windows + contradiction resolution | memory-kg (new) | 🔵 New | doc 46 (Graphiti) |
| C12 | Full-stack memory on Postgres/SQLite | Cognee-pattern: KG + vectors + sessions + ontology on single DB with remember/recall/forget/improve API; **doc 61:** every asset also exports to `~/.everyaios/memory/**/*.md` (readable/git-versioned — OpenHuman validation, view surface not second store); **v3.51:** note-object memory (A-MEM pattern, NeurIPS 2025) — atomic notes + typed links + evolution (link/merge/archive) as a third memory shape, same store/export | memory-store | 🔵 New | doc 46 (Cognee) + 61 + A-MEM (NeurIPS 2025) |
| C13 | Spaced-repetition (FSRS) | Port anki's Rust FSRS scheduler into everyaios-memory: retention-target scheduling, reschedule-on-review, simulator; "reinforce what I learned" review prompts | everyaios-memory | 🔵 New | doc 63 §2.2 (anki rslib/src/scheduler/fsrs) + fsrs-rs v6 | **✅ landed: `everyaios-memory::fsrs` (state prediction + next-interval scheduling) + simulator; `reinforce` (candidate extraction + due-review queue) + `bm25` signal + `planner` (C7) + `janus` structural passes + `cognee` graph API + `rtk` output compression + `usage` ledger; numbered **Algorithm #34** (v3.51); FSRS-7 upstream line (2025) tracked as adopt-when-shipped watch item (permissive fsrs-rs v6.x doesn't carry it yet)** |

## D. Office & files (user-critical) — ⏸ **ON HOLD (2026-08-22 user directive; partial lift 2026-08-26 — honest-viewer tier landed)**
> **Frozen except the honest-viewer lift.** D1–D8 engine + D9–D12 storage are landed and stay landed (P4.1–P4.8, tested). **Partial lift 2026-08-26 (doc-29 verdict):** P33.3 per-file tabs · P33.5 LibreOffice optional companion · P33.6 Google Docs/Sheets browser read path · P34.2–4.5/4.7 honest viewers (block list + selected-block `docx_patch` + track-changes · Excel ribbon + formula bar + Guard-2 edits · PPT slides rail + `pptx_notes` · PDF zoom/find/thumbnails + annotate/redact/form-fill · read-only-while-agent + takeover locks). Full-fidelity tiers (in-process LOKit, Google full surface, MS-ribbon clones, freeze/autofilter/CF, sign/stamps/canvas draw, Copilot panes) remain **post-hold** ("only if Univer-as-view, later"). **V1 Office (shipped):** honest viewer · supported surgical edits · Guarded mutation · rollback · conformance. **Post-hold Office (not shipped):** full-fidelity viewer · richer UI · full Google surface · Impress-like behavior · perfectness-gap features. Rows record landed status.

| # | Capability | Feature | Module | Status | Source |
|---|---|---|---|---|---|
| D1 | **Word open+edit** | block-patch engine, byte-preserving w:t, headers/tables/sections | sidecar office/docx | 🟡 **NEW** | 28, 04 | **✅ P4.1 landed: `crates/everyaios-office` — `OoxmlArchive` (ZIP open, parts index, `save` with `raw_copy_file` verbatim untouched entries — byte-stability proven by raw compressed-payload comparison) + `docx/` block-patch engine (anchored block tree w/ addresses `p1`/`t1:r1c2:p1`/`hdr1:p1`/`sec1` over body+headers/footers, plain-text render, minimal `w:t` prefix/suffix patch via byte-surgery with entity/UTF-8-aware splits, safety fallbacks NoTextAnchor/StaleEdit/PatchAcrossMarker). 30 tests — 393 ws tests (363 + 30), clippy 0, fmt clean. LibreOffice headless oracle → P4.5.** |
| D2 | **Excel open+edit** | IronCalc recalc + calamine read + workbook DSL + deterministic planner + flash-fill/pivot | Rust sidecar + sidecar | 🟡 **NEW** | 28, 04 | **✅ P4.2 landed: `crates/everyaios-office/src/xlsx/` — `read.rs` (calamine 0.30 windowed reads), `recalc.rs` (ironcalc 0.8.3 truth engine — every computed number engine-made (deterministic recalc, never LLM-invented)), `dsl.rs` (WorkbookCommandBatch + Excel-accurate formula-shift: `$` doesn't pin, deleted→`#REF!`, shrink, LOG10/string/sheet-prefix protection), `planner.rs` (regex NLP → DSL, zero-LLM; `NeedsLlm` fallback), `patch.rs` (surgical sheetN.xml/sharedStrings.xml + workbook rename, byte-stable); `src-tauri xlsx_open` (windowed) + `ui/pages/Spreadsheet.tsx` virtualized 100K+ row grid. 64 office tests (+34), 427 ws tests, clippy 0, fmt clean. Univer = H5 view (P4.7), IronCalc = the one calc truth engine (doc 58).** |
| D3 | **PPT open+edit** | surgical OOXML part editing (slides), add/remove slides, text/shape ops | sidecar office/pptx | 🟡 **NEW** | 04 | **✅ P4.3 landed: `crates/everyaios-office/src/pptx/` — `parts.rs` (content types + presentation rels + `<p:sldIdLst>` slide order), `text.rs` (addressable `<p:sp>` shapes → `<a:p>` → `<a:r>` → `<a:t>`; render + minimal `<a:t>` byte-surgery patch, bullets `<a:buChar>`/`<a:buAutoNum>` as read-only markers, multi-byte/entity-aware; NoTextAnchor/PatchAcrossMarker fallbacks), `mod.rs` `PptxEngine` (render/patch + `add_slide` clone part+rels+`[Content_Types].xml` registration + `remove_slide` deregistration; `zip::save_changes(modified, added, deleted)` so untouched parts stay verbatim). 80 office tests (+16), 443 ws tests, clippy 0, fmt clean.** |
| D4 | **PDF open+edit** | render (pdf.js), form-fill/annotate (pdf-lib), text-swap (lopdf), redact, re-author; **v3.40:** `annotationStorage` round-trip persist | sidecar + Rust bridge | 🟡 **NEW** | 04 | **✅ P4.4 landed (engine): `pdf/` — `form::form_fill` (lopdf 0.36, AcroForm `/V` + hierarchical field walk), `replace_text` (exact-match `Tj`), `redact::redact` (`/Redact` annotations), `author::author_pages` (re-author new PDF). 6 pdf tests (100 office), 463 ws tests, clippy 0, fmt clean. pdf.js renderer landed (P4.7: `pdf_bytes` base64 + `PdfViewer.tsx` canvas draw + page nav/zoom, React.lazy code-split); free-text/highlight annotation → follow-up.** |
| D5 | Universal read/ingest | markitdown-class extraction → RAG, chat overlay; **v3.39 `DocumentAsset` provenance** (uri, converter, versions, hashes) | sidecar core-files | 🟢 | v2.0 §P1 |
| D6 | Round-trip conformance | LibreOffice oracle in CI, byte-stability asserts | CI | 🟡 | 29, 04 §4.4 | **✅ P4.5 landed: `conformance.rs` — `parts_diff` (changed/added/removed zip parts, decompressed compare) + `LibreOfficeOracle` (`find_soffice` PATH+install-dirs, `check_opens` = `soffice --headless --convert-to pdf`, fails on repair/damaged warnings; gated `#[ignore]` live test).** |
| D7 | Rollback | snapshotBefore, atomic writes | sidecar office | 🟡 | 28 §2 | **✅ P4.5 landed: `atomic.rs` `write_atomic` (temp → fsync → rename + dir fsync) + `rollback.rs` `Snapshot` (capture/record_save/undo/dirty — snapshotBefore).** |
| D8 | Legacy formats | .doc/.xls/.ppt → convert-on-open, read-only | Rust + optional soffice | 🟡 | 04, 29 §3a | **✅ P4.6 landed: `legacy.rs` — `LegacyKind` (.doc/.xls/.ppt), `convert_to_modern` (headless soffice → .docx/.xlsx/.pptx), `LegacyOpen` read-only + edit-as-new. 94 office tests (+14), 457 ws tests, clippy 0, fmt clean.** |
| D9 | **Storage intelligence** | parallel work-stealing disk walker (crossbeam-deque, cycle/device-boundary-safe) + immutable arena snapshots (arc_swap, zstd save/load) + squarified treemap + per-dir aggregation; Guard-2-ticketed cleanup proposals | Rust everyaios-storage | 🟡 (P4.8 ✅) | doc 49 | **✅ P4.8 landed: `everyaios-storage` — `walk` (per-thread `crossbeam_deque::Worker` + steal loop, symlink-skip + `same_filesystem` device guard, `build_arena` u32-indexed `FileNode` arena w/ bottom-up aggregation), `snapshot` (arc_swap store + zstd round-trip), `treemap` (Bruls–van Wijk `squarify` + `treemap_for_dir` + xxHash3→HSL `color_for`), `dedup` (7-stage size→xxHash3→BLAKE3, hardlink-aware, reflink-eligible), `finder` (top-N size/age + ext filters), `cleanup` (Guard-2 decision-package proposals — crate never deletes), `search` (FTS5 prefix index + debounced notify watcher), `health` (90% threshold + sysinfo `Disks`). 15 tests, 479 ws tests, clippy 0, fmt clean.** |
| D10 | Duplicate detection by hash | 7-stage pipeline (size → xxHash3 prefix/suffix → BLAKE3), hardlink-aware, reflink-eligible flag, group reports; **v3.40:** persistent hash cache + delta re-scan | Rust everyaios-storage | 🟡 (P4.8 ✅) | doc 49 (fclones + eDirStat) | **✅ P4.8: `dedup::find_duplicates` (size→xxHash3 prefix/suffix→BLAKE3→hardlink dev+ino→reflink-eligible→wasted-bytes, fclones ordering).** |
| D11 | Large-file finder | top-N by size/age + filters + cleanup proposals | Rust everyaios-storage | 🟡 (P4.8 ✅) | doc 49 (WinDirStat feature list) | **✅ P4.8: `finder::find_large_files` (`SortBy::{SizeDesc,AgeNewest,AgeOldest}` + include/exclude extensions + min-size/max-age).** |
| D12 | Storage health & analytics | drive-threshold monitoring (90% full), agent-suggested cleanup plans (Guard-2 approved), dashboard (free space / top files / duplicates / trends) | Rust everyaios-storage + UI | 🟡 (P4.8 ✅) | doc 52 §3 | **✅ P4.8: `health` (`over_threshold` 90% + `check_health` via sysinfo `Disks`) + `cleanup::propose_*` plan inputs; storage tools runtime dispatch + Tauri `storage_cmds` + J16 battery gating + live `storage-view.tsx` dashboard landed 2026-08-20.** |

## E. Browser & computer use

| # | Capability | Feature | Module | Status | Source |
|---|---|---|---|---|---|
| E1 | CDP child browser | system Chrome/Edge + chrome-for-testing fallback; loopback-only discovery; version-tolerant client (P2.1 ✅) | Rust everyaios-cdp | 🔵 | 33, 34, 08 |
| E2 | 37-tool catalog (34 core + 3 file_ops) | tabs..run + bookmarks, tab_groups_manage, windows (8.2) — engine + 37-tool registry (P2.3 ✅; bookmarks/tab_groups gated: no CDP surface on stock Chrome); **v3.40:** read-only diagnostics (console/network/perf) on the same CDP session | Rust everyaios-browser (actions/read) + everyaios-mcp | 🔵 | 33 §6, 46, 55 |
| E3 | A11y snapshot/diff | refs [eN], interactive mode, URL-change short-circuit, iframe stitching (P2.2 ✅) | Rust everyaios-browser | 🔵 | 33 §5, 55, 08 |
| E4 | Script-eval (run) | rquickjs sandbox + browser SDK + InnerCallHook | Rust everyaios-script | 🔵 (P2.5 ✅: 64MB/512KB/30s/1K/2MB limits, SDK surface, InnerCallHook audit, ownership filtering — 14/14 tests) | 33 §6.3, 08 | **✅ P7.8 FS syscall broker for E4: `everyaios-guard::fs_broker` (BrokerHost + BrokerOp over BrokerTransport — path floor → profile rule → create-parent; `InProcessBroker`; IPC wiring = runtime)** |
| E5 | Session replay | injected recorder → NDJSON → SQLite; scrubber UI; has_gap; **v3.40:** optional HAR beside NDJSON | Rust everyaios-audit + UI | 🔵 **NEW** | 33 §9, 08 | **✅ P2.10 landed: `everyaios-browser/src/replay.rs` injected recorder (CDP `Page.addScriptToEvaluateOnNewDocument`; click/input/keydown/scroll/mutation capture; `x-recording-*` header contract; failed flush → sticky gap) + `everyaios-audit/src/replay.rs` ingest (`ingest_ndjson`/`ingest_batch`, chrome doc-id validation, dedupe-stable receipts, sticky `has_gap`, SQLite index one-tx commit with file-rollback, `ReplayStore` NDJSON + screenshot JPEGs, 7-day `retention_sweep` + `wipe`) + `session_log.rs` durable event log (10 §4.2 types) + idempotency classes (safe/unsafe/same-key/confirm) + `recovery_plan`. 16 audit + 5 browser tests; scrubber/audit UI = P3.1 landed.** |
| E6 | Tab ownership | mine/user/other-agent; claims; group-per-agent | Rust everyaios-browser + audit | 🔵 (P2.6 ✅: TabRegistry + sync/claim/release/can_close + tab_claim audit events + close_agent_group — 11 tests) | 33, 08 |
| E7 | Login import/sessions | capture-in-browser sign-in (vault path 1); optional Chrome profile import (path 3) | Rust everyaios-browser | 🔵 | 33 §3.2, 08 §8.9 |
| E8 | Authenticated scraping | logged-in sessions → tiered scrape → RAG | Rust+sidecar | 🟡 | 01/06 |
| E9 | Desktop computer-use (native + vision) | **Required ChatGPT+Claude parity, not a cut.** WGC occluded HWND capture + UIA invoke-first + SendInput + OCR fallback + see-pane overlay/Esc + Guard-2 allow-list/confirmations/hard-denies + observe→one-act→assert. Browsers stay CDP. **Not built — but P9.1 landed the engine 8/8 (2026-08-23):** `everyaios-desktop` — `SeeMethod`/`WinUia::read` (UIA tree + indexes + click-by-name) / SendInput + XTEST + osascript / `ocr::VisionHit` word-boxes / `verify::{Verifier,Locator}` / `router::Layer1Target` / `policy::{AppPolicy,ActionTaxonomy,GuardDecision}` / macOS twin. **P48.3 (2026-08-29):** engine attached to the effect funnel — `src-tauri/src/desktop_cmds` (lazy `DesktopEngine`, `desktop_status/windows/read/see/act/stop`); each `desktop.act` is Merkle-audited (human_gesture) + passes the engine's Guard-2 preflight with a **fail-closed** gate (risky classes Deny; hard-denies/kill-switch/rate-limit/safe-zones intact); crate **renamed `everyaios-computeruse`** to disambiguate from the src-tauri shell package of the same name. Honest ⚪ retained: desktop is not yet a loop tool, so the agent path is unattached. **Hermes computer-use implementation rules (2026-08-26, source-read):** H1 `DesktopSnapshot` (screenshot_ref+AX tree+interactive elements+stable `eN` refs — SOM pattern) · H2 **act→invalidate→re-observe→verify hard invariant** (shared with E3) · H3 Background/foreground policy (default background, no cursor/focus steal; foreground only for modality/user-ask) · H4 readiness as ● Ready/⚠ Perm-required/⚠ Driver-missing/⚠ App-unsupported/✕ Policy-blocked (feeds H35 doctor; never a binary “works”) · H5 opaque session-owned driver state (owner_work/agent/run id; no cross-agent reuse without transfer) + child-env sanitization (no API keys/vault secrets inherited by driver processes). H2's code follow-up (explicit ref-invalidation) **landed 2026-08-30 (P46.4)** — `everyaios-browser::locator::RefRegistry` enforces act→invalidate→re-observe via `BrowserActions::reject_stale_refs` (a pre-mutation ref is refused with a "re-observe before acting" error until a fresh snapshot). Full wording in spec E9 row. | Rust Windows (cfg) + UI viewport | 🟡 | 09, 48, 52 + ChatGPT Computer Use + Claude CUA + Hermes computer_use (2026-08-26) |
| E10 | **Lightweight engine tier** | Lightpanda (Zig, **default**, AGPL — ~16× less memory, vendor benchmark) + **Obscura (Rust, opt-in — 21K★, source-verified doc 55: own CDP server 14 domains + LP.getMarkdown, embedded MCP 32 tools, scrape workers, SSRF/file:// defaults, ~30MB RSS)** via CDP; tier 0 static→1 lightweight→2 full escalation; adapt = spawn `obscura serve` via ProcessSupervisor | Rust everyaios-cdp | 🔵 **NEW** | 08 §8.8, 55 | **✅ P2.4 landed: `everyaios-browser/src/tiers.rs` — `TieredEngine` (tier 0 `read_http`+`html2md` with SSRF/file://domain guards; tier 1 Lightpanda/Obscura `serve` spawn with `--block-private-networks`/`--disable-workers`/bounded connections; tier 2 Chrome `--disable-features=WebRTC`), E8 escalation loop. LIVE-verified: static→Lightpanda→Chrome against example.com; Obscura binary absent → clean BinaryNotFound → escalates. 355 ws tests (246 at P2.4 + 24 P2.5/P2.6 + 44 P2.7/P2.8 + 12 P2.9 + 18 P2.10 + 3 P3.1 + 8 P3.3), 5/5 live, clippy 0.** |
| E11 | **Session Vault** | multi-account per site, encrypted **full storage context** (cookies + localStorage + sessionStorage + IndexedDB, Chrome leveldb decode, persist/restore — doc 55) in SQLCipher, Trust-Ladder-gated access (agent never sees raw cookies), rotation, usage audit, expiry nudges | Rust everyaios-vault + everyaios-browser | 🔵 **NEW (user req)** | 08 §8.9, 55 | **✅ P2.7 landed: `everyaios-vault/src/session.rs` `SessionVault` (schema v5, capture/grant/inject/rotate/expiry/audit) + `everyaios-browser/src/session.rs` cookie glue (`get_cookies`/`set_cookies`, `seal_session`/`inject_session`, `inherit_cookies_from_chrome` + `group_cookies_by_site`) — 11 vault + 10 browser tests, agent-never-sees-cookies + inject-without-grant-denied + E13 LIVE-verified. localStorage/IndexedDB capture is the CDP `DOMStorage`/`IndexedDB` follow-on.** |
| E12 | **Challenge handler** | PoW captchas solved locally + LLM visual-grounding + human-in-loop pass-through (default) + optional BYO solver API (user key) | Rust everyaios-core + sidecar | 🔵+🟡 **NEW** | 08 §8.10 | **✅ P2.8 landed: `everyaios-core/src/challenge.rs` — PoW `solve_pow`/`verify_pow`, human-in-loop single-use registry (`surface`/`resolve_human`/`pending`), visual-grounding contract (`route_visual` + `grounding_request` + `parse_grounding_choice`), BYO solver HTTP (`solve_captcha`/`create_task`/`poll_task`, `UreqHttp` transport, key via A2/A3 keyring) + 23 tests. Turnstile (incl. hidden) is Cloudflare-managed → NOT locally solvable (honest route). The sidecar makes the actual LLM grounding call; the UI consumes `resolve_human`.** |
| E13 | Session inheritance | live-attach to user's own Chrome profile via CDP debug port (vault path 2, no re-login) | Rust everyaios-browser | 🔵 **NEW** | 08 §8.9 | **P2.7 landed: `everyaios-browser/src/session.rs` `inherit_cookies_from_chrome(port)` — `probe_browser` (loopback-guarded) → `connect_to_browser` → `Storage.getCookies` (modern browser-target method; `Browser.getCookies` is gone in current Chrome) → `group_cookies_by_site`. Unit-tested via mock CDP transport (round-trip/normalization/malformed-cookie) — **a real Chrome debug-port attach is a manual live-verification step, not yet in the test suite (annotation corrected 2026-08-18; previous "LIVE-verified" overclaimed)**.** |
| E14 | Behavioral realism | humanized input events (Bézier mouse curves, typing cadence), optional per-site | Rust everyaios-browser | 🔵 **NEW** | 08 §8.10 (CloakBrowser pattern) + 65 §2 (Scrapling Camoufox fingerprint rotation) | **✅ P2.9 landed: `everyaios-browser/src/humanize.rs` — `BehaviorProfile` (Bézier `mouse_path` + per-key `typing_delays`, host allow-list, seeded RNG) wired into `BrowserActions::with_behavior`; off by default, per-site gated via `site_enabled(url)`; click/hover/drag take real `mouseMoved` paths (drag releases exactly), typing emits per-char `dispatchKeyEvent` with cadence. 12 tests: deterministic path, jittered endpoint, word pauses, host gating, plain-click has 0 moves, per-key sequence, exact drag target — 355 ws tests (246 + 24 + 44 + 12 + 18 + 3 + 8), clippy 0, fmt clean.** |
| E15 | Electron-app CDP automation | Attach to any Electron app's debug port (VS Code/Slack/Discord/Spotify/Notion): a11y snapshot, click/fill/read, screenshot — existing CDP stack, zero new deps | Rust everyaios-browser + mcp | 🔵 New | doc 63 §4.1 (agent-browser) | **✅ landed: `everyaios-cdp::discovery` (probe_electron/discover_electron_apps/is_electron_version, loopback-guarded) + `everyaios-browser::electron` (`ElectronHandle::attach(port)` → snapshot/click/fill/read/screenshot)** |
| E16 | Slim snapshots + WebMCP | `snapshot(slim: true)` (drop non-actionable, collapse long text, depth cap — chrome-devtools-mcp SlimMcpResponse) + web-native MCP handshake (WebMCP) | Rust everyaios-browser + mcp | 🔵 New | doc 63 §4.2 (chrome-devtools-mcp) | **✅ landed: slim = `tree.rs` `SnapshotMode::Interactive` (~90% token cut) + `everyaios-browser::webmcp` (handshake) + `webmcp_http` (HTTP transport, tools/list + tools/call)** |
| E17 | Multi-protocol action parsing | Per-provider action-protocol adapters (native/CUA/Anthropic/UI-TARS) behind the router — any BYOK provider's action format drives the same browser layer | sidecar coordinator (parse_actions) + Rust browser | 🟡 New | doc 63 §4.3 (skyvern parse_actions.py) | **✅ landed (Rust half): `everyaios-browser::protocol` — `parse_action` for Native/Anthropic CUA/OpenAI CUA/UI-TARS → canonical `ParsedAction` → `to_act_kind`** |

## F. Connector hub

| # | Capability | Feature | Module | Status | Source |
|---|---|---|---|---|---|
| F1 | Hub routing | **MCP-first (Connector-platform decision 2026-08-16):** MCP Servers (user-supplied, stdio/npx or user-hosted HTTP) + Native (BYO OAuth/API-key in vault) + Tool Catalog (live `everyaios-mcp` registry); **Composio/Zapier/Nango aggregator tabs removed** — no double-connect | sidecar core-connectors | 🟢+🟡 | 13, decision 2026-08-16 | **✅ routing core + coordinator APP-registry bridge landed; external account attach remains credential/provider gated.** |
| F2 | Native adapters | 27+ direct adapters | sidecar core-connectors | 🟢+🟡 | v2.0 §3 | **✅ APP `core-connectors` is now a live lazy coordinator dependency (`connector/list` + read/query plan); vault token resolver and write execution remain host-gated.** |
| F3 | Browser-session connectors | drive logged-in web apps via browser layer | Rust+sidecar | 🟢 | 13, 08 | **✅ `connectors::browser_session` — `BrowserSessionConnector<S: CdpSession>` with Gmail inbox / Notion page / Linear issues / Outlook inbox readers, click_element, type_text — all over mock CdpSession seam, 7 tests green. Live Chrome 136+ isolated profile is the runtime seam.** |
| F4 | Local Auth Bridge | project PKCE client, no secret, local token manager | Rust everyaios-vault | 🔵 | 13 §5.5 | **✅ `everyaios-vault::auth_bridge` — S256 PKCE + single-use state + loopback token manager → key ring (loopback mock-token E2E); live Google OAuth round-trip remains account-gated.** |
| F5 | Composio/Zapier/Nango | **REMOVED by Connector-platform decision (2026-08-16)** — cloud SaaS holding OAuth tokens server-side contradicts the local-vault promise; superseded by official MCP servers (Gmail/Slack/GitHub/Linear) + Native | — | ⚪ (removed) | decision 2026-08-16 |
| F6 | MCP client (consume) | connect external MCP servers, reconcile; **v3.39 `MCPServerRecord`** + per-consumer enable; lifecycle = `ManagedResource`; **v3.40:** resources + elicitation + sampling (Guard-2 / broker); **v3.46 (P39.3):** loopback HTTP keep-alive + connection pooling (today `serve_http_once` = fresh TCP per request); MRTR covers long-running ops | sidecar | 🟢+🟡 | 10 | **✅ APP MCP client is lazily consumed through coordinator `mcp/search` with HTTPS/loopback validation; live user-server availability remains external.** |
| F7 | MCP server (serve) | our tools to Claude Code/Codex/Cursor/... via one endpoint; **two-channel injection — Channel B (doc 68 §4):** serve Office surgical editor + IronCalc, browser 37-tool catalog + Session Vault, search cascade (G8), memory retrieval (C-series), storage intelligence as MCP tools — any MCP-consuming agent gets our full capability set; **v3.40:** C10 handles as MCP **resources** | Rust everyaios-mcp | 🔵+🟡 | 33 §8, 34 §2, 68 §4 | **✅ host-owned `tools/list`/`tools/call` protocol core over stdio + authenticated loopback HTTP, 42 native tools + reconciled externals, loopback E2E; production supervisor/external Claude-Code E2E remains open.** |
| F8 | Harness installer | plan-before-touch install into the **F12 harness set (46-agent catalog — list lives in F12, single source of truth)**, ownership markers (doc 33 §8 harness-integrations pattern); **registry-fed discovery from the official ACP agent registry** (doc 57 §2 — CDN catalog + local cache + version pinning + curated allow-list) | Rust (new) | 🔵 ✅ **landed — `registry_index` (parse + `install_plan` + `merge_into` + `RegistryPolicy`) + `registry_client` (fetch/cache/offline-fallback) + `installer` (download→sha256→extract→install-state) + **Guard-2-ticketed install split (`acp_install_request` mints the decision-package ticket / auto-allows → `acp_install_commit` consumes via `use_ticket` + executes; `acp_install_status` per-agent state) + picker Install button with progress → Launch + inline install card (same ticket as Cockpit) + agent `authMethods` surfaced at launch (`authRequired`; `acp_authenticate` — url-type opens the browser, agent-type completes in the agent's flow)** | 33 §8, 45, 52, 56, 57, 69 + 65 §6 (skills_index.json discovery manifest + compose_stack read-only validation) |
| F9 | Unified Tool Registry | one normalized ToolDefinition + permission classes; **adopts ACP tool-kind taxonomy** (read/edit/delete/move/search/execute/think/fetch/other, doc 45 §4.3) | sidecar core-tools | 🟢 | 10, 45 |
| F10 | WSL/POSIX bridge | `wsl.exe` runners, `\\wsl.localhost\` paths, loopback IPC, native script exec in Linux | Rust everyaios-core + sidecar | 🟡 | doc 03 §5, v2.0 §P5 | **✅ P8.6: `everyaios-core::wsl` — `translate_windows_to_linux`/`translate_linux_to_windows` (+ legacy `\\wsl$\`, drive→`/mnt`), `ExecEnvironment` + `detect_environment`, `WslRunner` over the injected `CommandRunner` seam, `WslFrame` length-prefixed loopback IPC framing. Transport + real `wsl.exe` stay runtime wiring.** |
| F11 | Port/network hooks | async loopback listeners, inbound/outbound monitor, webhook ingress — gated behind trust levels; **browser network containment** (doc 55/06 §6.15): WebRTC disable + worker fail-closed under allowlist, SSRF-defaults (loopback/RFC1918 blocked), `file://` blocked | Rust everyaios-core | 🔵 | doc 03 §5, 55 | **✅ webhook ingress half landed (2026-08-18): coordinator `Bun.serve` loopback listener (127.0.0.1, POST → `scheduler/fire_webhook` path+required-keys validation + optional token) — the browser network-containment half is the E-rows/browser crate scope** |
| F12 | Harness-driving | drive the user's existing agent CLIs (Codex/**Claude Code/Claude Agent via official ACP wrapper**/Cursor/Grok/OpenCode/**Aider**/Cline/Pi/**Copilot CLI** — doc 56/57) side-by-side on the same workspace — own context each, shared files + session state, Trust-Ladder-gated + audited (reverse of F8; OpenWebUI Computer pattern). **External interface = ACP (Agent Client Protocol)**: our app = Client, stdio JSON-RPC, permission requests → Guard-2 cards, tool calls/file ops → audit NDJSON, cancel → watchdog kills (doc 45). **Two-channel capability injection — Channel A (doc 68 §4):** ACP mediates I/O — `fs/read` → slim/bounded previews + pass-by-reference (C10), `terminal/output` → RTK compression, `terminal/create` → Guard-1 + audit, `fs/write` → Guard-2 ticket + diff card — token-minimizing + surgical + guards live at the protocol boundary for any hosted agent. Compose as brain → core → surgeon (doc 52 §1); **ACP adapter reference: cowork-forge `acp/client.rs` + `agents/external_coding_agent.rs` (doc 56 C2)**; **discovery via official ACP agent registry (doc 57 §2)**; **v3.40:** Cursor is an IDE (ACP adapter only); Gemini CLI consumer SKU sunset 2026-06-18 — registry-fed, do not hardcode Antigravity/Kilo/Trae; **auth-mode badge** (subscription-backed / API-key-backed / local) on every harness (doc 57 §3 — Claude OAuth works only inside the official wrapper/CLI); **v3.45 Dynamic Chief (re-anchor):** the hierarchy's brain tier is a swappable slot — `primary_chief` = inbuilt or an ACP-installed agent (official `claude-agent-acp` / `codex-acp`); when external occupies the Chief slot, EveryAIOS is the governed workspace shell (memory passport, Guard-2, audit, tool catalog); Chief's subagents may be other agents (codex-acp subagent-as-tool-call). **v3.46 evidence (codeg/vscode-acp/registry live-verified):** ACP clients in the wild advertise fs/terminal and service them through their own path policy (vscode-acp FileSystemHandler, codeg file_system_runtime); withholding them = agent self-containment, not Channel-B forcing; official registry ships a `quarantine.json` (8 agents) — F8 should consume it | sidecar + Rust everyaios-core | 🟡 **NEW** | doc 35 §C, 45, 52, 56, 57 + 65 §11 + 68 §4 + 2026-08-23 live-verified repos |
| F13 | Messaging bridges | **Desktop-first** — WhatsApp/Telegram adapters to the same agent engine, in-app cards (no headless 24×7 daemon); scheduled reminders + memory reuse; **Signal/iMessage + always-on daemon deferred to post-v1** (Secure OpenClaw pattern; DeerFlow 2.0 channels = 10-IM-adapter reference impl w/ run_policy + dedupe, doc 39 §B1) | sidecar | 🟡 **NEW** | doc 36 §B | **✅ normalized adapter + dedupe/memory + one-shot reminder queue; Telegram/WhatsApp/email adapter contracts landed (Telegram = ureq HTTP core, loopback-tested) — real network delivery (bot tokens / WhatsApp Cloud API) remains open.** |
| F14 | Email connector | Gmail API via Auth Bridge OAuth (vault tokens) or IMAP/SMTP (imapflow / async-imap + lettre); read/search/send/reply/triage; browser-session last resort | sidecar + vault | 🟢 | doc 50 (openonion/email-agent) | **✅ guard-ticketed `Mailbox`/`EmailService` tool core + `connectors::gmail` (`GmailConnector<T,R>`: list_labels, search, get_message, send_message with 401→refresh→retry, modify_labels, trash, unread_counts — 9 tests) + `connectors::imap_smtp` (`ImapSmtpConnector<T>`: search, read, send, reply, archive, trash — 7 tests). All over injectable seams; live account is the runtime seam.** |
| F15 | Calendar connector | Google Calendar API + ICS; event CRUD, availability, nudge integration (B7) | sidecar + vault | 🟢 | doc 50 | **✅ `connectors::calendar` (`CalendarConnector<T>`: list_events, get_event, create_event, update_event, delete_event, free_busy, export_ics — 4 tests) + deterministic `suggest_nudge` for B7. All over injectable `HttpTransport` seam; live Google account is the runtime seam.** |

## G. Search & research

| # | Capability | Feature | Module | Status | Source |
|---|---|---|---|---|---|
| G1 | Free search surface (no API key) | the user-facing layer of G8 — searxng-first + public instances (live `searx.space/data/instances.json` feed, health-gated) + circuit breaker + SQLite 5-min TTL result cache; chat-level `search.query` tool | sidecar + everyaios-search | 🟡 | doc 52 §4, doc 63 | **not a second cascade — engine tiers = G8** |
| G2 | Deep research | breadth×depth tree, learnings-up, gap-check, cited reports | Rust everyaios-search | 🔵 | 07 | **✅ P8.4: `DeepResearch` (build tree, `all_learnings`, `gap_check`) + `CitedReport` (assemble + render_markdown, confidence metrics).** |
| G3 | Multi-channel search | arXiv/GitHub/EDGAR/Reddit adapters | Rust everyaios-search | 🔵 | 07 | **✅ P8.4: `Channel` enum (query builders + payload normalizers, pure).** |
| G4 | Data-analysis REPL | sandboxed pandas/numpy for CSV/Excel/SQLite | sidecar + sandbox | 🟡 | 07 | **P8.4: runtime seam — the rquickjs sandbox is the execution surface; a Python/numpy REPL needs an install-gated sandbox.** |
| G5 | Repo-wide engineering | scan/dep-map/test-loop/patch in workspace | Rust everyaios-search | 🔵 | v2.0 §P7 | **✅ P8.4: `RepoWideScan` (`dependency_map` + transitive `blast_radius`).** |
| G6 | Site/domain search | SeekStorm-class inverted index for local corpora | Rust everyaios-search | 🔵 | 32/21 | **✅ P8.4: `SiteIndex` TF-IDF inverted index.** |
| G7 | Instant filename/content search | SQLite FTS5 filename index + debounced notify watcher + optional OS-native hooks (Everything/MFT, mdfind, Baloo); Everything/UltraSearch UX, cross-platform; **v3.40:** FTS5 trigram + Windows USN journal incremental | Rust everyaios-storage | 🟡 (P4.8 ✅) | doc 49 | **✅ P4.8: `search::SearchIndex` (FTS5 prefix query) + `Debouncer`/`watch` (notify thread); OS-native hooks stay optional accelerators. 2026-08-24: USN raw WinAPI reader landed (`everyaios-storage::usn_reader` pure `USN_RECORD_V2` parse + `usn_winapi` CreateFileW→FSCTL_QUERY/READ/ENUM_USN_*, FRN→path cache; compile-verified x86_64-pc-windows-msvc; live kernel read = Windows-CI work).** |
| G8 | Tiered search cascade & cache | cached instant tier (SQLite, 5-min TTL) → optional Rust metasearch (WebSurfx, ~20–40MB) → SearXNG (instances from the live `searx.space/data/instances.json` feed) → external fallback via circuit breaker; parallel fetch cascade (50-page baseline ≈ single-page time); BM25 rerank per tier; Algorithm #33 | sidecar core-search | 🟡 | doc 52 §4 + 65 §2 (Scrapling adaptive selector resolver) | **✅ LIVE on the ticket path (2026-08-25):** `search.query` dispatches `everyaios-search::G8Cascade` (TTL cache → health-gated SearXNG with failure-threshold + cooldown → DDG circuit-breaker fallback) through the Stage-0 executor (`tools.rs` `g8_search_dispatch_returns_hits`); WebSurfx Rust fast tier + live `searx.space`/SQLite-cache runtime wiring remain transport seams (P8.4) — **G1 is its no-key user surface** |
| G9 | **Read-cleaner / content filter (doc 64 §4)** | strip ads/trackers/consent-walls before `read`/`snapshot`/markdown-export + domain/network blocklists for the F11 containment layer — **`adblock` crate (brave/adblock-rust v0.13.0, MIT) as direct dependency**: `FilterSet` (bundled + user lists, `add_filter_list_with_permissions`), `Engine::matches` (url/hostname/initiator/request_type/third-party → BlockerResult), `hidden_class_id_selectors` + `url_cosmetic_resources` (DOM cleanup), `get_csp_directives` (response hardening), `serialize`/`deserialize` (compiled-engine cache); **FiltersProvider composition** (component/custom/subscription providers → merged set, rebuild-on-change — brave `ad_block_*_filters_provider.*`) | `everyaios-browser::content` (FilterSet ABP subset landed) | 🟡 New | doc 64 §4 | **✅ DONE (P7.8) — `content.rs` `FilterSet`/`is_blocked`/`cosmetic_selectors`/`clean_markdown`; full brave `adblock` crate = documented swap-in; F11 containment feed = follow-on** |

## H. UI & product

| # | Capability | Feature | Module | Status | Source |
|---|---|---|---|---|---|
| H1 | Chat | streaming, token streamer, message branching, artifacts | UI | 🟢 (port) | v2.0 §P1 |
| H2 | Cockpit dashboard | live agent cards, Watch/Stop, recent sessions | UI | 🟡 | 33 §9.5 | **✅ P3.2 landed: `everyaios-audit/src/cockpit.rs` — `CockpitState`/`AgentCard`/`InterruptCard` (status/model/provider, token counters, capped action trail, `quiet_status()` single-sentence line, MCQ interrupt lifecycle, stop/undo; +8 tests) + Tauri `cockpit_cmds.rs` (`cockpit_snapshot`/`activity`/`tokens`/`upsert_agent` feed seams, `cockpit_quiet` → tray tooltip + window hide, `agent_undo` + `interrupt_respond` JSON-RPC over the unix control channel) + `ui/src/pages/Cockpit.tsx` flight deck (Running-now cards w/ LIVE chip + STOP/UNDO, slide-over panel w/ action cards + token totals, MCQ interrupt cards, quiet toggle, 2s poll). 393 ws tests (355 + 8 + 30 office), clippy 0, fmt clean.** |
| H3 | Audit + replay UI | searchable sessions, per-step screenshots, scrubber | UI | 🟡 | 33 §9.5 | **✅ P3.1 landed: `everyaios-audit` replay query layer (`search_sessions` substring filter, `timeline` = segment+events+screenshot steps, `screenshot_steps`/`screenshot_path`, `events_since` watch tail; +3 tests) + Tauri commands (`replay_sessions`/`replay_timeline`/`replay_screenshot` base64 data-URL/`watch_events`/`agent_stop` JSON-RPC over unix control channel) + `ui/src/pages/Audit.tsx` (scrubber bar, screenshot strip, searchable sessions, 2s watch poll, Stop button). 393 ws tests, clippy 0, fmt clean.** |
| H4 | Blueprint editor | live execution status on .md | UI | 🟡 | v2.0 §P2 | **✅ P8.5: `blueprint-view.tsx` (spec.md-style editor, task status + dep check + progress).** |
| H5 | Office editors | docx/xlsx/pptx/pdf views + chat overlay | UI | 🟡 | 04 | **✅ P4.7 landed: `office_cmds` (docx_open/pptx_open/pdf_open) + `ui/pages/{DocxViewer,PptxViewer,PdfViewer}.tsx` + `ChatOverlay` + formula bar/cell selection on the P4.2 sheets grid. Univer embed → deferred (OSS/Pro split + lossy-reserialize risk); pdf.js canvas renderer landed (`PdfViewer.tsx` + `pdf_bytes` + page nav/zoom/Canvas-Text toggle); notes panel + docx image render → follow-up.** |
| H6 | Reader | PDF/EPUB/web/markdown universal reader | UI | 🟢 (port) | v2.0 §P1 | **✅ P8.1: `everyaios-core::reader` — `extract_text` (markdown/plain/html zero-dep; PDF/EPUB explicit seams) + `ReaderIndex` chunked RAG with `chat_overlay` citations. UI surface stays open.** |
| H7 | Math + code rendering | KaTeX, syntax highlight + run/compile | UI | 🟢 (port) | v2.0 §P1 |
| H8 | Permission cards | Guard-2 diff cards, trust ladder UI | Rust+UI | 🔵+🟡 | 06 |
| H9 | Token/cost analytics | per-key/per-session dashboard; **v3.39:** feed `ProviderObservation` → A7 `RouteDecision`; **v3.55.** durable feed: vault `token_usage` → `usage/recent` → coordinator boot `hydrateObservations` (ARCH/05 seam, live keys never overwritten) | UI+Rust | 🔵+🟡 | 05 §5.6 |
| H10 | Personality | SOUL.md, user-tunable, core rules inviolable | sidecar+UI | 🟡 | v2.0 §P1 | **✅ P8.3: `everyaios-blueprint::persona` — `load_persona` SOUL.md parsing, `TonePreset` + `PersonaConfig::custom_override`, `CORE_RULES` always emitted first + fail-closed override-scan.** |
| H11 | Tray daemon | watchers + automations headless | Rust | 🔵 | v2.0 §P7 | **✅ headless-execution half landed (2026-08-18): the coordinator's due-ticker runs with the window hidden; tray gained "Run automations now" → `tick_scheduler` → `scheduler/execute` one-pass (watchers/office daemons remain the browser/storage crates' scope)** |
| H12 | Telemetry | opt-in, enumerated fields, no content | Rust | 🔵 | 33 §11 | **✅ P8.7: `everyaios-core::telemetry` — `Telemetry` single choke point, `TelemetryEventKind` enumerated buckets only, `TelemetrySample` has no content-bearing field, off-by-default + cold-boot no-request tests.** |
| H13 | Local OpenAI-compatible server UI (deferred with A8; no standalone server assumed in current runtime) | expose + manage | UI | 🟡 | A8 | **✅ P8.5: `local-server-view.tsx` (start/stop, port, endpoint URLs, VS Code/Cursor hint).** |
| H14 | Scheduled tasks UI | create from chat (nudge cards) + settings | UI | 🟡 | 33 §7, v2.0 | **✅ landed (2026-08-18): AutomationsPanel live-wired to `scheduler_list` (run-now/pause/resume/delete/enable + paused/running/retrying badges + last-run) + nudge cards in the chat EmptyState (session-open proactivity hook, doc 67 §3); NL create-from-chat wizard = the H22 automation-builder queue** |
| H15 | Voice input (VAD) | hands-free chat, speech-to-text; offline STT options (Vosk / sherpa-onnx / whisper.cpp) + optional wake word (openWakeWord) | UI | ⚪ | 33 §10, doc 50 |
| H16 | Magic-completion | inline context-aware completion (AnythingLLM Magic Tab, optional) | UI | ⚪ | 01 |
| H17 | Widget cards | inline render: weather, stock (yahoo-finance2), math/calc, lookups (Vane pattern) | UI | 🟡 **NEW** | doc 35 §B | **✅ P8.2: `everyaios-core::widgets` — `MathWidget` safe calculator, `LookupWidget`, `WeatherWidget`/`StockWidget` cards + TTL cache (live APIs as seams). Inline UI render stays open.** |
| H18 | Remote session handoff + mobile companion | LAN/Tailscale/tunnel view — resume a running desktop session from phone mid-run (opt-in; extends B2 resume + C8 sync). **(v3.64 §4.4):** H18 redefined as an additional **CLIENT** of the Work Gateway (`ClientSession`/`ClientCapabilities`: view/approve/review/steer, consuming the canonical `WorkEvent` stream — TODO P49.9/P49.19). **doc 68 §3:** Cowork/Work ship a phone *surface* (monitor/steer, not just handoff) — a mobile companion app is a distinct post-v1 item. Distinct from H33: H18 = client surface; H33 = execution node | Rust + sidecar + P49 WorkGateway | ⚪ | doc 35 §C + 68 §3 + v3.64 §4.4 |
| H19 | Progress steps panel (unified timeline, clickable, timestamps) | every row binds the B7 BackgroundTaskRecord — per-task queued/running/lost + cancel/retry/delivery state; waiting-on-approval steps stay visible | ui/ | 🟡 | v3.53 |ed timeline of all agent actions (shell+code+browser+office) with clickable entries, timestamps, expandable details | coordinator + UI | 🔵 New | doc 46 (Devin) |
| H20 | **Activity rail + multi-view viewport (work cockpit — v3.0)** | **48px right rail (Folder/Shell/Browse/Code) + ONE Office button → flyout (Sheets/Word/Slides/PDF) + session views (Progress/Diff/Audit/Storage) + `+` Add view; the right viewport keeps multiple open tabs, renders one active view at a time, supports close/reorder/persistence; views contract `ViewDefinition{id,icon,label,group:core|office|session|plugin,when,open}` (first-party + plugins register identically — I6 dogfood); per-session layout persistence (openViews/activeViewId/railCollapsed/splitRatio/browseMode/composerMode); chat + now-doing never unmount on collapse; **v3.53 Chat ⇄ Code mode switch (⌘⇧E)** — Code rail becomes the primary surface (tree + editor + terminal + diff + LSP diagnostics + plan docked), composer stays pinned, agent/chat one keystroke away; a layout, never a separate product** | UI + everyaios-browser + office-engine | 🔵 New | doc 67 §6 + ARCH/12 v3.0 + v3.53 (Cursor-class dual-mode) |
| H21 | Takeover/resume flow | Pause agent → user edits → resume with change description | UI + IPC + core | 🔵 New | doc 46 (Devin) |
| H22 | Automation builder (NL + templates) | Event-driven workflow creation with NL input and 10+ pre-built templates | UI + scheduler (B7) | 🔵 New | doc 46 (Devin) + 65 §14 (langflow visual component builder) | **PARTIAL — editor + list landed (P11.5.5); template gallery + NL creation NOT DONE (annotated 2026-08-18)** |
| H23 | Knowledge browser (trigger+macro) | Browse/edit knowledge items with trigger-based recall, macros, folders, repo-pinning | UI + memory (C6-C7) | 🔵 New | doc 46 (Devin) |
| H24 | MCP marketplace | Browse/install/manage MCP servers with status indicators; **v3.39:** UI over `MCPServerRecord` lifecycle (discover/validate/install/enable/health), not a second registry | UI + connector hub (F7-F8) | 🔵 New | doc 46 (Devin) |
| H25 | Generative UI (AG-UI) | live agent-emitted components in chat (AG-UI wire protocol, ~16 event types, single channel); sandboxed iframe + strict CSP + process isolation (Anthropic Artifacts pattern); artifact cards static → live on demand | UI | 🔵 **NEW** | doc 50 |
| H26 | Clipboard tool | read/write/history system clipboard (arboard); guard-ticketed (read = read-only, write = mutation) | Rust everyaios-core + UI | ⚪ | doc 50 |
| H27 | Resumable streams | **V1 (basic):** resume an in-flight response after transient disconnect/session restart where a checkpoint exists (Work-model resume; in-flight token-stream resume = LibreChat pattern, P11.5.12). **Post-v1 (advanced):** multi-device/distributed resumable streams (H33/H18 class) | coordinator + UI | ⚪ (basic = v1, advanced = post-v1) | doc 50 |
| H28 | Voice output (TTS) | offline sherpa-onnx default (Apache-2.0, active; hosts Piper VITS voices — ⚠️ rhasspy/piper archived) + optional BYOK cloud TTS (OpenAI/ElevenLabs) | UI + sidecar | ⚪ | doc 50 |
| H29 | **Local dashboard artifacts (the local-first "Sites")** | agent generates a mini web-app (dashboard/report/app) into a guarded workspace folder; `everyaios-script` sandbox serves it on `127.0.0.1:<port>`; previewed in the views rail w/ device frames (bolt.diy pattern — typed agent→runtime action stream `BoltAction` parse, `ActionRunner` state machine, live preview); Guard-2-ticketed serve/stop; artifact keeps updating as the agent iterates | everyaios-script + UI (views rail) | 🟡 New | doc 67 §1 (stackblitz-labs/bolt.diy) |

| H30 | **Voice memo → structured report** | speech-to-text (H15) → transcribe → agent synthesizes into a polished document (Word block-patch D1 / markdown / email F14); the end-to-end workflow row Cowork advertises ("reports from messy inputs: voice memos + scattered notes → polished documents") — I/O rides H15/H28 (STT/TTS, both deferred), this is the job that composes them | sidecar + office engine + UI | ⚪ | doc 68 §3 (Cowork help center) |
| H31 | **Corpus-first research surface + audio digest** | pick sources (files/folders/URLs/emails) → grounded, cited answers + mind-map/report artifacts (Gemini-Notebook-class); reuses C-series RAG + G2 deep research + EV1 citation fidelity; **audio-digest output** (podcast-style Audio Overview) rides H28 TTS — post-v1 | sidecar + memory + UI (views rail) | 🟡 New | doc 68 §2.2 (Gemini Notebook / NotebookLM) |
| H32 | **Agent picker + agent-native command surface** | pick an agent (F12/J17 ACP registry) → `initialize` capability card → composer renders the agent's live `available_commands` + `@` + mode indicator (one consistent UI, per-agent vocabulary); **agent-scoped model surface**: hosted agents expose their own models via `available_commands`/config — the full models.dev catalog (A6) lives only in the native-engine picker (intent-first Fast/Quality/Private/Cheap + power-user drawer), never a global model grid; **v3.41 chrome:** permission chip + Agent/Experts/Spec + grouped Settings (layout reference; auto-run not executor-honored) | UI (composer) + F12/J17 + A6/A7 | 🟡 New ✅ **agent picker landed — `ui/` Chat.tsx agent dropdown fed by `acp_agents`; inbuilt shows the model picker, ACP agents hide it + show auth badge; `chat_stream` threads `agentId`** |doc 68 §4 (ACP spec + registry) |
| H33 | **User-operated always-on executor node (post-v1)** | **(v3.64 §4.4):** H33 redefined as an **ExecutionNode** with `always_on=true` — the same `ExecutionNode` abstraction as laptop/home-server/VPS (TODO P49.3); **(v3.65 §4.4a(4):)** distinct from `ExternalRuntime` (external ACP agent = `RuntimeEndpoint` with `trust_class = protocol-boundary`; only mediated Channel-B calls are governed, Self-contained effects stay outside visibility — J17). Control plane stays the desktop (Guard-2/audit/memory/receipts); a headless runtime (same binary, `--headless`: core + B7 scheduler + browser/script/office engines, no UI) runs on user-operated hardware — mini-PC / old laptop / spare Raspberry Pi / home server / user-rented VPS (their own AWS-GCP-DO-Hetzner-Fly credentials, never a founder account) — executing scheduled/background work 24/7 while the control laptop is off; attaches via the landed P8.9 E2E sync transport (X25519 + ChaCha20-Poly1305, ledger reconcile + tombstones); Guard-2 never leaves the control surface (approval steps park as pending); no rented fleet, no founder compute (§8); deploy pattern researched at source from OpenClaw (`Dockerfile`/`docker-compose`/`fly.toml`, ref only) | Rust core (headless profile) + P8.9 sync + P49 ExecutionNode | ⚪ | OpenClaw deploy pattern (2026) + 2026-08-23 review + v3.64 §4.4 |
| H34 | **Autonomy Level — chatbar approval control (v3.56 — elevates the H32 chip to a first-class subsystem; UI half landed 2026-08-26: three-control composer + per-task `config_hash` freeze + Autonomy Limit escalation card + temporary elevation + live indicator + 7 store tests; Rust `permissions.toml` preset maps **landed 2026-08-30 (P44.5)**: `AutonomyPreset` → `PermissionsPolicy::preset` + `GuardService::set_autonomy_level`/`autonomy_level` + `guard/autonomy`/`guard/set_autonomy` Tauri + UI `guardAutonomy`/`guardSetAutonomy`/`syncAutonomyFromRust` — the composer indicator reads the live Rust preset)** | one composer control answers "how much may the agent do without asking?": 🛡 Sandbox (plan + read-only: reads/search/analysis/simulation only, every mutation denied) · 👀 Ask (default: safe reads auto, meaningful mutations + external side effects ask, destructive always ask) · ⚡ Auto (low-risk mutations auto: workspace edits, dir create/rename, local tests, format, generated artifacts; external sends/money/destructive/new domains/credential use/high-risk shell/scope expansion still ask — Trust-Ladder auto bands) · 🚀 Run Everything = Maximum autonomy within hard floors (never bypasses destructive, secret/credential access, financial, security changes, cross-workspace writes, irreversible external effects). **Policy presets on the existing permission engine, not a bypass around Guard:** level → `permissions.toml` preset → Guard-1 → Guard-2 (only when the level's band says ask) → ticket → executor → audit → evidence. **Per-session + per-task policy snapshot** (level frozen into the task's `config_hash`/runtime manifest at task start — same principle as scheduled runs). **Per-task escalation:** Autonomy Limit card (current mode · the action · why it exceeds · **Do Once / Allow For This Task / Change Level**). **Temporary elevation** ("Allow this task" = task-only AUTO, expires at task completion; chatbar stays at its level). **Live autonomy indicator during execution** (`⚡ Auto │ ✓ 24 reads · ✓ 6 edits · ⚠ 1 external — approval required`). Level + risk policy + workspace scope + agent capability scope compose into effective permission (J21 decision packages). "Customize" opens the advanced policy. Same answer as R1 (approval fatigue) with one visible knob | Rust everyaios-guard::permissions (preset maps) + UI composer + H32/J21 | 🟡 New | Hermes/OpenCode agent UX (source-read 2026-08-26) + doc 52 §2 + doc 84 |
| H35 | **CLI/doctor/admin surface** | **`everyaios doctor` = V1 support primitive (2026-08-26)** — per-subsystem readiness (Core · Vault · Browser · ⚠ Chrome/CDP pairing · MCP · credentials · local runtimes · DB · Disk), derived from the same signals as E9 H4. **Full admin CLI stays post-v1:** `providers` / `models` / `agents` / `mcp` / `skills` / `tasks` / `audit` / `sessions` / `repair` / `export` / `import` / `acp` — OpenCode CLI pattern; desktop cockpit stays primary; CLI serves automation/support/power users/P40 headless node. Rides Tauri command layer + P8.8 updater + `--headless`; no second runtime | src-tauri cmds + P40 | ⚡ (doctor v1) + ⚪ (full CLI post-v1) | OpenCode CLI (source-read 2026-08-26) + v3.57 review | **v3.57 added; 2026-08-26: doctor promoted to v1 (queued), full CLI stays post-v1** |

## I. Forge & skills

| # | Capability | Feature | Module | Status | Source |
|---|---|---|---|---|---|
| I1 | Code synthesis loop | write→sandbox→test→iterate | sidecar + sandbox | 🟡 | v2.0 §P6 | **✅ P7.1: `everyaios-core::forge::ForgeRuntime` — `iterate` (write→test→rewrite, SHA-256 loop guard + budget) + `tdd_loop` (RED→GREEN) + `run_js` (rquickjs `everyaios-script`) + `DockerSandbox` (mounts/network/limits → `docker run`); live execution stays executor-bound.** |
| I2 | Skill registry | ~/.everyaios/skills/, manifest + ownership markers, auto-inject into planner; **SKILL.md format alignment** (name/description/allowed-tools frontmatter + references/ — agent-browser `skill-data`, doc 55) so our skills work with the ecosystem | sidecar + Rust | 🟡 | v2.0 §P6, 33 §8, 55 + 65 §4 (SKILL.md anatomy) + 65 §6 (skills_index.json) | **✅ P7.2: `everyaios-blueprint::skill_store` — scanner, SKILL.md manifest + ownership, `SkillIndex` (scoring + MAX_ACTIVE_SKILLS=20 + planner-tier render), restart-survival, `taste_skill`, `grow_from_task`.** |
| I3 | WASM fuel-metered sandbox | compute budget + epoch kill | Rust (post-v1) | ⚪ | 09 |
| I4 | TDD loop | auto-generate tests, read stderr, rewrite | sidecar | 🟡 | v2.0 §P6 | **✅ P7.1: `ForgeRuntime::tdd_loop` — write test → prove RED → iterate implementation until GREEN over the `CommandRunner` seam.** |
| I5 | ECC guardrails | plan-before-build, session scanning | sidecar | 🟡 | 09 |
| I6 | **Extension/plugin ABI** | versioned bundles (`abi_version`, cumulative host adapters — Zed WIT `since_v0_0_x` pattern); typed manifest: `contributes` + `capabilities` allow-lists with `*`/`**` arg wildcards (Zed `CapabilityGranter`); fail-closed per-extension trust flags (Hermes `allowed_*`); explicit agent-binding (Cherry Studio); lazy activation (VS Code); host facades `ctx.llm`/`ctx.files`/`ctx.approval()`; dogfood first-party plugins; **v3.40 executor hooks** (`PreToolUse` deny-only, never skip ticket; PostTool*/batch; turn/session) — distinct from J18 profiles | sidecar + Rust everyaios-guard | 🟡 | 44 §5 | **✅ P7.3: `everyaios-guard::granter` (`CapabilityGranter` — manifest allow-list ∧ host grant, `wildcard_match` `*`/`**`, fail-closed `TrustFlags` + sandbox/approval required for dangerous powers, agent-binding, deny-narrows-allow refinement) + `everyaios-blueprint::plugin` (`PluginManifest` manifest.toml schema + validation, `PluginRegistry` scan + lazy activation, `HostFacades` ctx.llm/files/approval, `first_party_catalog` + `dogfood_rule`).** |
| I7 | RepoMap (tree-sitter + PageRank) + **Warp semantic index (doc 56)** | Codebase context selection via tag extraction, graph building, personalized PageRank, binary-search budget fitting (**deterministic default — zero embeddings**); **optional semantic layer (Warp `full_source_code_embedding`: tree-sitter semantic chunker MAX_TRAVERSAL_DEPTH=200 + coalesce_fragments, merkle-tree content-hash incremental sync, search shaping w/ char-boundary-safe reads, `file_outline` — doc 56 W1/W2) gated behind C5** — one crate, two query paths (deterministic context selection vs semantic search/outline), not two indexes | everyaios-repomap (new crate) | 🔵 New | doc 46 (Aider), 56 + 65 §3 (xerj autoindex) + 65 §7 (code-review-graph persistent graph + git-diff rebuild) |
| I8 | Edit strategy pattern (per-model) | Multiple edit formats (SEARCH/REPLACE, udiff, whole, patch) with fuzzy matching, selected per model; **v3.40:** one verified apply = one git commit (K2 unit) | coordinator | 🔵 New | doc 46 (Aider) |
| I9 | Architect mode (two-pass) | Reasoning model → Editor model split for code changes (aider-reported 82.7% benchmark — doc 51); **composes with F12 surgical hierarchy — the surgeon tier may run the two-pass architect/editor; distinct from the oracle/review pass (TODO P11.5.10: heavyweight post-edit review) — planning then editing ≠ reviewing after** | coordinator + sub-agents (B3) | 🔵 New | doc 46 (Aider), 51, 52 |
| I10 | File watcher + AI comments | Watch source files for `// ai!` markers, extract context, auto-submit to agent | everyaios-core (notify) | 🔵 New | doc 46 (Aider) |
| I11 | LSP code-intel | One LSP client (neovim runtime/lua/vim/lsp/* reference): hover/docs, go-to-def, references, rename-with-preview, diagnostics, code actions, inlay hints, watchfiles; guard-ticketed (read = read-only, rename/apply = mutation) | everyaios-codeintel (new) | 🔵 New | doc 63 §2.1 (neovim) + doc 56 LSP note + 65 §8 (serena symbol-editing: safe-delete / replace-body / LSP catalog) | **✅ landed: `everyaios-codeintel` — `lsp` framing/types + `session` (transport trait, stdio spawn + keep-alive, initialize lifecycle) + `semantic` (symbol_where/callers/unused_exports) + `scip` (protobuf wire reader) + `repomap` (PageRank + budget fit, TagSource plug-in) + **P7.1 live diagnostics: `lsp_runner::LspRunner` spawns a configured server (`lsp-config.json`) and collects `publishDiagnostics` (process E2E via `mock-lsp-server`);guard ticket call-site = harness wiring** |
| I12 | **Zed-class Rust IDE capability (post-v1 — one capability behind the Code rail, not the main product)** | base: tree-sitter parsing/highlight + folding, multi-buffer + splits + search, terminal + diff, git staging/blame, tasks runner — over existing stacks (LSP = I11, indexing = I7 + watchfiles); **distinctives:** (1) **native-core reserves** — `gpui` **Apache-2.0** (verified from Zed's own open-sourcing announcement: "use it to build high-performance desktop applications and distribute them under any license you choose") + `floem-editor-core` (Lapce's editor core, Apache-2.0) are the documented v2 native-core candidates (own core on either, direct dep); the Zed *editor application* layer is **GPL-3.0 pattern-only**; (2) worktree-first parallelism (Codex-app: per-sub-agent worktrees, merge through the plan, K2-reversible); (3) everything ticketed (I8 → Guard-2, no silent autosaves); (4) receipts-in-editor (K1); (5) any-brain (no model; H16/agent-panel/F12 compose) | ui/ + codeintel + core | ⚪ | Zed announcement (gpui Apache-2.0, editor GPL-3) + Codex-app worktrees (2026-08-23) | **✅ P41.1 landed (2026-08-23):** **Monaco embed** (MIT — VS Code's own editor component; CodeMirror-6 lock superseded) + VS Code-style workbench `ide-workbench.tsx` (activity bar · Explorer over real `fs_cmds` · SCM over real `git_cmds` · Problems over real I11 `LspRunner` diagnostics via `lsp_cmds` · editor tabs · bottom panel · status bar) + offline `?worker` bundling (5 workers, own cacheable chunk) — P41.2 worktrees / P41.3 ticketed writes / P41.4 receipts all landed 2026-08-24 (`fleet.ts` worktree planning + `worktree_cap.rs` disk cap; `fs.ts` P41.3 ticket split pair; receipts/verify inline in the IDE Diff rail)
| I13 | **Knowledge → Skill Compiler (`/learn`, post-v1)** | evidence (URL · PDF · repo · conversation · folder) → versioned testable skill: ingestion → evidence extraction → skill blueprint (SKILL.md + references) → sandbox → test → provenance → versioned skill. Rides landed `skill_store::scan` + `grow_from_task` + Forge `iterate`/`tdd_loop` + I6 ABI (capability allow-list + agent binding) — the missing piece is the first-class user command composing them (Hermes `/learn` pattern). Never runs unsandboxed; output = a normal versioned skill, not a plugin with new powers. The existing registry + Forge are the v1 surface; the /learn command is post-v1 | everyaios-blueprint skill_store + forge | ⚪ Post-v1 | Hermes /learn (source-read 2026-08-26) + v3.57 review | **v3.57 added — post-v1** |
## J. Cross-cutting

| # | Capability | Feature | Module | Status | Source |
|---|---|---|---|---|---|
| J1 | Trust Ladder | 0–100 graduated permissions, 15 tests built | sidecar core-tools | 🟢 | 06 |
| J2 | Guard-1 regex interceptors | compiled blocklist, pre-exec scan | Rust everyaios-guard | 🔵 | 06, 03 §8 | **✅ P7.4 landed: `everyaios-guard` — `blocklist` (40+ patterns / 8 categories) + `prescan` (`scan_shell`/`scan_path`/`scan_url`/`scan_all`) + `urlfloor` (file:// inside granted roots, scheme guard) + `redteam` gate (35/35 blocked)** |
| J3 | Guard-2 diff cards | human-gated approval card with exact decision details, card-bound nonce, web-action confirmation, and audit receipt; **consent lives in a dedicated approval window (F1 landed), so the main renderer can never approve; the native OS trust boundary remains open** | Rust everyaios-guard | 🔵 | 06 | **✅ P7.5 + P10.2 nonce hardening + F1 landed:** `DecisionPackage` → `PendingGuardCard` over Tauri IPC; `guard_respond` requires the rendered card nonce **and the `guard` window label** — the main webview (browser views / generative UI / plugin content) is refused outright, so a compromised main renderer can no longer draw a fake card over a real ticket. The consent surface is now the dedicated always-on-top `guard` window (`src-tauri/src/guard_window.rs` + `ui/guard.html`, a minimal dependency-free page that only ever renders tickets from Rust IPC; `guard_open_window` brings it up from cards/panels/Excel). `everyaios-guard::diffcard::NativeCard` is a Rust-owned serializable card shape, not an OS-native dialog; the true OS-native card remains a hardening item.** |
| J4 | Path/scope hard-floors | canonicalization, symlink-safe boundaries | Rust everyaios-guard | 🔵 | 06 | **✅ P7.7 landed: `pathfloor` — `normalize_lexical` + `canonicalize_no_follow`, `enforce_floor` (Allowed/ParentEscape/SymlinkEscape/OutsideRoot), adversarial fuzz gate (Allowed ⟹ inside) + P7.8 6-axis `PathGrant`/`GrantAxis` (read_only_recursive/read_write_create/… feeding permissions.toml, 0-escape invariant)** |
| J5 | Audit trail | append-only, token estimates, receipts, replay; durable event log + idempotency classes (doc 53 §4: safe-retry/unsafe/same-key/confirm-after-uncertain); **+ inspect-by-source Trajectory view (doc 61 — `ContextInjection` event type + `context_injections`/`list_session_ids`, Tauri `trajectory_sessions`/`snapshot`, `ui/` Trajectory.tsx)** | Rust everyaios-audit | 🔵 | 33 §9, 06, 53, 61 | **✅ P47.5 landed (v3.62):** `WorkReceipt` now carries a nested `effects: Vec<EffectReceipt>` (K1 per-effect proof — requested vs authorized op, resource, before/after refs, diff, `rollback_ref`, `has_gap` + uncertainty), part of the receipt self-hash + markdown render; exported from `everyaios-audit` (7 receipt tests).
| J6 | Prompt-injection defense | <user_document> wrapping, context scan, tool-result sanitization | sidecar+Rust | 🟡 | 25, 16 | **✅ P7.6 landed (Rust half): `everyaios-guard::injection` — `scan_context`, `wrap_user_document`, `sanitize_tool_result`, `Estop`; sidecar ingest call-site remains P7 wiring** |
| J7 | ProcessSupervisor | spawn/restart/backoff/circuit-breaker; **v3.39:** a `ManagedResource` controller for sidecar/browser/MCP/ACP/model-runner children | Rust everyaios-core | 🔵 | v2.0 §4.3 |
| J8 | Key vault | SQLCipher, CES executor, crash scrubbing | Rust everyaios-vault | 🔵 | 06 §6.8 |
| J9 | Config-as-files | everyaios.toml + agents/*.md + providers.toml | all | 🟡 | v2.0 §7.6 |
| J10 | Watchdog | connect/idle timeouts re-armed per byte | Rust+sidecar | 🔵+🟢 | 28 §3 |
| J11 | Hard $ budget per session | default $2.00/agent; core-providers live-pricing + sqlite counters; kill sidecar on exceed; "stopped: $X limit" UI; reasonix token discipline upstream brake | sidecar+Rust | 🟡 | 43 + 65 §1 (codeburn efficiency metrics) + 65 §9 (loop-engineering budget-guard) |
| J12 | Orphan-prevention on Rust death | Linux `prctl(PR_SET_PDEATHSIG, SIGTERM)` (code-verified); Windows Job Object `KILL_ON_JOB_CLOSE`; macOS posix_spawn process group; 5s parent-PID poll belt+suspenders | Rust everyaios-core | 🔵 | 43 |
| J13 | Sidecar heap safety | `--max-old-space-size=512`; self-restart at 80% heap; resume from last Hermes checkpoint (20snap/500MB); 30min rotation | Rust+sidecar | 🔵+🟢 | 43 |
| J14 | Distributed tracing | OpenTelemetry Rust↔Node shared trace_id; audit gains trace_id+span_id; agent-session observability refs: agentlens (local coding-agent traces), agentsight (eBPF) | Rust+sidecar | 🟡 | 43, 52 | **✅ P3.3 landed: `everyaios-core/src/tracing.rs` — `TraceContext` (root/child spans, opentelemetry 0.27 `TraceId`/`SpanId`/`TraceFlags`), W3C `traceparent` wire format (the header `@opentelemetry/sdk-node` reads) + `inject_headers`/`extract_headers` for the Rust→sidecar→provider→sandbox boundaries, `SpanRecord`/`SpanAttrs` (all doc-43 fields), `TraceReporter` (console + `<data_dir>/traces/traces.ndjson` NDJSON; not-sampled dropped; OTLP/Jaeger post-v1) + `AuditEvent`/`SessionEvent` `trace_id`+`span_id` columns (`write_traced`, serde-default legacy compat). 8 tests — 393 ws tests, clippy 0, fmt clean.** |
| J15 | Length-prefixed IPC framing | `[u32 LE][payload]`; bounded channels (cap 16) + backpressure; truncation → `ref:` handle | Rust everyaios-ipc | 🔵 | 43 |
| J16 | Process lifecycle hardening | UNIX socket over TCP (zero port collision); pre-spawn coordinator at boot (~60MB Bun binary — realistic; 25MB is hello-world-only, doc 43 §1.3); warm-pool 5min idle | Rust everyaios-core | 🔵 | 43 |
| J17 | **ACP harness bridge** | ACP client over stdio JSON-RPC (official Rust crate or `@agentclientprotocol/sdk`) for F12; `initialize` handshake (protocolVersion + optional-by-default capabilities); `session/request_permission` → Trust Ladder + Guard-2; `session/update` → audit NDJSON; `session/cancel` → watchdog/budget kills; v2-draft monitored; **generalized-client reference: Hermes issue #5257** (`copilot_acp_client.py` → generic `ACPClient` + `acp_agent_registry.py`); **A2A = secondary interface (doc 61):** ACP local CLIs, A2A v1.0 + Signed Agent Cards for remote discovery/identity | Rust + sidecar | 🟡+🔵 ✅ **module-level landed — `everyaios-acp` (`frame`/`messages`/`client`/`registry`): `initialize`+capability negotiation, `session/new`, `session/prompt` w/ Guard-2 permission seam, `session/cancel`, `LaunchRegistry` (46-agent catalog seeded from the official ACP `registry.json` + ollama launch, default inbuilt)**; **v3.45 `ChiefAdapter` + `GovernedSession` (mechanism corrected v3.46):** generic adapter trait (`initialize`/`start_session`/`send_message`/`stream_events`/`cancel`/`request_permission`/`update`, inbuilt + ACP impls); **corrected: omitting `fs`/`terminal` does NOT force MCP Channel B** (codeg `host_tools_policy` + ACP v2 RFD) — agents fall back to their own backends; governed paths = **Mediated** (advertise+service through Guard) or **Channel B**; v2 removes the fs/terminal surface; badge = Governed-Mediated | Self-contained | NotGoverned; no boundary → no governance claim | 45, 57, 61, 69 + 2026-08-23 ACP-spec + codeg/vscode-acp/registry live verification |
| J18 | Profile-gated **security profiles** | minimal/standard/strict enforcement (Allow/Ask/Block thresholds). **v3.40:** not Claude-style executor hooks — those are I6 (`PreToolUse` deny-only, never skip ticket) | everyaios-guard | 🔵 New | doc 46 (ECC) | **✅ P7.7 landed: `everyaios-guard::profiles` — `Profile` (minimal/standard/strict, approval thresholds Strict<Standard<Minimal) + `gate(profile, hook)` over 7 profile gates (Allow/Ask/Block)** |
| J19 | Merkle hash-chain audit | Cryptographic tamper-evident append-only log with hash chain verification | everyaios-audit | 🔵 New | doc 46 (OpenFang) | **✅ P7.7 landed: `everyaios-audit::merkle` — SHA-256 chained rows, `verify` → first-bad-row, `verify_against_head` catches truncation** |
| J20 | AgentShield config scanning | Scan everyaios.toml, blueprints, MCP configs, hooks for injection/secrets/permissions | everyaios-guard | 🔵 New | doc 46 (ECC) | **✅ P7.7 landed: `everyaios-guard::configscan` — guard-disable keys, destructive config, callbacks, injection markers, malformed-TOML finding** |
| J21 | Escalation rules & decision packages | permissions.toml policy layer (delete=always_ask, multi_file_edit=ask_if_gt_5, external_network=ask_if_new_domain, terminal_shell=ask_if_destructive), min_confidence_for_auto threshold, structured decision package (goal + diff + risk + paths) → Guard-2 cards; approvals/denials feed correction-detector + taste profile; ticket contract (doc 53 §3): ticket_id/agent_id/session_id/tool_id/operation/args-hash/paths/expiry/single-use/approval-source/risk/audit-seq | everyaios-guard + sidecar | 🟡 | doc 52 §2, 53 | **✅ landed: `everyaios-guard::permissions::PermissionsPolicy` (`parse` TOML → `Rule::{AlwaysAsk,AlwaysAllow,Block,AskIfGt,AskIfNewDomain,AskIfDestructive}` + `evaluate(Operation)` + `min_confidence_for_auto`/`user_feedback_learning`) + `everyaios-guard::decision::DecisionPackage` + `everyaios-core::guard_service::GuardService` (estop→policy→profile→ticket; `evaluate` + `use_ticket` executor call-site; `guard/*` JSON-RPC into `ChatRelay::spawn`) + coordinator `guard.ts` + Cockpit diff card/estop strip/receipts; the live tool-executor invocation is now wired (Stage 0 landed 2026-08-20 — `tool/exec` → evaluate → ticket → `tool/commit` → Merkle audit)** |
| J24 | **Credential-provider abstraction (post-v1 — user-controlled credential fill without secret exposure)** | `CredentialBroker` boundary: agent requests *opaque fill* into an approved target (password manager / OS Keychain / browser credential store) without ever receiving the password/OTP/raw credential — `fill(credential_handle, target)` → Guard-2-approved → broker writes into the target field. Extends the E9 hard-deny (password-manager surfaces) + Session Vault (cookies/localStorage/IndexedDB, agent never sees raw) from *storage* to *input* (fill). Agent never requests a secret by name; it requests a fill into an explicitly approved target. Substrate: J8 vault/broker | everyaios-vault broker + guard | ⚪ Post-v1 | v3.57 review (password-manager boundary) | **v3.57 added — post-v1** |

**Totals:** 156 feature rows · status buckets: 🟢 25 · 🔵 65 · 🟡 66 · ⚪ 16 (multi-status rows like `🟢+🔵` are counted in every bucket they carry; **recounted 2026-08-26 v3.57 — 25/67/66/16 (re-verified 2026-08-26 audit: actual status column = 🟢 25 · 🔵 65 · 🟡 66 · ⚪ 16 — the declared 🔵 66/🟡 67 were off by one each; corrected here) (was declared 24/68/62/16): drift from row-level status upgrades 08-23→08-26 (P42/P43 crate landings, E15–E17, office viewers); ⚪ 13→16 from I13+H35+J24; previous declared 27/70/59/9 did not match this file's rows (2026-08-18)**; A8 🟡→⚪ + F5 🟢→⚪ per the Connector-platform decision; **2026-08-26 v3.56 re-count: 153 = 151 pre-existing + A11 + H34 — the old "149" was last counted before H29–H33/I12 landed**; 🟡 67→68 from A11/H34; spec §0 index 156 matches this file exactly; **v3.57 adds I13 + H35 + J24 (all ⚪ post-v1): 153→156, ⚪ 13→16**). **2026-08-18: B6's `chat/interrupt` emit moved ⚪ → 🟡 (Stage-0 plan-executor seam landed — see the B6 row).** Added in the docs 49–51 gap pass: A10, D9–D11, F14–F15, G7, H25–H28 (+ H15 extended); doc 52 adds D12, G8, J21 (+ F12/G1/E9/J14 extended); doc 63 adds C13, E15–E17, I11 (+ I7 SCIP note confirmed as third path); doc 64 adds G9 (read-cleaner / content filter — brave adblock crate); doc 65 adds **no new rows** (batch-3 agent-infra/scraping/search/UI — every steal extends an existing row: codeburn→A9/J11, Scrapling→G8/E14/F11, claude-mem→P5, serena→I11, code-review-graph→I7, loop-engineering→P6/J11/B6, awesome-claude-skills→I2, agentic-awesome-skills→F8; xerj/career-ops/lobehub/langflow/Pake/mirrord REF); doc 66 adds **no new rows** (anomalyco org — models.dev direct STEAL extends A6/A9/A7/A2/A3/J11: two-tier lab/provider catalog + cache pricing + 30-provider sync automation; opentui→P11.5 REF, openauth→A2/A4 REF, sst SKIP; opencode ⭐ refresh); doc 67 adds **H29** (local dashboard artifacts — bolt.diy) + **redefines H20** (9-tab strip → activity rail + views contract, ARCH/12 v2.0) + extends **B7** (heartbeat automations — Hatchet lease pattern, reawaken same conversation w/ context, resume from audit-event checkpoint) + **B2** (durable-execution-the-hard-way blueprint); doc 68 adds **H30** (voice memo → structured report, ⚪) + **H31** (corpus-first research surface + audio digest, 🟡) + **H32** (agent picker + agent-scoped model surface, 🟡) + extends **H18** (mobile-companion note), **F7/F12/J17** (two-channel capability injection), **A6/A7** (agent-scoped model picker). **Landing annotations on this matrix are live: E15–E17, C13, I11, J2, J4, J6, J18, J19, J20, J21 (ticket contract), F8 (install button + Guard-2 ticket + auth surface), J17 (authenticate/logout/auth_required) all carry ✅ module-level notes as of 2026-08-16.** The build plan (10) sequences these so every milestone ships working value.

> **Honesty corrections 2026-08-18 (third-party audit pass):** (1) **A8** was mislabeled 🟡 — it is P9.5 post-v1 with zero work → ⚪ (row + spec §0 + totals). (2) **F5** (Composio/Zapier/Nango) was 🟢 "(partial)" but the 2026-08-16 Connector-platform decision removed the aggregator tabs → ⚪ removed (rows in ARCH/09 + spec §0/§2/P6/§7 + ARCH/01/10/11/12/DIAGRAMS all updated to the MCP-first surface). (3) **G8** annotated NOT built (no WebSurfx tier, no coordinator `search.ts`); **G1** annotated exists-but-unwired (core-search consumed by nobody at runtime yet — both are the Stage-0 tool-runtime seam). (4) **H22** annotated PARTIAL (editor + list landed; template gallery + NL creation NOT DONE). (5) **E13** "LIVE-verified" corrected → unit-tested via mock CDP transport; a real Chrome debug-port attach is a manual live-verification step. (6) **Totals line corrected** to this file's actual status-field counts (🟢 24 · 🟡 68 · 🔵 62 · ⚪ 12) — the previous declared 27/70/59/9 never matched the rows. (7) **Retest-gate honesty:** the C-series 🔁 memory algorithms were reimplemented natively in Rust (`everyaios-memory`, 168 tests) which supersedes the TS-sidecar retest; the remaining TS algorithms (5/7/12–17) keep their 🔁 flag until the coordinator consumes them (see the spec Algorithm Index disposition). (8) **EV1** is a dev/test-time gate (`cargo test`), not a runtime surface — no coordinator/Tauri refs yet. (9) **CI** now runs the UI (typecheck + build) always, and the coordinator (bun test + typecheck) — 2026-08-29: the `@personal-ai/core-*` packages are vendored in-repo at `packages/core-*`, so the coordinator job runs unconditionally (the legacy `APP_CLONE_TOKEN`/`sarv-projects/APP` private-repo checkout is no longer required).
