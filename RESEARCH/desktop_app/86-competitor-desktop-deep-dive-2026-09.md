# 86 — Competitor desktop deep-dive (P12.1 live research, 2026-09-04)

> **Method:** six parallel web-research passes 2026-09-04 (WebFetch + WebSearch against live repos, docs sites, release notes; source files read raw where it mattered). **No installs** — there is no GUI/display in this environment, so every hands-on install/test leg stays open (see §13). VERIFIED = fetched this session (key URLs in §9). Prior docs re-verified, not re-assumed; corrections in §1.
> 🔗 **Repos:** `anomalyco/opencode` · `OpenCoworkAI/open-cowork` · `NousResearch/hermes-agent` (+`apps/desktop`) · `fathah/hermes-desktop` · `Mintplex-Labs/anything-llm` · `janhq/jan` · `CherryHQ/cherry-studio` · `chatboxai/chatbox` · `andrewyng/openworker` · `open-webui/open-webui` (+`desktop`, +`computer`) · `holaboss-ai/holaOS` · `openchamber/openchamber` · `openclaw/openclaw` (+`acpx`) · `charmbracelet/crush` · Anthropic Claude Cowork (product, no repo) · Zed (docs) · Cursor (docs+reviews).

## 1. Corrections to prior docs (apply on next touch)

| Prior claim | Live truth 2026-09-04 |
|---|---|
| Chatbox is Tauri (TODO:1272) | **Electron** 35.7.5 (`chatboxai/chatbox`; `Bin-Huang/chatbox` redirects to `chatboxai/chatbox`). The true Tauri twin is **Jan (Tauri 2.7)** |
| open-cowork = Claude Code wrapper (docs 40/75) | **Stale since v3.0.0.** Now SDK-first multi-model on `@mariozechner/pi-coding-agent` + `pi-ai`; Claude Code CLI survives only as a WSL/VM provisioning helper. Thesis-VALIDATION verdict stands, single-harness lock-in verdict sharpens |
| opencode "50-named provider directory" | **75+** via AI-SDK + Models.dev (docs header). `baseURL` override + blacklist/whitelist + custom `npm:@ai-sdk/openai-compatible` hold |
| opencode desktop = Tauri note (community fork README) | Upstream `packages/desktop` is **Electron 42** (`@opencode-ai/desktop` 1.18.27). The Tauri file is a community fork, not upstream |
| `electron/main.cjs` (Hermes brief) | Now `electron/main.ts` → bundled `dist/electron-main.mjs`. Desktop pkg 0.17.0 lags agent v0.21.0 — never quote them interchangeably |
| hermes-agent ~226K★ | **241K★ / 49.5K forks, v0.21.0 "Pantheon" (31 Aug)**. opencode **203.8K★, v1.18.27**. open-cowork **2,104★, v3.3.1**. holaOS **11.1K★** (+5K in a month). OpenWorker **17.3K★** (+4K). Cherry **51.4K★, v2.0.12 (today)**. Jan **44.3K★, Apache-2.0, v0.8.4 (23 Jul — no Aug release)**. AnythingLLM **65.6K★, v1.16.1 (27 Aug)**. Chatbox **41.6K★, v1.23.1 (02 Sep)**. Open WebUI **150.9K★, v0.11.3 (31 Aug)** |
| Jan license (mixed old notes) | **Apache-2.0** (Menlo Research). Cherry = **AGPL-3.0** (pattern-only, never code). Chatbox = **GPL-3.0** (pattern-only). Crush = **FSL-1.1-MIT** (source-available, NOT OSI). holaOS = **Modified Apache 2.0** (commercial gate on hosted/embedded). open-webui/computer = proprietary Open Use License |
| "Progress rail" (internal name) | No vendor uses the term — it stays an EveryAIOS name for the step feed. Cowork calls it step feed/progress indicators; OpenChamber an ops rail |

## 2. opencode + opencode desktop (`anomalyco/opencode`, MIT, 1.18.27)

**Engine delta since doc 38:** 25+ packages now; compaction constants re-verified exact (`PRUNE_MINIMUM 20K`, `PRUNE_PROTECT 40K`, `TOOL_OUTPUT_MAX 2K`, tail-turns + `preserve_recent_tokens` ≈ 25% usable, `splitTurn`, overflow replay, synthetic continue); subagents gain `task_id` resume + `subagent_depth` (default 1) + `deriveSubagentSessionPermission` + forced denies + experimental **background subagents**; new `scout` agent joins general/explore; **policies layer** (`experimental.policies`, provider.use allowlist) replaces enabled/disabled providers; **managed/enterprise config** (remote `.well-known/opencode`, MDM); permissions now `allow|ask|deny` with `always`-pattern learning (`bash git status*`), `external_directory` scoping, `doom_loop` 3× guard, `.env` deny; ACP 12-file TS split, `opencode acp` stdio; Zen massively expanded (~70 IDs, endpoints `/zen/v1/...`, free tier = training-data-bearing); "OpenCode Go" is a subscription plan, not Go.

**Desktop (`packages/desktop`, Electron 42):** main spawns local `opencode serve` sidecar on loopback with per-launch Basic token; renderer is the shared `packages/app` (desktop/web reuse); typed `window.api` preload only; state in `electron-store` + `drafts.sqlite`; packaging via electron-builder (dmg+zip notarized, nsis, AppImage/deb/rpm, `opencode://` deep links, electron-updater). UX: **session tabs + multi-window + multi-server (local/WSL/http)** with per-window restore; Plan/Build `Tab` cycling; `@`-autocomplete; ask→once/always/reject; `/connect` + `/models` + `small_model` title offload; first-launch onboarding with default-project creation; artifacts = markdown/shiki/diff/file-tree only; i18n-strict.

**Weaknesses we exploit:** no real browser/computer-use (only webfetch + OS-browser open); no persistent memory (summaries only); coding-only tools; Zen US-hosted + 30d retention + training-use free tier (weaker privacy story than local-first BYOK); share = public links; Electron weight + `Access-Control-Allow-Origin:*` + `openPath(app)` exec (larger surface than Tauri+Rust).

## 3. open-cowork (`OpenCoworkAI/open-cowork`, MIT, v3.3.1, Electron 41)

**Headline correction:** pi-agent (`@mariozechner/pi-coding-agent` + `pi-ai` registry), NOT a Claude Code wrapper. Single-harness loop, single-workspace-at-a-time, `sandboxEnabled:false` by default.

**Steal-grade internals (source-read):** three-tier sandbox composition — lexical `isPathWithinRoot` + realpath symlink double-check + `validateCommandSandbox` path extraction + `DANGEROUS_PATTERNS` + `execFileAsync` argv + session/distro allowlists + rm-rf root guard + safeEnv allowlist (but default is host exec; VM is opt-in WSL2/Lima — README headline overstates); **config-store** with rotation/backup scaffolding and env-clear-then-export BUT a **hardcoded stable key, keychain integration REMOVED in v3.3.0** (at-rest = obfuscation — we must bind OS keychain, never copy this); `findPreferredWindowsNpxPath` (system-over-bundled + trusted-list); MCP name-sanitize for OpenAI-compat + 5-min parallel discovery; SKILL.md loader (builtin→global→project priority, chokidar+polling hot-reload, symlink-escape guard, `validateSkillName`); stepwise **API diagnostics with `errorType→fix` codes** + Ollama loopback discovery; in-process daily/weekly timer scheduler (dies with app — back ours with OS scheduler); ````artifact` JSON contract + icon map (list affordance only, no canvas); sudo stdin-pipe pattern.

**Onboarding (best-in-class for API-key users):** 90-second BYOK path — provider table incl. CN presets (GLM/MiniMax/Kimi) + Ollama discovery + live diagnostics + config-set blank/clone + auto-close-after-save. Genuinely low friction; add a harness picker where they have a provider grid.

**Openings:** single-harness lock-in (no router, no ACP driving); no approval-policy engine (allow/deny/ask only); memory foundation-only; Linux build-from-source; god-files (`index.ts` 107KB, `gui-operate-server` 231KB).

## 4. Hermes agent + Hermes Desktop + Bot Mode + community fork

**Agent v0.21.0 (241K★, MIT):** ~85 merged PRs/day in the Aug window. New since doc 02/38: **cron remembers** (continuity, per-job notepad, monitor-mode, `bot-chat` delivery, `context_from` chains, preflight, model-drift guard, per-job model/effort pins, incidents ledger, `cron doctor`, runs ledger); **profiles hardened** (`~/.hermes/profiles/<name>`, OAuth-share stays root, `home_mode:profile` opt-in); **multi-connection registry** (Local/Remote-SSH/Hermes-Cloud, unique device names, HTTP+WS Test, token vault never in renderer, optional OS-keychain toggle default-OFF); **delegation defaults moved** (250 iterations / 10 concurrent + steer/stop + JSON-schema + per-delegation cost); +6 providers; protected-instruction-files + redaction sweep + Windows destructive-approval + macOS TCC identity; `hermes acp` stdio.

**Hermes Desktop (`apps/desktop`, Electron 40, MIT):** three boundaries — Electron shell / React (`@assistant-ui/react`, xterm, tanstack-virtual, CodeMirror, shiki) / headless `hermes serve` over WS (`@hermes/shared` client, seq-stamped replay). Six-step backend resolution ladder with serve→dashboard fallback; `hermes://` deep-link MCP install; detached per-OS updater with **fleet fan-out** (backends first, app last, stale-GUI warn); 3-tier uninstall; `desktop.log` forensics. UX inventory: streaming chat + **side-by-side preview rail** + file browser + voice; **annotate-to-composer browser loop** (pins + selectors + redaction + DOM-grouped batching); composer model picker with **sticky-vs-default split + training-tier + aux-split warnings**; tabs/windows/panes + HUD + Quick Entry + terminal + Git review + worktrees + Artifacts gallery + Memory Star Map + Projects; customizable **status bar** (context meter + breakdown popover, cache%, tok/s); timeline rail + Find-in-page + queue editing; **error cards naming the failing layer** with Retry (deterministic-hide) / Switch-provider / Open-logs (local-vs-remote label swap) / Send-diagnostics (redacted, consent, 14-day expiry, dual-log bundle) / Copy-details; DESIGN.md token/primitive discipline.

**Bot Mode (bundled, default-on):** bot = profile (isolated config/memory/skills/creds/history) with forever **Bot Chat** (`/new`→`/compact` inside); roster + sections + avatars + **routines as namespaced cron** (`[bot:<name>]`) docked to owner; **group chats** (2–6 bots, ≤3 serial rounds, `@user` escalation); **bot-to-bot `@mention` + `message_agent` DMs** with typed `reason` codes; Desktop-relay vs `hermes peer` duality; per-invocation delivery (no live interrupt — copy the pragmatics).

**Community `fathah/hermes-desktop` (14.1K★, unaffiliated, MIT):** lowest-friction onboard, broadest provider presets, token/cost footer + `/usage`, 22 slash commands, 16 gateways — but a **parallel protocol** (own 8642/SSE vs canonical 9119/WS) = drift risk; unsigned installers; scope creep. UX reference only, never a dependency.

## 5. AnythingLLM / Jan / Cherry Studio / Chatbox

**AnythingLLM (65.6K★, MIT, v1.16.1):** desktop = Electron shell over bundled Express+LanceDB inside the monorepo (no standalone desktop repo — `Mintplex-Labs/anythingllm-desktop` 404s). Since Aug: **CoT/activity single-collapsible rollup** (best-in-class long-horizon readability) + **true abort end-to-end** + nav-away warning; **Foundry Local embedded + GenieX for Snapdragon** (capability auto-detect, GPU/NPU variants); `/img` image-gen; folder drag-drop with hierarchy; mid-session tool toggle; scheduled-job agent tool; MCP Tools-only + **lazy-start** (starts on Skills-page open or `@agent`, Refresh/Start/Stop without restart); workspace = RAG+agent boundary (heaviest mental model here, strongest isolation). No artifact pane (files/charts/images inline). Magic Echo/Beacon/Tab = OS-assistant direction, Pro-gated. *(Carryover flag: the 14-engine switch / DDG-default / `introspect()` / `reportSearchResultsCitations` specifics are the Aug source-read — NOT re-verified Sep 2026, the docs web-browsing page has thinned to one paragraph. Treat as carryover, not fresh.)*

**Jan (44.3K★, Apache-2.0, v0.8.4 — no Aug release):** the true Tauri twin (Tauri 2.7 + Rust llama.cpp sidecar + MLX + 55–150MB bundles). **A5 gold standard:** Hub fit pill (`Fits / May be slow / Won't fit`, no-download check) + Small/Balanced/Large quant tiers + Recommended default + top-bar % + cancel + pause/resume + bulk-delete-with-space-freed + per-model ctx/GPU-layers gear + VRAM guard + Increase-Ctx banner. **BYOK security pattern:** backend store + OS keyring + auto-migrate + downgrade snapshot + copy/reveal + multi-key rotation. Chat: queue-while-streaming + 2-stage stop + editable pending chips + auto-titles. MCP: lightweight router model + server-side orchestration toggle + auto-reconnect.

**Cherry Studio (51.4K★, AGPL-3.0 — pattern-only, v2.0.12 today, fastest shipper):** **BYOK depth benchmark** — provider drawer (key + multi-key rotation + one-click Fetch + health check + capability flags + per-model avatar + reasoning/effort + token presets + custom headers + endpoint override) + Spotlight fast-switch + quota-aware picker; Assistants/Topics grouped+pinned + multi-model simultaneous + numbered exportable citations + TTFB footer; **the only real artifact pane** (file→pane, dir→OS manager, sandboxed HTML, PDF selectable+outline); searchable settings sidebar; agent capsule + permission modes; IM channels + scheduler with run-status cards; Cherry Cloud entitlements (account friction — anti-pattern for us). 340–380MB bundles.

**Chatbox (41.6K★, GPL-3.0 — pattern-only, v1.23.1, Electron):** BYOK-simplicity praise explained — provider Spotlight + `models.dev`-enriched registry + one-click Ollama + localized errors (request ID + retry + translate-error). Chat polish to copy: collapsible branches (**minimap UNVERIFIED** — no minimap code found, doc 87 §1) + queued-while-generating + cached-reset confirm. Work Mode (workdir+permission carryover, Full-Access default) informs our approvals but stays weaker than our guard. Full backups (convos+images+attachments).

## 6. OpenWorker / Open WebUI / Claude Code + Codex / holaOS

**OpenWorker (17.3K★, MIT, Tauri+Python/aisuite, v0.2.1):** most aligned rival (local-first BYOK, Ollama, no lock-in). **Best-in-class permission/risk code to port:** hard floors first (own settings files, shell full-path match) + protected-in-project `human_only` + persistent-authority `human_only` + fail-closed unlocated writes + **MCP ≥EXTERNAL floor always** + allowlist splitter (opaque-construct/arg-executor/interpreter guards) + run-grant expiry + task exact-target standing rules + reviewer-model auto-approve with circuit breaker + unattended-inbox parking + provenance logging. v0.2.0 adds Skills, project memory boards, guided MCP setup, auto-compaction (transcript immutable). No Linux, Windows unsigned, no OS sandbox, Python server.

**Open WebUI (150.9K★, mixed license; desktop 2.7K★ AGPL early-alpha; computer 505★ proprietary):** superset workspace (chat+KB+agents+terminal+calendar+channels+admin) at extreme velocity — server-first, out of our scope to clone, rich to mine: **workspace-as-OpenAI-model gateway** (`cptr/<workspace>` + `X-OpenWebUI-*` sync headers) as the harness-driving pattern; incremental SHA-256 dir-sync; hybrid BM25+vector+rerank with per-attachment Full-Context toggle; `kb_exec` lesson (shell-chaining beats fan-out); Channels multi-agent critique; Notes full-fidelity attach; oikb 45-source sync. Computer's trust model is explicit SSH-equivalent (no sandbox — we must do better).

**Claude Code + Codex CLI:** loves — Shift+Tab mode cycling + status bar; plan→Yes+auto; granular never-vs-on-request; `Bash(npm test)`/`Agent(Explore)` allowlists; hooks-as-code (Pre/PostToolUse, PermissionRequest, tighten-only); cheap-worker subagents (Haiku/terra); one-click checkpoints/rewind; Codex OS sandbox (Seatbelt/bwrap) + permission profiles + fail-closed auto-reviewer. Hates — prompt fatigue vs yolo temptation; classifier opacity + cost; **checkpoint bash-blindness** (only edit-tool writes tracked — document against ours); headless rewind gap; quota visibility gap; network_proxy two-switch confusion. Steal the frontmatter (`tools/model/permissionMode/maxTurns/skills/mcpServers/effort/isolation:worktree`), Esc×2 rewind dialog (fork/code/both), approval source-labels.

**holaOS (11.1K★, Modified Apache 2.0 — commercial gate, Electron):** closest whole-product articulation AND the sharpest contrast. Validates everything (any-agent-any-model, shared file-based memory, real browser, real .xlsx/.pptx/.docx, side-by-side live apps, Combos, HolaHub recipes, trigger+schedule self-filing, IM-native ctx) — then couples it to founder servers: **hosted frontier default + holaProxy credit meter on ALL integration calls even BYOK + Stripe Connect commission + commercial-gated license (SaaS/embedding toll, logo lock, unilateral relicense)**. Steal the workspace UX (side-by-side drive-visible, IM per-scope approve-then-read, run→recipe bundles); never the toll model.

## 7. Cowork / OpenChamber / OpenClaw / Crush / Zed / Cursor

**Anthropic Claude Cowork (product, Jan 2026 → Pro/Team/Enterprise, Win Feb, cloud sessions mid-2026):** best non-coding agent UX reference. Steal: **3-mode composer approvals (Manual/Auto/Skip)** + per-connector Always/Needs/Blocked matrix + admin-hide + per-task override; **shared Chat/Cowork home + one composer with switch** + task step feed (files/tools/choices) + steer + anywhere check-in + finish/need-input notifications; folder-scoped FS; side-panel browser with cookie import; `/schedule` + Projects persistent workspaces; plugin = skills+connectors+subagents bundle + private marketplace + scanning. Cloud-first/account-bound = anti local-first; audit-log gap admitted.

**OpenChamber (`openchamber/openchamber`, ~9.5K★, MIT):** OpenCode-powered supervisor — closest cockpit shape after Hermes: Session Goals (finish-line + per-turn check + survive-close) + Scheduled-with-goals; **Multi-run (1 task × ≤5 models × worktrees) + Fusion** + Changes Walkthrough; ops rail (working/waiting/finished/failed + approvals + limits + **per-session tokens/cost**); Private Relay QR pairing + revocable tunnels; skills Catalog (git sources, global/project scope, collision triage). Coding-centric, inherits OpenCode limits.

**OpenClaw (`openclaw/openclaw` + `openclaw/acpx`, MIT):** cleanest host/guest split — native subagent vs ACP harness vs bridge-server vs CLI-fallback. Steal: **queued→running→terminal ledger + 7-day prune + lost-sweeper + `/tasks` board + mirrored flows + push delivery** (direct vs session-queued + heartbeat wake) + `sessions_spawn{runtime,agentId,thread,mode,cwd,resumeSessionId,model,thinking}` — **but add the cost carrier OpenClaw lacks** (ACP child rows carry NO usage/cost — known issue #130257); **acpx shape** (named sessions + queue + `--cwd` + json/quiet + flow run + 17-alias normalization + Codex CODEX_HOME isolation + doctor gate); bind-here vs thread-auto/here/off + persistent bindings; **ClawHub** (publish/version/vector-search/pin/frontmatter-security/default-off bridges).

**Crush (`charmbracelet/crush`, 27.9K★, FSL-1.1-MIT source-available):** original author's Go continuation, terminal-only. Steal: **LSP integration** (per-lang command+args+env, diagnostics→model, debug-lsp) + Catwalk provider registry + mid-session switch preserving context + `crush serve --cwd` workspaces. TUI craft praise; token-heavier reports; FSL friction.

**Zed (ACP-host reference):** most disciplined host/guest split (Zed never pays/bills external). Steal: **tool_permissions schema** (global→tool→regex allow/deny/confirm + hardcoded rm guards + chained parse + Test-Your-Rules checker + Allow-once/Deny/Always-for-tool/pattern); Review Changes multi-buffer + per-hunk + checkpoints + follow + background notify; `@` completer + model+provider picker + favorites + No-tools/MCP warnings + `/compact` + New-From-Summary; Registry install + agent_servers custom + boundary table + acp-logs.

**Cursor (closed, docs+reviews):** only GUI orchestration reference with parallel VMs. Steal adapted local-first: agent cards (status/steps/files/tests) + Awaiting-Input pause + conflict flag + handoff artifacts; Agents Window (parallel tabs, local+cloud sidebar); Rules matrix + Hooks lifecycle + team MCP scoping; Automations event matrix + APPROVAL_POLICY.md/ROUTING.md policy-file discovery. Ignore the clone-IDE/cloud-billing/GitHub-lock-in.

## 8. License discipline (binding on all steals)

| License | Repos | Rule |
|---|---|---|
| MIT / Apache-2.0 / ISC | opencode, open-cowork, hermes-agent+desktop, AnythingLLM, Jan, OpenWorker, OpenChamber, OpenClaw/acpx | Concepts + code-patterns OK with attribution |
| AGPL-3.0 / GPL-3.0 | Cherry Studio, Chatbox | **Patterns only, never code** |
| FSL-1.1-MIT / Modified Apache 2.0 / proprietary | Crush, holaOS, open-webui/computer | **Observe only** — behavior reference, no derivation |
| Mixed/brand-encumbered | Open WebUI core | Treat as observe-only until per-file check |

## 9. Gap matrix — EveryAIOS vs top 5 (2026-09-04)

Top 5 by whole-product overlap: Hermes Desktop · opencode desktop · Cherry Studio · holaOS · AnythingLLM.

| Capability | EveryAIOS (v1 scope) | Hermes Desktop | opencode desktop | Cherry Studio | holaOS | AnythingLLM |
|---|---|---|---|---|---|---|
| Local-first + BYOK, no founder server | ✅ core thesis | ⚠️ Portal/cloud tiers, funds relay | ⚠️ Zen hosted, US retention, training-use free tier | ⚠️ Cloud entitlements | ❌ hosted default + credit meter + commission | 🟡 cloud + Pro gating |
| Multi-harness host (ACP drive + router) | ✅ F12/J17 | ❌ one agent | ❌ own engine only | ⚠️ Pi/DSH fetch only | ✅ Claude/Codex/built-in side-by-side | ❌ own agent |
| Approval/guard depth | ✅ Guard-2 + floors (J) | ⚠️ Safety pages, no floors | ✅ permission patterns + always-learn | ⚠️ permission modes | ⚠️ approvals underspecified | ❌ minimal |
| Secrets (OS-bound vault) | ✅ vault-first | ⚠️ 0600 files, keychain opt-in OFF | ⚠️ auth.json file | ❓ undisclosed | ❓ undisclosed | ❌ server env/DB |
| Persistent memory | ✅ P5 algos | ✅ continuity/notepad/graph | ❌ summaries only | ⚠️ Global Memory scope | ✅ shared file memory | ⚠️ memory.js only |
| Real browser + computer-use | ✅ E-series | ✅ annotate loop + agent-driven tab | ❌ webfetch only | ❌ none | ✅ signed-in browser | ❌ none |
| Office deliverables | ✅ block-patch engine | ❌ none | ❌ none | ⚠️ viewer/pane only | ✅ real xlsx/pptx/docx | ✅ doc-gen agent |
| Artifact pane | ✅ viewport | ✅ preview rail + gallery | ❌ markdown only | ✅ best pane | ✅ side-by-side apps | ❌ inline only |
| Scheduler/automations | ✅ B7 + OS-backed | ✅ cron+continuity+incidents | ❌ background experimental | ✅ run-status cards | ✅ trigger+schedule self-file | ✅ jobs + flows |
| Error recovery UX | ✅ retry/switch/logs (H) | ✅ best cards + fleet update | ✅ health-gate + server switch | ⚠️ basic | ❓ undisclosed | ⚠️ basic |
| Onboarding (first-message-minutes) | 🟡 thinner | ✅ guided + choose-later | ✅ default project | ⚠️ expects BYOK fast | ✅ one-line install | ✅ keyless boot |
| Footprint | ✅ Tauri+Rust | ❌ Electron+Python venv | ❌ Electron | ❌ 340–380MB Electron | ❌ Electron | ❌ Electron+server |
| Multi-model/agent orchestration | ✅ H2 + MoA | ✅ Bot Mode + groups | ⚠️ child arrows | ✅ subagents + channels | ✅ Combos + Hub | ⚠️ flows |
| Cost/token visibility | ✅ ledger (H9) | ✅ meter + breakdown | ✅ per-message schema | ✅ footer + TTFB | ✅ per-workspace (metered) | ✅ PR #6150 |

Second tier (not in top-5 matrix, steal sources): Jan (A5 + keyring), Chatbox (BYOK simplicity + chat polish), OpenWorker (risk×mode floors), Open WebUI (workspace-gateway + RAG), Cowork (3-mode approvals + shared home), OpenChamber (goals + multi-run/fusion + per-session cost), OpenClaw (ledger + acpx + ClawHub), Crush (LSP + Catwalk), Zed (permissions schema + ACP boundary), Cursor (cards + automations), fathah/hermes-desktop (onboarding + `/usage`).

## 10. Positioning hooks (inputs to TODO:1276/1291-1292)

1. **"Every agent. Your keys. Your machine."** — the only cockpit that hosts ALL harnesses (ACP-driven) with zero founder servers. holaOS couples; Hermes/Cowork account-bind; Zen retains.
2. **Guard-2 approvals with floors, not vibes** — OpenWorker-grade risk×mode matrix + Codex-grade OS sandbox + Zed-grade permission regex, local and auditable.
3. **Memory that compounds** (P5 algos + FSRS + reinforce) vs summaries-only (opencode) and foundation-only (open-cowork).
4. **Real deliverables, really local** — byte-stable Office engine + signed-in browser control, no per-call meter (anti-holaOS).
5. **Tauri+Rust footprint** vs Electron+Python/venv/server everywhere in this batch.
6. **Honest cost ledger** — per-session tokens/cost with OpenChamber-style visibility and the OpenClaw-missing cost carrier built in from day one.

## 11. Steal queue → spec rows (no code copied; AGPL/GPL/FSL/Modified-Apache = patterns only) — filed as TODO P51 (33 items, all open)

- **A providers/BYOK:** Cherry drawer (rotation+fetch+health+capability) + Jan keyring + Chatbox Spotlight + AnythingLLM keyless boot + router + opencode variants/policy + Crush Catwalk.
- **A5 local models:** Jan fit-pill + quant tiers + pause/resume + bulk-delete + VRAM guard (gold standard); AnythingLLM Foundry/GenieX embedded second.
- **B sessions/agents:** OpenClaw ledger+flows+push (+cost carrier) + OpenChamber goals/multi-run/fusion + Hermes Bot=profile forever-chat + routines + typed-reason DMs + Cursor cards/automations + Cowork schedule/projects + OpenWorker specialist picker.
- **C chat/composer:** Cowork one-composer switch + Zed @/picker/compaction + Cherry topics/citations + AnythingLLM CoT rollup + true abort + draft persist + Jan queue-while-stream + Chatbox branches/minimap.
- **E browser/sandbox:** Hermes annotate loop + execution-boundary banner + proxy-header allowlist; Codex Seatbelt/bwrap profiles; OpenClaw host-runtime honesty (no sandbox theater); open-cowork path/sandbox composition (default-deny, VM badged).
- **F MCP/connectors:** open-cowork npx-resolution + name-sanitize + OAuth + bundled-node PATH; AnythingLLM lazy-start + no-restart refresh; Jan router + orchestration toggle; Cherry searchable tabs; acpx aliases+queue+doctor; holaOS per-scope IM approve.
- **H UI/UX:** Hermes error cards + onboarding + status bar + timeline + HUD/Quick Entry; opencode tabs/multi-server + drafts.sqlite + always-patterns; Cherry settings-search + artifact routing; OpenChamber ops rail + relay pairing; Zed review/permissions; Cursor Agents Window; fathah `/usage` footer.
- **I skills:** ClawHub publish/version/vector/pin + OpenChamber catalog (git sources, collision triage) + Cowork bundle + Crush/Zed Agent Skills paths + Hermes hub fallback + open-cowork hot-reload loader.
- **J security:** deny-first + floors + MCP-EXTERNAL + profiles + auto-review fail-closed + inbox-parked unattended + provenance (OpenWorker/Codex/Claude/Zed composite); dual-provider remote-attach (Hermes OAuth-portal/user-pass-LAN-only); `REMOTE_URL`-override + sign-in-still-required.
- **P4 office:** Cherry pane routing + holaOS real-file saves + AnythingLLM doc-gen/`/img` + Chatbox image-gen (pane pattern from Cherry).
- **P5/P6 memory/routines:** Hermes cron-continuity + notepad + monitor + preflight + drift-guard + incidents + doctor + runs ledger; opencode compaction budget formula + overflow replay.
- **P49 remote:** Tailscale-first + bot fallback + workspace-as-model (Open WebUI) + per-workspace cost display without toll.

## 12. Ledger deltas (applied as ledger section 35, 2026-09-04 — doc 27 touched this pass)

New names: `openchamber/openchamber` (~9.5K★, MIT, STEAL) · `fathah/hermes-desktop` (14.1K★, MIT, REF-UX-only, unaffiliated) · `chatboxai/chatbox` (41.6K★, GPL-3.0, canonical move from `Bin-Huang/chatbox`) · `openclaw/acpx` (3.2K★, MIT, STEAL) · `NousResearch/Hermes-Bot-Mode` (659★, MIT, ARCHIVED → in-tree, note only) · `reecelikesramen/open-webui-desktop` (93★, MIT, unofficial fork, note only). Moves: open-cowork engine note (pi-agent, not Claude-Code-only); Jan license Apache-2.0; Cherry AGPL-3.0; Chatbox GPL-3.0 + Electron; Crush FSL-1.1-MIT. Live-star refreshes in §1 table.

## 13. Open legs (hands-on — needs a GUI machine)

P12.1 install/test legs for AnythingLLM, Jan, Cherry Studio, OpenWorker, Chatbox, Open WebUI desktop, holaOS, Hermes Desktop, opencode desktop, open-cowork: none installed here (no display; WSL down). Doc 86 covers code/docs/review-level depth; first-run feel, perf, and installer behavior still need hands-on runs on a GUI box before rows close.

## 14. Key URL appendix (all fetched live 2026-09-04 across the six passes)

**opencode:** `github.com/anomalyco/opencode` · `opencode.ai` · `opencode.ai/download` · `opencode.ai/docs/{providers,permissions,agents,config,models,zen,acp,tui,keybinds,themes,share,ide,policies}` · `registry.npmjs.org/opencode-ai/latest` · raw `dev/package.json`, `packages/{opencode,desktop}/package.json`, `packages/desktop/{electron-builder.config.ts,AGENTS.md,README.md}`, `packages/desktop/src/{main/{index,ipc,server,windows,onboarding,updater,menu},renderer/index.tsx,preload/types}.ts`, `packages/app/src/app.tsx`, `packages/opencode/src/{session/compaction,tool/task}.ts` (+ GH contents API for `packages/`, `packages/desktop/src`, `packages/opencode/src` trees).

**open-cowork:** `github.com/OpenCoworkAI/open-cowork` (+ releases v3.3.1) · `opencoworkai.github.io/open-cowork` · raw `package.json`, `electron-builder.yml`, `ROADMAP.md`, `CHANGELOG.md`, `SECURITY.md`, `src/main/{sandbox/native-executor,sandbox/path-guard,tools/tool-executor,schedule/scheduled-task-manager,config/config-store,agent/agent-runner,skills/skills-manager,utils/{store-encryption,artifact-parser}}.ts`, `src/renderer/{components/{WelcomeView,ConfigModal},utils/artifact-steps}.tsx?(.ts)` + tests (`scheduled-task-manager`, `mcp-npx-resolution`, `api-diagnostics`, `config-store-env`, `chat-view-stop-entry`, `artifact-icon`, sandbox/path containment + command-injection).

**hermes:** `github.com/NousResearch/hermes-agent` (+ releases, v2026.8.31 Pantheon notes) · trees `apps`, `apps/desktop`, `apps/desktop/{electron,src,src/plugins}` · `apps/desktop/{package.json,README.md,DESIGN.md,electron/backend-command.ts}` · `hermes-agent.nousresearch.com/{desktop,docs/user-guide/{desktop,bot-mode,profiles,multi-connection-desktop,features/cron,configuration},docs/developer-guide/programmatic-integration,docs/user-guide/{integrations/nous-portal,guides/run-hermes-with-nous-portal,reference/cli-commands}}` · `github.com/NousResearch/Hermes-Bot-Mode` (+ README) · issues #95031, #95745 · `github.com/fathah/hermes-desktop` (+ README, releases v0.7.6, `package.json`) · `hermesdesktop.app/docs`.

**anythingllm/jan/cherry/chatbox:** `github.com/{Mintplex-Labs/anything-llm,janhq/jan,CherryHQ/cherry-studio,chatboxai/chatbox}` (+ releases: v1.16.1 / v0.8.4 / v2.0.12 / v1.23.1) · `docs.anythingllm.com/{,agent/usage/web-browsing,mcp-compatibility/desktop,installation-desktop/{overview,system-requirements},chat-ui,features/keyboard-shortcuts}` · `jan.ai/docs/desktop{,/manage-models,/assistants,/settings}` · `docs.cherry-ai.com/docs/en-us` · raw `package.json` ×4 + `LICENSE` (anything-llm MIT, jan Apache-2.0) + `chatboxai/chatbox electron-builder.yml` · `github.com/Mintplex-Labs/anythingllm-desktop` (404, verified no standalone repo).

**openworker/open-webui/claude-code/codex/holaos:** `github.com/andrewyng/openworker` (+ releases v0.2.0/v0.2.1) · raw `coworker/{permissions,risk,connections}.py` · `github.com/open-webui/{open-webui,desktop,computer}` · `docs.openwebui.com/{features,features/workspace/knowledge,features/extensibility,ecosystem/computer,getting-started/quick-start/connect-an-agent/cptr}` · `openwebui.com/computer` · `code.claude.com/docs/en/{permission-modes,permissions,hooks,hooks-guide,sub-agents,agent-sdk/permissions}` · `developers.openai.com/codex/{agent-approvals-security,permissions,agent-configuration/subagents}` · `github.com/holaboss-ai/holaOS` (+ raw `LICENSE`) · `holaos.ai{,/pricing}`.

**cowork/openchamber/openclaw/crush/zed/cursor:** `claude.com/product/cowork` + support articles (13345190, 13364135, 14479288) · `openchamber.dev` + `docs.openchamber.dev/{quickstart,install,skills-catalog}` + `github.com/openchamber/openchamber/blob/main/README.md` · `github.com/openclaw/{openclaw,acpx}` + `docs.openclaw.ai/{cli/acp,tools/acp-agents,tools/acp-agents-setup,automation/tasks}` + `github.com/openclaw/openclaw/issues/130257` (ledger cost gap) + `github.com/openclaw/clawhub` + `clawhub.ai` · `github.com/charmbracelet/crush` (+ discussions #360, issues #1097) · `zed.dev/docs/ai/{agent-panel,external-agents,tool-permissions,parallel-agents,skills}` + `zed.dev/acp` · `cursor.com/{docs/cloud-agent,docs/cloud-agent/automations,docs/approval-agents,docs/rules,help/ai-features/background-agents}`.
