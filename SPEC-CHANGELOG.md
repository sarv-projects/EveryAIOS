# DESKTOP-APP-SPEC.md — Version History (archived)

> **Then 2026-08-29 (skills-store install flow + engine pure-stage Rust port pass):** two post-user-request items landed. **(1) Skill-store install flow (P9.7 partial → the signing *and* install/consent half; marketplace-index provider side stays post-v1):** `everyaios-guard::skillstore` (one Ed25519-signed skill index, verified against a pinned `STORE_PUBLIC_KEY` embedded in the shell; tampered index ⇒ rejected, nothing installs; capability floor `RUNTIME_CAPABILITY_ALLOWLIST` = `fs.read`·`fs.write`·`tool.mcp`·`tool.connector`) + new `src-tauri/src/skills_cmds.rs` `{skills_catalog, skills_install, skills_uninstall}` (Tauri commands; catalog re-verifies the signed seed on every call; install writes a verified `SKILL.md` into `blueprint::SkillStore` → `~/.everyaios/skills/<slug>/SKILL.md` — the manifest name is the slug `id` because `SkillStore::save` validates `[a-z0-9-]+`) + the **Connectors→Skills** UI tab (`ui/src/components/panels/skills-panel.tsx` + `ui/src/lib/skills.ts` bridge) rendering per-skill plain-language Guard-2 consent cards + install/uninstall state. 2 embedded-seed shell tests. **(2) `everyaios-engine` crate — the engine's *pure* stage pipeline ported to Rust (Alg #2/#17):** `contract::default_contract` (surface-contract.ts), `plan::{plan_retrieval, plan_tools, family_of}` (retrieval/tool-planner), `risk::assess_hallucination_risk` (Alg #8 risk compass, score contract exactly mirrored), and `gate::{evaluate, approve, clear, SessionApprovals}` (Alg #17 PermissionGate + per-session approval map — allowlist hard-fail, `higher_risk`, session-first/always confirm, 30-min TTL, size cap 200). 26 tests, clippy-clean. **Honest boundary:** the async `ConversationEngine run()` loop stays TS (I/O-bound; the coordinator `StreamSession` overrides it) — recorded in spec §5/§4.2.9/§4.0 and TODO as the remaining multi-thousand-line port, not faked. **Docs:** spec crate map heading + §6 both updated 17→**18 crates** (added the `everyaios-engine` row), algorithm-index Where cells #2/#17 point at the Rust modules, I2 row records the signed store, §4.2 gained mermaid **4.2.8 Skills-Store install flow** and **4.2.9 Engine pure-stage port** (both diagrams, no ASCII), TODO P9.7 updated + new engine `[DONE]` row. No algorithm-count change (still 34), no census change (docs-only + 1 new crate's tests).
> **Then 2026-08-26 (v3.58 — architecture-review contradictions + hardening pass; every claim verified against code first):** a 22-point architecture review was line-checked; 11 items were real and are applied, the rest were verified already-covered or agreed post-v1. **Contradictions fixed:** **(1) §0 invariant scoped** — now "every side-effecting operation **performed through an EveryAIOS-controlled capability**" + the honesty clause that an external agent's own internal effects in Self-contained mode are governed only by that agent's declared sandbox (EveryAIOS claims no visibility — J17) — the old wording listed "external-agent mutation" as ticketed, which contradicted the Self-contained path. **(2) Capstone reworded** — "every meaningful write approved" → "every mutation governed by the user's autonomy policy (risky approved, permitted low-risk automatic — H34)"; "every data disclosure explicit and policed" → "data access and external transmission governed and policy-checked (full two-zone data-release firewall = post-v1, K5)"; end-state sentence explicitly labeled **FINAL VISION** with a v1-scope qualifier (voice H15 / clipboard H26 / TTS H28 post-v1). **(3) H27 split** — v1 = checkpoint-based resume after transient disconnect/session restart; advanced multi-device/distributed streams post-v1 (prose marker added in §4.2.5). **Hardening additions:** **(4) A11 probe-safety policy** — `CapabilityProbePolicy { MetadataOnly · SafeSynthetic · Billable }`, default MetadataOnly, SafeSynthetic only on a provider-declared safe no-op test, Billable only on explicit user action (a tool-call round-trip is not necessarily side-effect-free); mirrored in ARCH/09. **(5) Capstone/§0 positioning** — "protocol-native" → "protocol-native **MCP/ACP/A2A** control plane" with explicit roles (ACP=drive · MCP=consume+serve · A2A=discover; never a v1 ACP server, §8). **(6) `BatchTicket` named type** — UC-1 "approve all" binds an exact immutable change set (operation list + args-hashes + resource identities), never an operation category; listed in the §6 named-type contracts. **(7) P9 build row split** — P9 (v1) = E9 computer-use (landed); P9+ (post-v1) = WASM/HTML→video/H18/A8/AutomationBench/full CLI/A10/H26/H28. **(8) Cache mermaid diagram** — intent-classification gate before prompt/semantic/result lookup (mutation intents skip semantic/result replay entirely), matching the A9 prose that was already there. **(9) H35 split** — **`everyaios doctor` promoted to a V1 support primitive** (per-subsystem readiness, 2026-08-26 review: "not a post-v1 luxury"); full admin CLI (providers/models/agents/mcp/skills/tasks/audit/sessions/repair/export/import/acp) stays post-v1; queued in TODO P46.2 (no new checkbox — census unchanged). **(10) Office distinction made explicit** — "V1 Office (shipped): honest viewer · supported surgical edits · Guarded mutation · rollback · conformance" vs "Post-hold Office (not shipped): full-fidelity viewer · richer UI · full Google surface · Impress-like behavior · perfectness-gap features" in the spec + ARCH/09 D headers. **(11) §0 status/scope legend added** — post-v1-marked rows = FINAL VISION/planned; unmarked rows = v1 scope (V1 CORE vs V1 OPTIONAL per TODO); colors (🟢 landed · 🔵 wired · 🟡 partial · ⚪ post-v1) live in ARCH/09. **Verified already-covered (not re-added):** auth-mode badge + governed-badge already shown together on the H32 picker + composer chip (spec line 671); `/learn` stays post-v1 (agreed); R8 remains the #1 tracked engineering gap (browser attach → ExecutionKernel disk → K1 receipts, all queued). **Census unchanged by this pass: 1142 = 1055 done + 87 open at the time** (docs-only; P46.2 stays one `[NOT DONE]` item). **Amended 2026-08-26 (code audit):** the P44.6 three-control composer UI landed in the same window (commit `adf99c0` — `chat-composer.tsx` + `store.ts` `permissionMode`) — current census **1142 = 1056 done + 86 open**. **Amended 2026-08-26 (P44.6 remainder):** the per-task `config_hash` freeze, Autonomy Limit escalation card (Do Once / Allow For This Task / Change Level, surfaced from live Guard tickets), temporary elevation (task-only AUTO, expires at turn end), live autonomy indicator, and 7 store tests landed in `ui/src/lib/store.ts`/`bridge.ts`/`now-doing-strip.tsx`/`autonomy.test.ts` — census unchanged (row was already `[x]`); Rust `permissions.toml` preset maps stay open on P44.5.

> **Then 2026-08-26 (UI mock + UI-DESIGN-PROMPT v3.57 pass):** the `ui/` tree is the mock of the finished product; it still shipped the v3.41 **Normal · Plan · Research · Quick · Code** pills even though spec §4.1 / TODO P44.6 already specified three independent controls. **Code:** `ChatMode` is now `auto | plan | build | research` (old `normal`/`quick`/`code` normalize on restore); composer always shows **Work Mode ▾** + **Autonomy ▾** (casual chips `[🤖 Auto] [🛡 Ask]`); power reveals **Agent ▾** + Experts/Spec + budget/ctx; Settings Chat & Auto-run / Permissions use H34 labels (Sandbox / Ask / Auto / Maximum — Maximum is not a Guard bypass); now-doing strip shows the live autonomy chip; **⌥M** / **⌥U** cycle mode/autonomy (OpenCode Tab-for-Plan/Build is *not* copied — Tab stays focus); title/status badge `v3.57`. **Docs:** `UI-DESIGN-PROMPT.md` header, office views (honest engines, not ribbon clones), Guard v1 webview+nonce, Connectors live-OAuth + P42 not-attached, §13 competitive lineage (OpenCode Plan/Build, Cowork Home+Ask, Hermes sandbox/providers, OpenClaw task ledger); `ui/DESIGN-SYSTEM.md` composer + layouts index. No checkbox flips — **census stays 1142 = 1055 done + 87 open**.

> **Then 2026-08-26 (second live-status pass — six leftover wording/formatting nits closed):** a follow-up review listed six non-contract nits; each was verified against code and fixed. **(1)** TODO `---## PHASE 4` markdown glue fixed (separator + heading on separate lines — the PHASE 4 heading now renders). **(2)** TODO scope line "151 capabilities" → **156** (spec §0 index v3.57 incl. A11/H34 + I13/H35/J24). **(3)** TODO UI-v2 migration note (2026-08-16) amended with 2026-08-26 status: Audit view is live-capable via the `lib/audit.ts` replay/audit bridge (demo fallback only in plain-browser preview); the Native connectors tab renders real vault OAuth accounts (`oauth_accounts`) and the P42 Graph/Workspace connector crates landed 49/49 — the "no OAuth wiring yet" placeholder line is superseded (live external OAuth attach stays P42/P6 UI-wiring follow-on); the P11.5 "Audit/Storage views are mock" row annotation corrected the same way. **(4)** TODO P4.7b D2-gap row: "viewer surfacing remains UI work" → landed 2026-08-26 (P34.2 track-changes display via `docx_tracks` in the Word honest viewer). **(5)** Spec §9 R8 closing paragraph: "browser/office/search engine attachment" → **browser** engine attachment only (search + office dispatch through the ticketed executor as of 2026-08-25/26, per the R8 body; browser honest-fail until CDP attach stays true). **(6)** TODO P33/P34 goal intros relabeled "**Original goal (ARCH/12 v3.x — long-horizon target)**" so the "complete Microsoft ribbon / LOKit" product sentence reads as the historical target, not current contract — the hold/lift qualification follows immediately. No capability change, no checkbox flips — **census stays 1142 = 1055 done + 87 open**.

> **Then 2026-08-26 (live-status reconciliation — stale "current-state" lines corrected against code, per a line-by-line audit):** an external doc review listed ~20 lines that read as today's contract but contradicted later rows/landings; every claim was verified against code before touching anything, and the stale lines were corrected in place (no capability change, no checkbox flips, no count change — **census stays 1142 = 1055 done + 87 open**). **(1) Office-hold wording scoped to the actual 2026-08-26 partial lift** — spec §0 D header + H5 + §2 scope + §6 item 6 + P4 phase row, ARCH/09 D header, ARCH/12 §4.1c, TODO PHASE 4 header + SUMMARY TOTAL + P33/P34 goal blurbs + P33.3 row all now read: engine + honest-viewer tier landed (per-file tabs, LO optional companion, Google Docs browser read path, P34.2–4.5/4.7 honest viewers with ticketed edits + takeover locks), full-fidelity tiers (in-process LOKit, Google full surface, MS-ribbon clones, freeze/autofilter/CF, sign/stamps/canvas draw, Copilot panes) remain post-hold — the earlier blanket "no further Office work" was factually wrong since honest-viewer UI shipped. **(2) G8/search + office attachment status corrected** — code dispatches `search.query` → `everyaios-search::G8Cascade` and office ops through the ticketed executor (tools.rs `g8_search_dispatch_returns_hits`; the "G8 search cascade is not built" string is gone); ARCH/09 G8 "NOT built" → "✅ LIVE on the ticket path (2026-08-25)"; TODO S0.1 dispatch row + Stage-0 intro updated; spec R8 honesty-annotation wording narrowed to the genuinely-unattached surface (browser "session not attached" — verified still honest in tools.rs). **(3) TODO P36 RouteDecision honesty note updated** — was "router.ts is capability-filter + cost-sort only" (pre-wiring state); now records the live loop: `router.ts` ports the consensus scorer and feeds per-turn `ProviderObservation`s (`observations.ts` + `chat.ts`), cost-sort only as the no-observations floor. **(4) Path-floor warning corrected** — TODO H1 said "pathfloor is not called from `xlsx_cmds.rs`"; verified `control::floor_user_file` → `everyaios-guard::pathfloor::enforce_floor` is called on the xlsx/fs/office commit paths (7 xlsx sites + fs + office). **(5) P4.7/P8.5 row annotations updated** — Word/PPT P4.7 rows no longer claim "no surgical edit UI"/"notes panel follow-up" (P34.2/4 landed); P8.5 Settings no longer claims BYOK is local React state (`vault_keys_list/add/remove` are live Tauri commands wired into `settings-sections.tsx`, verified). **(6) Nits fixed** — TODO header "Spec: v3.55" → v3.57; duplicate P7.8/P7.9 parenthetical removed from the TODO header; ARCH/09 duplicate `## B. Agent orchestration` header removed; ARCH/09 A5 "P27 backend not built" → "P27 landed 2026-08-23 (6/6)"; TODO P42/P33.6 relation note un-frozen. Historical changelog entries (incl. the 2026-08-25 claimed-vs-reality pass) preserve their dated claims per convention — they are archive, not current status.

> **Then 2026-08-26 (Hermes computer-use read — five E9/E1 implementation rules folded in; engine already landed):** a Hermes `computer_use` + permissions source-read was line-checked against the E9/E1 spec rows and the landed `everyaios-desktop` crate (P9.1 8/8, 2026-08-23). Most of what the read proposed was **already established**: observe→one-action→re-observe loop (P9.1 Act/Verify rows), UIA+OCR (Read/Vision rows), semantic element refs (`ref_id`/`@e1` in everyaios-browser selector/locator), Guard-2 hard-denies + kill switch (policy), tiered engines / browser-stays-CDP (Layer-1-first), macOS twin, element-ref style (`eN`). **Five rules were genuinely new and are now folded into the spec E9 row (+ ARCH/09 mirror + TODO P9.1 annotation) as contracts on the landed engine — not new scope:** **(H1)** `DesktopSnapshot` canonical observe type — screenshot_ref + window_id/app_id + AX tree + interactive elements + bounds/labels/roles/states + stable `eN` refs + timestamp (SOM pattern; composes the existing `SeeResult`/`ReadNode`/`ReadResult` + `VisionHit`), model acts by stable element ref, never raw pixel coords. **(H2)** **Ref-invalidation hard invariant** — every state-changing action invalidates prior refs; re-observe before the next action (observe → act → invalidate → re-observe → verify); applied to both desktop (E9) and browser (E3 row annotation; `parse_batch` in `locator.rs` consumes fresh snapshots; small code follow-up = explicit locator invalidation). **(H3)** **Background/foreground interaction policy** — default Background (drive the target without stealing cursor/keyboard focus when possible); Foreground only for app-required focus, unmanipulable modals, user ask, or challenge interaction. **(H4)** **Readiness as derived state, never a binary claim** — ● Ready · ⚠ Permission required (macOS TCC / Win UIAccess / Linux AT-SPI) · ⚠ Driver missing · ⚠ App unsupported · ✕ Blocked by policy; honest failure wording per EV1 READY/DEGRADED/BLOCKED; feeds H35 `everyaios doctor`. **(H5)** **Opaque session-owned driver state** — DesktopSession binds owner_work_id/agent_id/run_id + target_window + snapshot_version + opaque backend state; no cross-agent reuse without explicit ownership transfer (mirrors browser TabRegistry claims); helper/driver processes never inherit model/API keys or vault secrets unless explicitly granted (child-env sanitization, same class as the vault-key env scrub in `vault_key.rs`). **Follow-up tracked (same day):** the one code piece the read flagged — explicit ref-invalidation in `locator.rs`/`selector.rs` (`ref_id`/`eN` + `parse_batch` fresh-snapshot parsing exist; the invalidation call is implied, not enforced) — is queued as **TODO P46.4** with an acceptance test (a ref from a pre-mutation snapshot cannot be used post-mutation without re-observe). Census: **1142 = 1055 done + 87 open** (was 1141/1055/86; P46 3→4 open).

> **Then 2026-08-26 (v3.57 — third-party comparison line-check: five real items applied, the rest verified already-covered):** a Hermes/OpenCode/ChatGPT-Work/Cowork comparison chat was checked line-by-line against spec + TODO + code — no assumption, no bias; nearly every "gap" it named was verified **already landed or already a written decision**, and exactly five items were real and are now applied. **(1) Spec §0 one-liner corrected — effect plane vs inference plane:** the invariant now scopes to "every **side-effecting** operation that changes user state, external state, credentials, network resources, or connected systems"; model/agent **inference stays in the model/data plane** (credential policy, budget, egress — never a Guard-2 card per call, already the J8/A2/A3/A9 architecture). **(2) A11 wording split — ProviderRecord = Identity + Profile, ProviderObservation stays the separate runtime-observation contract (A7/H9)** (code already carries `verified_report`/`capabilities_verified_at` on the record; observations ride RouteDecision). **(3) A6 catalog wording — vendored bootstrap snapshot + live refresh + cache + source version + ETag/hash** (the "shipped in the binary" vs "live catalog" duality is now explicit bootstrap/fallback). **(4) Composer — three independent controls replace the v3.56 "Normal · Plan · Research · Quick · Code" mode pills (which mixed task type + execution strategy + UI convenience):** **Agent ▾ (WHO — H32 picker, default Auto)** · **Work Mode ▾ (WHAT — 🤖 Auto · 📐 Plan · 🔨 Build · 🔎 Research; Code/browser/Office/terminal are capabilities inside Build, not modes; the agent may choose/transition modes, the user keeps the final override)** · **Autonomy ▾ (HOW MUCH — H34 Sandbox/Ask/Auto/Maximum, separate from mode)**. Default composer `[🤖 Auto] [🛡 Ask]`. Applied to spec §4.1, UI-DESIGN-PROMPT power row, and TODO P44.6 scope. **(5) §8 non-goals written (so they are never re-raised as gaps):** team/shared Work in v1 (post-v1 Shared Work = cryptographically scoped: Work snapshot + selected artifacts/receipts/memory references, never vault/raw memory; same user-owned-node principle as H33) + EveryAIOS-as-ACP-server in v1 (client-only stance stays; future `everyaios acp` would enter the same Work kernel, never a path around the executor). **New matrix rows (spec §0 index 153→156; ARCH/09 mirrors): I13 — Knowledge→Skill Compiler (`/learn`, post-v1; rides landed `skill_store::scan`/`grow_from_task` + Forge `iterate`/`tdd_loop` + I6 ABI) · H35 — CLI/doctor/admin surface (post-v1; `everyaios doctor`/`providers`/`models`/`agents`/`mcp`/`skills`/`tasks`/`audit`/`sessions`/`repair`/`export`/`import`/`acp`; rides Tauri command layer + `--headless`, no second runtime) · J24 — credential-provider abstraction (post-v1; CredentialBroker opaque-fill boundary, extends E9 hard-deny + Session Vault from storage to input). Queued as **TODO P46 (3 items, all `[NOT DONE]` post-v1)**. **Verified already-covered (not re-queued):** J17 governance badges (Governed-Mediated | Self-contained | NotGoverned + no-boundary→no-claim honesty) · J17 "omitting fs/terminal does NOT force MCP Channel B" (v3.46 correction) · credential classes (brokerable vs process-bound, 2026-08-18) · ManagedResource process-only wording · J21 permissions bands + I6 allow-lists (OpenCode-expressiveness parity) · A9 read-only-intent cache gating · messaging MessageAdapter trait + Telegram/WhatsApp/Email adapters (F13 desktop-first) · P9.1 password-manager hard-deny · P9 split (E9 v1, P9.2–9.9 post-v1) · H33/P40 user-owned node 3/3 · native OS card = documented open hardening · ACP-server = non-goal. ARCH/09 bucket totals re-counted from live rows (25/67/66/16 — drift from row-level status upgrades 08-23→08-26; ⚪ 13→16 from the three new rows); spec §6 live-count claims 151→156 and census 1138→1141 updated in lockstep. **Census: 1141 = 1055 done + 86 open** (was 1138/1055/83; P46 added 3 open).

> **Then 2026-08-26 (perf-review re-check — P29/P39 already landed; P45 queued for the real micro-perf deltas):** a re-stated performance-research doc was verified line-by-line against the tree rather than taken at face value. **Confirmed already landed (not re-queued):** P29 native-sidecar migration **11/11** (Tier 1a–1c collapse IPC + guard→native + **broker owns SSE token streaming end-to-end** — the doc's "biggest single win" is done; Tier 2a–2d memory/search/files/automations→Rust; Tier 3a–3c; exit criterion); P39 perf queue **5/5** (payload budgets, distillation seam, MCP keep-alive pooling, KV-cache knob, lazy-load enforcement); browser E3/E16 short-circuit + slim snapshots; `useVirtualList` UI virtualization; office windowed-read + lazy pdf.js; memory quantized intro/parallel fusion/result cache/LazyGraphRAG. **Confirmed decided-against (never re-queue):** MessagePack / FlatBuffers / shmem ring stay **not adopted** per **R4** ("binary only if profiling proves JSON matters") — the doc's "free 3×, just do it" contradicts the spec's own measure-first rule; Python-as-engine remains the documented non-goal (the doc concludes the same); P29 Tier 1–3 as if open would be stale. **The doc's Tier-0 "✅ already done" flags were factually wrong** for several items absent from the tree — corrected and now queued as **TODO P45 (9 items, all measurement-gated like P39)**: SQLite `synchronous=NORMAL` on non-vault DBs (vault stays safer) · `mmap_size` on read-heavy indexes · `journal_size_limit` + WAL checkpoint throttling · connection pool (4R+1W) · honesty-preserving turn-boundary audit-NDJSON batching · `content-visibility:auto` · provider DNS cache · 60s sidecar idle-kill (heap.ts is 30-min rotation today) · UI memo/worker/speculative-prefetch. Each P45 item carries an R4 measurement gate + acceptance test; audit batching must not reduce the append-only integrity invariant. **Census: 1138 = 1055 done + 83 open** (was 1129/1055/74; P45 added 9 open).

> **Then 2026-08-26 (P44.4 landed — capability-probe verification in code):** the "catalog says ≠ runtime is" rule moved from queue-contract to a real implementation. New module `everyaios-catalog/src/probe.rs` (pure + testable; the network probe is the caller's injected transport seam). **(P44.4)** `ProbeResult` (observed_model_ids / tool_call_ok / structured_output_ok / observed_context_len / codex_ok), `AdvertisedHardCaps`, and `verify_report(advertised, observed)` — a verdict for **every** hard capability (Tools / StructuredOutput / CodexResponses): Verified when advertised-and-confirmed or observed-beyond-advert, **Unverified (fail-closed)** when advertised but the probe did not confirm — routing must not rely on it — and Unadvertised when neither advertised nor observed. `VerificationReport.hard_caps_verified` = true only when nothing advertised sits in Unverified; `trusted_capabilities(report)` returns exactly the Verified subset routing may use. **Registry integration:** `ProviderRegistry::apply_probe(canon, observed, verified_at)` is alias-aware, computes the advertised hard caps from the provider's catalog/transport metadata, stores the report on `ProviderRecord.verified_report`, stamps `capabilities_verified_at`, and returns the fully-verified bool; `verified_at()` reads the stamp back. Same verified-vs-advertised pattern feeds agents/MCP (J17 / F6) by building a `ProbeResult` from their handshake outputs rather than `/v1/models`. **Verification:** 5 new probe tests + 2 registry integration tests — everyaios-catalog now **37/37** (was 32); `cargo check --workspace` 0 errors (the one `everyaios-core` unused-method warning is pre-existing). Remaining P44: P44.5–8 autonomy presets / chatbar / discovery surface / routing feed, P44.9 tests+wiring. **Census: 1129 = 1055 done + 74 open** (was 1054/75; P44 6→5 open).

> **Then 2026-08-26 (P44.1–P44.3 landed — Provider Record + alias + OpenAI-compatible profiles in code):** the three core engine pieces of the v3.56 A11 ProviderRecord queue moved from spec/queue-contract to a real crate implementation. New module `everyaios-catalog/src/provider.rs` (pure + testable, same discipline as the catalog crate — network/registry access stays an injected caller seam). **(P44.1)** `ProviderRecord` — typed provider identity: `Transport` (openai_chat / anthropic_messages / codex_responses / bedrock_converse — the API-mode mapping that decides wire protocol), `Auth` (api_key / api_key_env / oauth_device_code / oauth_external / external_process / aws_sdk / vertex / keyless), api_key_env handles, base_url + base_url_env override, `AggregatorKind` (is_aggregator vs is_routing_aggregator — OpenRouter passthrough vs OpenCode-Zen/Go flat-namespace reseller), models_source, capabilities, `DiscoverySource` provenance (models.dev → overlay → user config → plugin profile), capabilities_verified_at, config_hash — plus `merge_from` field-by-field resolution-order merge (later layers override per-field, never wholesale replace) and `effective_base_url`. `ProviderRegistry` — canonical-id + normalized-alias lookup, `canonical_id`, and `mark_verified` write-back (accepts aliases). **(P44.2)** alias normalization — `normalize()` case/separator folding (Claude-Code/claude_code→claude-code) + the `ALIASES` table (claude/claude-code/claude-agent→anthropic, chatgpt→openai, codex→openai-api, glm/z-ai/zhipu→zai, kimi/moonshot→kimi-for-coding, nim/nvidia-nim/nemotron→nvidia, dashscope/aliyun/qwen→alibaba, aws/amazon-bedrock→bedrock, hf/hugging-face→huggingface, ai-gateway/aigateway→vercel, zen→opencode, grok/x-ai→xai, llamacpp/lmstudio/ollama→local …). **(P44.3)** the OpenAI-compatible profile table — one `OpenaiChat` transport, 13 reference profiles (Baseten `https://inference.baseten.co/v1` · Cerebras `api.cerebras.ai/v1` · DeepInfra `api.deepinfra.com/v1/openai` · DeepSeek `api.deepseek.com/v1` · Fireworks `api.fireworks.ai/inference/v1` · Groq `api.groq.com/openai/v1` · OpenRouter `openrouter.ai/api/v1` · Together `api.together.xyz/v1` · xAI `api.x.ai/v1` + novita/stepfun/perplexity/mistral long-tail) with reference base URLs + `_API_KEY` env shapes + full user base_url/base_url_env override; `base_registry()` also registers the distinct non-OpenAI transports (anthropic-messages / openai-api codex-responses / bedrock-converse+aws-sdk / vertex / nous device-OAuth / opencode-free keyless / local) and tags aggregators (openrouter=IsAggregator, opencode=IsRoutingAggregator). **Verification:** 9 new provider tests green (normalize folding, resolution-order field-by-field merge, aliases→canonical, profile transport+reference-url, user base-URL override, mark_verified flip, distinct transports) — everyaios-catalog now **32/32** (was 25/25); `cargo build -p everyaios-catalog` clean no warnings; `cargo check --workspace` 0 errors. Keys still resolve in Rust only (`effective_base_url` never holds a secret). Remaining P44: P44.4 capability probes, P44.5–8 autonomy presets / chatbar / discovery surface / routing feed, P44.9 tests+wiring. **Census: 1129 = 1054 done + 75 open** (was 1051/78; P44 9→6 open).

> **Then 2026-08-26 (office hold lift — honest viewers + companion + catalog sync pass):** the frozen Office-hold rows (P33.5/6, P34.2–4.5/4.7) plus P14.5 and P30.16 were implemented **per the documented doc-29 verdict** (wire what exists; no in-process LOKit, no MS-ribbon clones, no bundled office MCPs — the verdict stands and is now the implementation). **P33.5** — the optional LibreOffice companion is live (`find_soffice`/`office_open_external` + "Open in LibreOffice" buttons + error-banner fallback in all four office views; soffice convert + CI conformance oracle already in-tree); in-process LOKit is a documented non-goal (LO re-serializes and drops unknown OOXML, conflicting with the surgical "untouched bytes stay" invariant — doc 29). **P33.6** — Google Docs/Sheets read path: `store.openInBrowser(url)` routes to the authenticated browser view + auto-navigates; `OfficeOpenBar` detects `docs/sheets/drive.google.com` links (hostname-exact, lookalike-safe — 3 tests green); the agentic half (Drive/Sheets API → OOXML → engine → write-back) stays P42 connector wiring. **P34.2–4.5** — honest viewers on the existing engines (Word block list + selected-block `docx_patch` + track-changes; Excel ribbon + live formula bar + Avg/Count/Sum status + Guard-2-ticketed edits/pivot/shift; PPT live slides rail + `pptx_notes`; PDF zoom/fit + find + text-snippet thumbs + annotate/redact/form-fill via `pdf_page_op`); freeze/autofilter/CF, sorter/outline, sign/stamps + canvas draw tools deliberately not built (verdict: "wire the engine, don't build Impress"/"only if Univer-as-view, later"). **P34.7** — read-only-while-agent + takeover locks verified across all four views (`locked = running && !paused`; H21). **P14.5** — catalog sync automation is now runtime-callable: `sync.rs` `merge_refresh()` (per-provider merge → gate on the merged set → whole-refresh reject on any gate error; 3 new tests, 25/25 green) + Tauri `catalog_sync_plan`/`catalog_sync_refresh` in new `catalog_cmds.rs` (baseline + fetch are caller-supplied transport seams). **P30.16** — companion layer: the coordinator seam (`companionFrameFor`/`moodForState`) gained a live UI consumer (`companion-chip.tsx` in the status bar — persona name + session-state mood dot); the pixel-pet overlay itself stays post-v1 (doc 83 verdict), but the seam is no longer dead code. **Census: 1129 = 1051 done + 78 open** (was 1042/87; −9 open). Only remaining open buckets: P5.2 FFI, P6.9 Signal, P9.2–P9.9 (20), P12 GTM (47), P44 (9). Verification: cargo workspace check + everyaios-catalog 25/25 + UI tsc + bun tests green.

> **Then 2026-08-26 (v3.56 — Provider Record + Autonomy Level pass; Hermes/OpenCode source-read):** the user pasted a Hermes/OpenCode comparison + two "steal this" proposals (autonomy chatbar control; universal resource autodiscovery) and asked for each line to be verified against the live repos and folded into the spec. **Verification (source-read, not inferred):** `NousResearch/hermes-agent` `hermes_cli/providers.py` (fetched live) confirms the pasted provider architecture exactly — `HERMES_OVERLAYS` (transport/auth/base-URL/env metadata layered on models.dev), `ALIASES` (claude→anthropic, kimi/moonshot→kimi-for-coding, glm/z-ai/zhipu→zai, nim/nvidia-nim→nvidia, dashscope/aliyun/qwen→alibaba, hf→huggingface, aws/amazon-bedrock→bedrock, ai-gateway→vercel, zen→opencode, lmstudio/ollama/llamacpp→local, grok/x-ai→xai…), models.dev as the canonical live DB (109+ providers), transport→API-mode mapping (openai_chat/anthropic_messages/codex_responses/bedrock_converse), `is_aggregator` vs routing-aggregator (flat-namespace resellers OpenCode-Zen/Go), plugin profiles under `plugins/model-providers/<name>/`, and user-config merge. OpenCode (opencode.ai/docs/providers, live) confirms the 50-named provider directory + OpenAI-compatible profiles (Baseten/Cerebras/DeepInfra/DeepSeek/Fireworks/Groq/OpenRouter/Together/xAI base URLs) + arbitrary base-URL override. **What was already in the spec (verified, not duplicated):** H32 already carried the composer permission chip (Sandbox/Ask/Auto/Run everything) as a v3.41 layout reference; J21 already carried permissions.toml bands + decision packages + `min_confidence_for_auto`; ManagedResource + the four live registries (MCP/ACP/models.dev/searx) + "live-registry hit ≠ capability" + ACP initialize negotiation + MCP handshake reconcile were already §0/§6. **What was genuinely missing and is now added: (1) A11 — Provider Record + alias layer:** provider as first-class identity merged from models.dev + overlays + user config + plugin profiles, with alias normalization and the OpenAI-compatible one-transport-many-profiles table, plus **capability-probe verification** (advertised vs verified: `/v1/models`, tool-call round-trip, context probe → `capabilities_verified_at`) — the "catalog says ≠ runtime is" rule the spec only had for agents/MCP, now explicit for providers/models. **(2) H34 — Autonomy Level:** the H32 chip elevated from layout reference to a first-class subsystem — explicit per-level semantics (🛡 Sandbox plan+read-only · 👀 Ask default · ⚡ Auto low-risk-auto · 🚀 Run Everything = Maximum autonomy within hard floors — destructive/secrets/financial/security/cross-workspace/irreversible never bypass), policy-preset-on-the-existing-engine (never a Guard bypass), per-session + per-task policy snapshot frozen into `config_hash` at task start, **Autonomy Limit escalation card** (Do Once / Allow For This Task / Change Level), **temporary elevation** (task-only, expires at completion), **live autonomy indicator** during execution — the same answer as R1 approval-fatigue with one visible knob. **Docs updated:** spec §0 index 151→153 (+A11/H34 — both in the spec master index and ARCH/09), H32 annotated to point at H34; ARCH/09 totals re-counted from live rows — **153 = 151 pre-existing + A11 + H34** (the old "149" was last counted before H29–H33/I12 landed; 🟡 67→68); TODO header census 1120→1129 (1042 done + 87 open) + new **P44 queue (9 items)** (ProviderRecord merge resolver · alias normalization · OpenAI-compatible profiles · capability probes · autonomy preset maps · chatbar control + per-task snapshot + escalation + elevation + live indicator · discovery surface · routing feed · tests/wiring); README census updated. No code changed this pass — both additions land as spec/queue contracts on existing surfaces (`everyaios-catalog` A6, `everyaios-guard::permissions` J21, composer H32) with the full implementation enumerated in P44.

> **Then 2026-08-26 (QA/CI + connectors + task ledger + reconciliation pass; TODO recovered & re-verified):** **(A) The implementation round:** P10.4+P10.5 shipped — `.github/workflows/` (ci.yml Tauri-build matrix job + UI tests, nightly-e2e.yml, perf-regression.yml) + `.pre-commit-config.yaml`; `everyaios-cdp` browser-detect unit tests + vault portable-bytes test (SQLCipher refuses wrong keys at open — fail-closed). **P43 — task ledger (4/4):** `everyaios-core::task_ledger` (`TaskLedger`/`TaskRecord`, `queued→running→terminal{succeeded,failed,timed_out,cancelled,lost}`, push completion via `watch()`, 5-min lost-grace `reap_lost`, 7-day `prune`, `tasks/*` JSON-RPC in `chat.rs`) + `src-tauri/src/tasks_cmds.rs` + `ui/src/lib/tasks.ts` + Tasks rail in the Automations panel. **P42 — M365/Google connectors (3/3):** `everyaios-core::connectors::{graph,workspace,scopes}` over the HttpTransport seam (mail/calendar/OneDrive/Teams + Drive/Docs/Sheets; read-only-first scope manifests, 49/49 connectors tests green) + honest scope surface in the Connectors panel. **P39.2 — distillation seam:** `everyaios-core::distill` (LLMLingua-2-pattern third compaction stage, `enabled(false)` default, `pruned_digest` + `has_gap`, 6/6 tests). **Office hold lift (partial — per-file tabs):** `office-file-switcher.tsx` → real per-file tab strip within each office view (open-close-switch, fallback to most recent), store `officeHistory`. **P10.4-5 cross-platform/CI:** browser-detect + portable-vault tests. **Verification:** workspace cargo ~447 core + full suite green (one known timing-flaky RSS test passes in isolation), desktop lib 14/14, UI tsc + vite build green. **(B) TODO.md recovery + reconciliation:** the live TODO.md working copy (1026/94, uncommitted) was truncated to zero bytes by a script — unrecoverable from git (HEAD has 841/279 from 2026-08-24). Restored from HEAD, then re-synced ~181 flips per this changelog's documented landings (verified by code search: P13 11/11 usage-parser/resolver/fingerprints/skills/symbol-graph/loop/saved, P14 4/5, P15 5/5, P16 8/8, P17 10/10, P18 5/5, P19 4/4, P20 2/2, P21 2/2, P22 3/3, P23 4/4, P24 3/3, P25 4/4, P26 3/3, P28 13/13, P29 11/11, P30 15/16, P32 8/8, P33 6/8, P35 4/4, P37 16/16, P38 7/7, P39 5/5, P40 3/3, P41 4/4, P10 50/50, P11.6 5/5). 170 `[x]`+`[NOT DONE]` contradictions fixed (annotation → `[DONE]`), 16 under-flipped rows the recovery missed flipped from git HEAD (P14.1-4, P18.1/2, P18.4/5, P22.1/3, P24.1-3, P25.2-4), P14.5 rightly left open (deferred), P33.5/P33.6 + P34.2-5/7 rightly left open (Office hold). SUMMARY regenerated entire from bullet counts. **Final live census: 1120 = 1042 done + 78 open** (verified zero contradictions). ARCH/09 I12 stale "P41.2-4 stay open" corrected. **Remaining open (78):** P5.2 FFI gated, P6.9 Signal deferred, P9.2-9 (20), P12 GTM (47), P14.5 sync loop, P30.16 pixel-pet, P33.5/6 + P34 (Office hold 7). (P7.8/P7.9 sandbox rows stay done — profiles/broker/seccomp DSL; kernel-apply is the documented F-residual seam.)

> **Current-state crosscheck — 2026-08-25 (F1 + F2 landed, amended):** the evaluation text was reconciled against the implementation and canonical architecture. **Fixed in the current contract:** Guard-2 Mermaid now says v1 **dedicated approval window** + nonce (main renderer cannot approve; native OS card remains open); memory language now uses the C-series/34-algorithm index and states composition order (retrieval/fusion → ranking → scope/budget → compaction; FSRS is a separate review scheduler); the control-plane/suite boundary is explicit (rich views must call the native ticketed engines; no second mutation engine/ledger; Office hold, I12 Code rail, E9 optional composition); H33 is explicitly per-node single-writer plus cross-node version-vector/tombstone reconciliation; ARCH/09 J3 no longer mislabels the Rust `NativeCard` data shape as an OS-native dialog. **F5 ref lifecycle is already fixed** (coordinator refs have TTL + capacity + sweep/clear; Rust refs are stateless content-addressed). **F1 — landed 2026-08-25:** consent now terminates in a dedicated `guard` webview window (`guard.html`, minimal dependency-free page; `guard_respond` rejects the main renderer by window label); the true OS-native card stays open. **F2 — landed 2026-08-25:** the equal-rev conflict rule is now a stated policy — `ConflictPolicy` (Manual | LastWriteWins | KeepBoth) + `resolve_conflicts` + `SyncSession::reconcile_with_policy` in `everyaios-core::sync`; state scopes converge LWW with a deterministic ts tie-break (`SyncItem::ts_ms`), effect-plane records (tickets/receipts/audit) stay `Manual` and are never auto-resolved. **Remaining valid architectural risks:** native OS consent surface (post-F1 hardening), F3 coordinator monolith/P29 migration, F4 probabilistic browser content-inflow scan (effects still ticket-gated), and the deferred P42/P6 release work. Historical entries below preserve their original dates and claims; TODO.md is authoritative for current counts/status.

> **Then 2026-08-25 (smoke-test pass on the new UX surfaces — 3 surfaces verified, 2 real bugs found + fixed):** the NPS timing, session-recording and feedback-disk-write surfaces were made testable and exercised. **(1) NPS timing — extracted to pure `lib/nps.ts`** (storage-injectable; `npsShouldPrompt`/`npsRecordScore`/`npsScores`; the component is now just the card) — 5 unit tests. **Two real bugs caught + fixed:** (a) with no prior prompt ever (`lastPrompt = 0`), the 90-day re-prompt window check blocked the *first* eligible prompt at day 7 — the window now only applies once a prompt has actually happened; (b) `Number(firstSeen) || now` treated the legitimately stored epoch-0 first-seen as falsy and reset the clock on every call, so the 7-day window never elapsed — now `Number.isFinite`. **(2) Session recording — storage-injectable `lib/session-recording.ts`** (`__setRecordingStorage` for tests; `elementIdentity` now duck-types instead of requiring the DOM `Element` global, so it runs in any runtime) — 7 unit tests (disabled no-op, content-free aria-label identity, tag fallback, label truncation, 2000-ring-buffer cap, clear + disable-clears, JSON export shape). **(3) Feedback disk write — `feedback_cmds.rs` refactored**: the append core is `write_feedback(base, …)` with validation moved in (single enforcement point — the tests caught that the command-level validation didn't exist on the core) — 2 unit tests over a temp dir (append + category line count, missing-title/body/kind rejected). **Verified:** bun `nps.test.ts` + `session-recording.test.ts` **12/12 pass** · desktop `cargo test --lib` **14/14** (was 12, +2) · UI `tsc --noEmit` clean (test files excluded from the app tsconfig via `exclude: ["src/**/*.test.ts"]` — bun runs them) · `vite build` green · desktop `cargo check` clean. Census unchanged: **1120 = 1020 done + 100 open**.

> **Then 2026-08-25 (UX polish — P11.6 5/5 + P35 3/3 + the P35.1 flagged gap closed):** the last open UI-polish registers were implemented, tested and counted. **P11.6 User Research & Feedback Loops (5/5):** (1) in-app beta feedback — `feedback-panel.tsx` (Settings → Feedback; bug/feature tabs, title/body/category, submit appends to `<data_dir>/feedback/feedback.md` via the new `feedback_submit` Tauri command, copy-to-clipboard; local-only by design, nothing leaves the device); (2) NPS/satisfaction prompt — `nps-prompt.tsx` (dismissible 0–10 card + optional comment, local-only, prompted after 7 days of use, at most once per 90-day window); (3) user-testing plan — `UX-TESTING-PLAN.md` (5 testers × 3 rounds across alpha/beta/RC, persona mix, a common scripted skeleton, measured metrics tied to P11.6.4, go/no-go gates); (4) key UX metrics — `ux-metrics.ts` (local counters: sessions created, turns completed/failed, time-to-first-tool-result = time-to-value, approvals granted/rejected) wired into `store.ts` (`newSession`/`streamFinalize`/`streamFail`/`streamToolResult`) and the guard window, surfaced in Settings → UX metrics with completion/error rates + reset; (5) opt-in session recording — `session-recording.ts` (off by default; content-free click/navigation ring buffer capped at 2000; JSON export/clear; delegated document listener in App.tsx; element identity from aria-label/testid/view/tag only — never message content). **P35 Full Animation Wiring (3/3 + gap):** P35.2 entrance stagger — `lib/stagger.ts` (`staggerStyle(i)`, 28ms step, backwards fill) + `enter-stagger` CSS utility applied to the sessions list (left-sidebar), connectors list, memory entries, guard ticket rows and artifact cards; P35.3 press/hover audit — global CSS (every `button`/`[role=button]` gets a 150ms press transition + `active:scale(0.98)` + a visible `:focus-visible` ring; framer-motion components keep their own inline transitions); P35.4 generative-UI/widget animations — `widget-enter` (300ms crossfade) on `DescriptorRenderer` + generative `ArtifactCard`, `enter-surface` on the generative-view sections; **P35.1's flagged gap closed** — `spark-draw` finally has its Automations consumer: a 7-run success-trend SVG sparkline in the History tab header. **Verified:** UI `tsc --noEmit` clean · `vite build` green (guard.html still emitted, guard-main.ts now also bundles ux-metrics) · desktop `cargo check` clean (new `feedback_cmds` module; no unused imports). TODO census: **1120 = 1020 done + 100 open** (was 1012 + 108 — P11.6 ×5 + P35 ×3 flipped; P11.5 SUMMARY + P35 SUMMARY + header + TOTAL updated in lockstep).

> **Then 2026-08-25 (F1 — consent leaves the main renderer; F2 — the two-writer conflict rule is now a policy):** the two structural flaws from the W1–W6 / fault-line audit that were "documented registers" got real implementations. **F1 — dedicated Guard-2 approval window:** the approval decision used to terminate in the main webview — the same renderer that displays browser views, generative-UI iframes and plugin views, so a compromised main renderer could draw a fake card over a real ticket (the nonce stops forgery, not deception). Now: a dedicated always-on-top `guard` window (`src-tauri/src/guard_window.rs` — `ensure_guard_window`/`open_guard_window`, loads only the bundled `guard.html`, never remote/untrusted content) whose page (`ui/guard.html` + `ui/src/guard-main.ts`) is a minimal dependency-free surface that polls `guard_tickets` and renders the full decision package straight from Rust IPC. **Enforcement:** `guard_respond` now takes the injected `window` and refuses any caller whose label is not `guard` — the main renderer physically cannot approve or reject. `guard_open_window` (new command) is what the main UI calls: the transcript MCQ permission cards, the Guard panel, and the Excel ask-verdict cards now open the window and wait for the human there (Excel polls until the ticket is consumed; a rejected ticket then fails the commit honestly with a revoked-ticket error). Vite is now multi-page (`rollupOptions.input` main + guard) so the build emits `guard.html`; the `guard` capability grants the window `core:default`. Honest ceiling unchanged: it is a dedicated webview, not an OS-native dialog — that remains the open hardening item. **F2 — cross-node conflict semantics:** `everyaios-core::sync` now answers "whose write wins" instead of leaving it to the caller: `ConflictPolicy` (`Manual` · `LastWriteWins` · `KeepBoth`), `resolve_conflicts(diff, policy, local, remote) → ResolvedDiff`, `SyncSession::reconcile_with_policy`, and `ConflictPolicy::for_scope`. State-plane scopes (messages/memory/connector) converge Last-Write-Wins using the new `SyncItem::ts_ms` (`#[serde(default)]` — old bundles stay readable, untimestamped items lose to timestamped) with a **deterministic symmetric tie-break** (equal ts → payload byte-order) so both nodes compute the same winner in one round; effect-plane records (tickets, receipts, audit) are never replicated through `SyncSet` today and the policy makes explicit that if a future scope ever syncs effect-like records it must stay `Manual` — "whose ticket wins" / "what a receipt means after reconcile" is a governance decision, never a merge rule. 6 new tests (manual-never-resolves, LWW-by-ts, symmetric tie-break convergence, keep-both suffix, session-with-policy, untimestamped-loses). **Verified:** `everyaios-core` lib **437 pass / 0 fail** (was 431, +6); desktop `cargo check` clean (only the pre-existing core warning); UI `tsc --noEmit` clean and `vite build` emits `dist/guard.html` + a ~few-kB `guard-*.js` chunk (the env's broken node_modules was restored via `bun install` — the coordinator's 7 test fails are the pre-existing `ENOENT` into the unrelated `/home/sarvesh/business_Dev/APP` tree, untouched this round). TODO census unchanged: **1120 = 1012 done + 108 open** (no checkbox flips).

> **Then 2026-08-25 (closing the fault-line leftovers — retention sweep wired + durable observation feed landed):** the two "small wiring" items registered by the fault-line pass are now fixed, plus a pre-existing build error. **(1) Ledger-growth sweep now enforced, not just callable:** `src-tauri/src/maintenance_cmds.rs` — `audit_compact` Tauri command (manual/scheduler trigger) **and** `run_audit_sweep_if_due`, invoked from Tauri `.setup`: the daily marker-gated sweep (`SWEEP_INTERVAL_MS` 24h, `.audit_last_compact` marker) compacts `audit.ndjson` with `everyaios-audit::retention::compact` in a genuine writer-quiescent window — lock the `audit_log` mutex, drop the writer (fd closed), rewrite, re-open (seq resumes from the last event; the `log.rollup` header at seq 0 is skipped). Non-fatal on failure. **(2) ARCH/05 durable-observation seam closed:** vault `recent_usage(limit)` (`RecentUsage` row: ts_ms/provider/model/tokens/cache/cost, camelCase wire, limit-clamped 1..=500, tested — `recent_usage_feeds_the_coordinator_observation_ring`); the core `ChatRelay` dispatches a new `usage/recent` request (indexed vault query on the consumer loop); the coordinator hydrates its RouteDecision ring once at boot (`index.ts` — `sendRequest("usage/recent", {limit: 200})` best-effort, empty ring → honest cost-sort fallback) via `hydrateObservations()` (observations.ts — durable rows are successes with measured total-$ converted to per-1K cost; latency unknown 0; **live this-process keys are never overwritten** — marked in `recordObservation`; per-key cap MAX_PER_KEY; `resetObservations` clears the live-key set). Tests: **observations.test.ts 4/4** (hydrate, live-wins, cap, malformed-skip) + vault lib test. **(3) Fixed a pre-existing desktop build error:** `office_cmds::pdf_page_op` mixed `PageOpError` (pages.rs) and `PdfError` (form/redact/annot) in one match — every arm now maps to `String`; also removed two unused imports. **Verified:** `everyaios-vault` **105 pass / 1 ignored** (was 104), `everyaios-audit` 51 pass, `everyaios-core` lib 431 pass, `cargo check` desktop clean (only the pre-existing core warning), coordinator **17 tests (budget 13 + observations 4) 0 fail**, tsc clean. TODO census unchanged: **1120 = 1012 done + 108 open** (the two 2026-08-25 top-entry reveals still stand; this entry amends them with the live call sites).

> **Then 2026-08-25 (fault-line + W1-W6 architecture audit — two real gaps fixed, rest verified as documented registers):** the Cowork head-to-head, the six fault lines, and W1–W6 were checked against the tree; each is either already mitigated in code, honestly documented as a deferred register, or was a real gap — and the real gaps were fixed. **(W5 — ref lifecycle, fixed):** the sidecar `RefRegistry` (budget.ts) was an unbounded one-shot store: a handle the peer never resolved (abort/error/compaction) leaked its payload forever. Now **leased**: `DEFAULT_REF_TTL_MS` 10-min expiry checked lazily on `put`/`take`/`size` (deterministic `now()` injectable for tests), `MAX_REF_HANDLES` 64 capacity with oldest-inserted eviction, `sweep()` + teardown `clear()`; `take` still burns one-shot. Per-turn abort revocation is *not* done (refs resolve synchronously within their turn; a global clear would invalidate concurrent streams) — the TTL+cap closes the leak without cross-stream interference. 4 new lifecycle tests (expiry, per-handle TTL, cap-eviction, clear). *Note: the Rust `HandleStore` counterpart is the stateless content-addressed `everyaios-memory::reference` (no registry ⇒ no leak class there).* **"Ledger growth" fault line — fixed:** ARCH/06 §6.7 promised "audit configurable" retention with no mechanism; `everyaios-audit::retention::compact` now ships the mechanism — full payloads kept for N days (default 7), older events re-emitted as `{rolled_up: true, payload_sha256}` digests (seq/ts/kind/trace fields preserved, J19 Merkle verifiability intact), `log.rollup` header at seq 0 so `AuditWriter::open` resumes correctly, atomic tmp+rename, no-op on missing file, 1 test (digested-not-deleted + resume-at-8). Single-writer caveat documented (must not race a live writer). CLI/scheduler wiring of the sweep is a small remaining Tauri maintenance command (open). **W3 — regime note added (doc):** ARCH/06 §6.11 "Event types (single writer)" now reads "per node" + a boundary note that cross-node causal ordering is the version-vector/tombstone reconcile regime (`everyaios-core::sync` P8.9 / P40.2 node attach), never an extension of the single-writer primitives. **Verified as already-registered (no code change):** W1 native-OS Guard-2 card is deferred + now labeled "v1: webview + nonce" in the guard panel (P5-24); W2 ACP v2 fs/terminal surface risk is mitigated by Channel B + sandbox + honest badge discipline and documented as a protocol-maintenance tax; W4 sidecar monolith (7 responsibilities in coordinator) is transitional with the P29 exit plan already queued; W6 browser inflow is the spec's own stated asymmetry (probabilistic content-in el, deterministic downstream); browser ceiling (stock-CDP ~17/37, Chrome 136 default-profile refusal) is in the tiered-engine design docs; approval-vs-autonomy (R1) is the spec's own top risk (Trust Ladder + min_confidence_for_auto + learning from approvals, Guard-2 never auto). **Verified**: Rust workspace `cargo test --workspace` aggregate **1917 passed / 0 failed** (was 1916; +1 = the new compaction test; the 18-ignored nightly-gated tests unchanged — only `everyaios-audit` gained a test, `#[test]` not ignored); `cargo check -p everyaios-core -p everyaios-desktop` clean (1 pre-existing warning — module addition only, no API change); coordinator `tsc --noEmit` clean on budget.ts; **budget.test.ts 13/13 pass** (bun, +4 new lifecycle tests). TODO census unchanged: **1120 = 1012 done + 108 open** — TODO register note appended (ledger-retention + ref-lifecycle + W3 regime + verified-registered list).

> **Then 2026-08-25 (closing the "honest partials" — live memory graph/episodes + pdf.js find + tightened LO fallback):** the three flagged partials from the recheck were upgraded to live endpoints. **(1) P5-22 — real `memory/graph` + `memory/episodes`:** `GraphStore::snapshot()` (everyaios-memory) lists the real store's nodes/edges; `MemoryService::graph_snapshot`/`episodes` (everyaios-core) dispatch `memory/graph` (nodes + edges with confidence/source-span, zero-token) and `memory/episodes` (facts grouped by session, newest first) — both with Rust tests (`graph_snapshot_lists_the_real_store`, `episodes_group_facts_by_session`); the UI bridge (`memoryGraph`/`memoryEpisodes`) and memory-panel Graph/Episodic tabs now render the live store in the shell (demo fallback only in browser preview). Honest ceiling: MemoryService's episodic store is the facts table grouped by session (no separate episodic table), labeled as such. **(2) P2-12 — pdf.js text-layer find:** `pdf-canvas.tsx` rewritten to build a pdf.js text layer (textContentItems positioned over the rendered page) + `findText(query)` returning matched ranges; `office-pdf-view` search input now highlights real matches in the rendered document (was `texts.findIndex` over the text dump). **(3) P3-15 — LO fallback tightened:** the "engine refused — open in LibreOffice" button now shows only on genuine engine-refuse errors (parse/floor refusals — `isOfficeEngineRefuseError` in `office.ts`), not on every error banner. **Verified:** Rust workspace **1916 passed / 0 failed / 18 ignored** (was 1914; +2 new tests) · `cargo check -p everyaios-memory -p everyaios-core` clean (the one warning — `exec_with_refresh` never used — is pre-existing) · touched UI files syntax-clean under a scoped `tsc`. TODO census unchanged: **1120 = 1012 done + 108 open** (annotations updated in place, no checkbox flips).

> **Then 2026-08-25 (verified done/not-done list → implemented the not-done wiring):** the office/connectors/memory/guard list was checked line-by-line; the claimed-done rows (office tools on the loop + floor, G8 search, browser attach, chatCancel, artifact-open, docx blocks/tracks, Excel status bar, LO buttons, vault key writes) all verified true; then the not-done rows were implemented. **P0-5 — RouteDecision scorer wired (recheck-corrected same day):** the scorer is extracted to dependency-free `scorer.ts` (a faithful `Scorer::score` port — `scorerScore`/`routeDecision`, weights 0.20/0.10/0.15/0.12 + 0.08 cache bonus) and `router.ts` re-exports it (`RouterOptions.observations`). The **live loop is now real**: `observations.ts` keeps a 5-turn ring per provider:model and `chat.ts` records one `ProviderObservation` per completed/errored turn (ok/latency/cost-estimate — budget kills excluded) and feeds `currentObservations()` into `selectModelForTask`, so the next routing decision ranks by observed health/cost/latency. `preferPowerful` (planner tier) is honored on the scorer path: it health-gates candidates then picks the most capable; without observations (or with nothing healthy) the router honestly falls back to capability-filter + cost-sort. **8 pure scorer tests pass** (`scorer.test.ts` — exact-weights 0.5225 case included) + 3 integration tests (`router-scorer.test.ts`, CI — needs the APP catalog deps that are broken in this checkout). ARCH/03 · ARCH/05 · ARCH/09 no longer claim "router.ts is cost-sort only". **P2-12 — PDF surgical ops:** `pdf_page_op` gained `annotate` (text/highlight via `pdf::annot`), `redact` (`pdf::redact`) and `form_fill` (`pdf::form`) JSON-payload ops — the same engine the agent tools use — plus a thumbnails rail + Annotate/Redact/Fill-form buttons in `office-pdf-view`. **P1-9 — takeover locks:** the `running && !paused` read-only lock (was Word-only) now covers Excel/PPT/PDF (edit controls disable + banner). **P2-11 — PPT:** slide rail + live `pptx_notes` render when a deck is open; the guizang demo rail is preview-only. **P3-15 — honest LO fallback:** every office error banner offers "engine refused — open in LibreOffice" (keeps the failed path). **P1-7 — per-file access:** `officeHistory` (store) + `office-file-switcher.tsx` per-view dropdown — a second file of the same kind no longer silently replaces the first. **P5-22 — memory live:** `memoryFacts()` (`memory/status`) feeds the Knowledge/Episodic/Graph tabs (demo fallback in preview); **real bug fixed:** `SemanticTab` referenced the stale `FACTS` name (renamed `DEMO_FACTS`) — a live `tsc` break. **P5-23 — connectors live (recheck-corrected same day):** the Native tab renders real vault OAuth accounts (`oauth_accounts`) when the shell is up; **when the shell is up and the vault is empty it now shows an honest empty state — it no longer falls back to the mock `NATIVE_SAMPLES` "connected" rows** (that was the recheck's flagged bug). **P4-20 (honesty only):** P42 planned rows (Google Workspace · M365 Graph) shown as "not attached" — P42.1/42.2 remain unwired. **P5-24 — Guard-2 honesty:** panel labels the v1 approval surface "webview + nonce (no OS-native dialog)" and tags preview rows. Also fixed a pre-existing syntax error in `ui/src/lib/office.ts` (stray `}` that broke the whole UI build). **Verified:** Rust workspace **1914 passed / 0 failed / 18 ignored** · `cargo check -p everyaios-office -p everyaios-desktop` clean · coordinator `tsc` clean on `router.ts` (env stubs for missing bun/vite types) · UI files syntax-clean under a scoped `tsc` (the checkout's `node_modules` is broken — react/typescript packages are dead symlinks — so full UI typecheck remains env-blocked). TODO census unchanged: **1120 = 1012 done + 108 open** (no checkbox flips — the rows were `[DONE]`-with-caveats or frozen; annotations updated in place).

> **Then 2026-08-25 (claimed-vs-reality dashboard audit + Stop wiring fix):** a 10-row "claimed done vs reality" dashboard was checked line-by-line against the code. **All 10 claims verified true** — none are fabricated, though they split into (a) honest fail-closed seams with the engine crates landed but the agent-loop wiring not live, and (b) genuine wiring gaps. **(a) Honest, documented seams (crash-verified, not force-closed):** G8 search dispatches `"G8 search cascade is not built"` (`tools.rs:715`; `everyaios-search` crate landed, executor dispatch deferred); browser tools return `"browser session not attached"` and office tools `"office engine not attached on this path"` on the core agent loop (engines live in their own crates + Tauri command modules, not on the loop); kernel sandbox `SandboxProfile::apply()` still errors (documented explicit seam); two ACP "side-by-side" harnesses are the `mock-agent-permission` fixture — the real live-CLI spawn test stays `EVERYAIOS_LIVE_ACP`-gated; RouteDecision/OmniRoute: the `routing.rs` scorer is landed + tested but the shipping coordinator `router.ts` is filter + cost-sort (scorer not the live mechanism); the OS Guard-2 approval is a webview React card w/ `guardRespond`+nonce (no native OS dialog — the `diffcard.rs` is a Rust data-shape module). **(b) Genuine wiring gaps (fixed the highest-value one):** Pause/Stop was cosmetic — `chatCancel` was exported but never called. Fixed: `sendUserMessage` now captures and stores the per-session stream id (`liveStreamId`), and the chat-panel Pause button issues `chat_cancel` against the real Rust stream before flipping local state, so Rust emits the terminal event that ends the turn. Remaining documented gaps (BYOK Settings add/remove still toast-only `notify(...)` — no vault write; memory panel initializes from `mockMemory` and connectors rows from `mockConnectors`; these are already tracked as UI-wiring/gated work). Verification for this pass is verification, not new tests; **Rust workspace stays 1911 passed / 0 failed / 18 ignored**; `cargo check` on the Tauri app clean. TODO census unchanged: **1120 = 1012 done + 108 open** — the audit is the honest labeling of what "done" means per row, not a checkbox flip.

> **Then 2026-08-25 (adversarial bug-review pass — verified, fixed, or refuted each reported issue):** an independent crosscheck of the product/safety + security inventory was triaged against the code, and every genuine bug fixed with a regression test; three claims were refuted as already-correct/stale. **High product — fixed:** (1) chat `done` passed `fullText` (whole message) onto already-batched deltas → duplicated the reply (`streamFinalize` now *replaces*, never concats); (2) chat-events ignored `sessionId` → tokens landed on the wrong transcript after a mid-stream chat switch (stream state is now keyed per session and the bridge routes by `e.sessionId`); (3) demo `mockSessions` could be `session_put` into the real vault on first boot (persistence now gated on `sessionsHydrated`, set when the shell owns the list via `session_list`/`newSession`); (4) the older code view saved via unguarded `fs_write_file` → now goes through `fs_write_ticket`/`fs_write_commit` (same Guard-2 floor as Monaco); (6) pause/resume was cosmetic — resume only inserted `(resumed)…` locally and never continued the agent → resume now dispatchs a real turn via `sendUserMessage` (chat_stream) on the session bound to that agent; (**7**) `blast_radius` had the boolean `changed` shadow the `&str` param, so it filtered against the literal `"false"` and left the changed crate in its own radius → renamed the boolean and exclude the actual `changed` name (+ test). **High security — fixed:** (8) sandbox `apply()` was a kernel no-op, but it is a documented explicit seam with no hot path relying on it — ARCH/02's "workers apply their sandbox before serving" corrected to the honest "kernel-apply seam"; (9) `<user_document>` wrap was injectable via embedded `</user_document>` → delimiters are now neutralized to inert `[user_document]` brackets (+ test); (10) A2A + MCP loopback checks were `starts_with` prefixes (`http://localhost.evil.com` / `127.0.0.1.nip.io` passed) → both now parse the host and require a literal loopback (127.0.0.1 | localhost | ::1) (+ tests); (11) ACP installer skipped sha256 when the pin was empty and `zip::extract`/`tar::unpack` were not path-confined → empty sha fails closed *before* download; extraction is now confined by `safe_join` (reject `..`/absolute/drive-root) for both zip and tar (+ tests; the tar crate also independently rejects hostile members); (14) Tauri vault fail-open to in-memory was silent with a predictable key → the fallback now uses a random key **and** tags `boot_report` with `EPHEMERAL VAULT` so non-persistence is visibly surfaced; (15) Windows `default_data_dir` used only `HOME` → now resolves `USERPROFILE`/`HOMEDRIVE+HOMEPATH`, never the cwd; (16) the vault passphrase was copied into process env (readable via `/proc/<pid>/environ` + children) → carried thread-locally and cleared after the KDF; (17) the SQL stacked-statement detector refused any trailing `;` (`SELECT 1;`) and mishandled SQL `''` escaped quotes → a single trailing `;` is a legitimate terminator and the string-state machine honours `''` (+ tests). **Refuted (already correct in-tree):** (5) "ToolExecutor can skip Guard-2 on IPC errors" — the Rust `exec` enforces a consumed ticket at `commit` for non-read-only tools regardless of the fallible `guard/evaluate` call; (13) "vault-wrapped = base64 of the raw key" — sync bundles are sealed with ChaChaBox AEAD, no plaintext key on disk; and the coordinator `chief/resolve` badge claim — which was *true* (badge hardcoded to `not_governed` even when a governance mode was recorded) — **fixed** to return the actual `governanceBadge(governance)`. **Medium — fixed:** search URL builders (arXiv/GitHub/EDGAR/Reddit) now percent-encode the query (no param/shape injection, + test); the read→GenericWrite permission-matrix rendering is presentational (no read Operation variant; the real read path auto-Approves) — comment corrected. **Verification:** Rust workspace **1911 passed / 18 ignored / 0 failed** (was 1903) — +8 from the new regression tests; `cargo check` on the Tauri app clean. UI TypeScript could not be re-run here (`typescript`/`bun` missing in this checkout's `node_modules` — pre-existing environment limitation); the TSX edits were reviewed for type-consistency and import changes are minimal. TODO census unchanged: **1120 = 1012 done + 108 open**.

> **Then 2026-08-25 (P10.1–P10.3 E2E/QA + Security + Perf suites landed — 35 rows flipped):** the cross-cutting testing pillar went from 1/50 done to 36/50. **P10.1 (12/12)** — `everyaios-core/tests/p10_e2e.rs` (journey install→BYOK→chat→tool-call, memory persistence across restart, sub-agent planner→2 agents→merge, crystallization 4th-run zero-token, connector-hub Gmail read→respond, headless scheduled task, messaging-bridge stub, lazy-extension capability boundary) + `everyaios-browser/tests/p10_pipeline.rs` (navigate→snapshot→act→diff→assert) + `everyaios-office` `byte_stability_noop_save_and_roundtrip` (open→edit→save→reopen byte-stability) + `everyaios-acp/tests/p10_harness_drive.rs` with the new `mock-agent-permission` stdio fixture (spawn agent via ACP → request_permission → harness Allow → audit → stop; real CLIs stay credential-gated) + `everyaios-mcp/tests/p10_external_client.rs` (external client initialize→tools/list→tools/call snapshot over the real standalone server). **P10.2 (9/9 + 1)** — `everyaios-core/tests/p10_security.rs`: red-team corpus vs Guard-1, 50+ prompt-injection payloads, 10K path-traversal fuzz (0 escapes), symlink suite (chains/circular/TOCTOU), revoked-key suspension+failover, sidecar-crash no-orphans, kill-core children-die<5s (PDEATHSIG, Linux CI gate; non-Linux mechanism covered), malicious SKILL.md AST audit, over-capability plugin denial + policy delete-without-approval. **P10.3 (14/14)** — `everyaios-core/tests/p10_bench.rs`: cold-start boot path, idle + warm RSS published (J16: warm <80MB unachievable as-is), IPC round-trip, browser snapshot a11y tree, memory retrieval 10K facts, FTS5 100K chunks, compaction 200K tokens, 50 concurrent tool calls, 10-tab/3-agent ownership isolation, 100 scheduled tasks, plus 30-min heap / battery / 4-hr stability as nightly-gated (`EVERYAIOS_LONG_TESTS=1`). P10.4 cross-platform + P10.5 CI/CD remain open. **Verification:** Rust workspace **1901 passed / 18 ignored / 0 failed** (was 1868/15) · coordinator 266 passed · coordinator + UI TypeScript clean. Historical counts preserved below. **Follow-on doc-sync in the same pass:** the P15/P19/P20/P21/P23/P25/P26 checkbox rows were stale (`[NOT DONE]` while the SUMMARY table + this changelog's P15–P26 closure entry said done) — all 19 implementations verified present and tested (artifact server/views, gateway router, swarm, prompt-anatomy ref, ui-ux-pro-max skill, seek hybrid index, worktree orchestration, demo compiler, ShowUI ref, plugin manifest, marketplace+2 new tests, workflow DAG, graphify, MCP containment, gws connector, jobs vertical) and the rows flipped; final live census is **1120 = 1012 done + 108 open** (was 958/162 before this pass).

> **Then 2026-08-25 (P15–P26 batch-queue closure pass):** completed and tested the remaining local adapters and contracts: H29 dashboard artifacts (`everyaios-script::artifact` + UI artifact view/checklist), Kilo gateway routing, ruflo-style blueprint swarm, optional SeekStorm hybrid index, Superset-style worktree review, OpenAdapt demonstration compile/replay/repair, Claude plugin manifest and marketplace model, inbuilt native skill packs, repository `DESIGN.md`, browser-harness self-healing helper store, minimal-code prompt doctrine, Airflow-style workflow states/retry/backfill, Graphify repository knowledge projection, MCP tool-hijack validation, official `gws` managed-child read-first connector seam, and the Guard-2 Jobs vertical. The Jobs approval contract was corrected to require a separately recorded approval and now hashes payload bytes, not only lengths. **Verification:** at the time of this entry, workspace tests 1850 passed / 0 failed (ignored live/account tests remain explicitly gated); coordinator 266 passed; UI TypeScript clean. Current audit snapshot (2026-08-25): workspace 1868 passed / 15 ignored / 0 failed; coordinator 266 passed / 0 failed; coordinator and UI TypeScript clean. `cargo clippy --workspace --all-targets -D warnings` still reports pre-existing strict-lint debt outside these changes and is not claimed clean. New external binaries/accounts remain runtime-gated; no third-party code was copied.

> **Then 2026-08-24 (closing the three v3.55 wiring seams — USN raw reader + agent-registry Tauri commands + bundleToToml schema fix):** the last documented "seam, not built" items from the P36/P31 landing got closed. **(1) G7 USN raw WinAPI read** — `everyaios-storage::usn_reader` (pure parser for the `USN_RECORD_V2` byte layout: 48-byte `READ_USN_JOURNAL_DATA_V0` header cursor, record-length walk, truncation-stop, reason-bit map with CLOSE-never-wins priority, UTF-16-LE names; 9 cross-platform tests incl. truncated-buffer, unknown-major-skip, path assembly) + `everyaios-storage::usn_winapi` (`cfg(windows)`: `CreateFileW` with backup semantics → `FSCTL_QUERY_USN_JOURNAL` → `FSCTL_READ_USN_JOURNAL` (per-record USN → the existing `UsnCursor` gap-abort contract) → `FSCTL_ENUM_USN_DATA` FRN→path resolution with a cached, incrementally-updated `FrnIndex`; every windows-sys 0.61.2 name verified line-by-line against the vendored bindings — DeviceIoControl lives in `Win32::System::IO`, the FSCTL_* constants + `*_DATA_V0` structs in `Win32::System::Ioctl` (own feature), PascalCase fields, `CreateFileW` gated behind `Win32_Security` — and the whole reader compile-checked against `x86_64-pc-windows-msvc` via a standalone windows-sys-only crate (the workspace C deps need the MSVC toolchain). Honest: compile-verified only; a live NTFS journal run is Windows-CI work. **(2) Agent-registry Tauri command** — `src-tauri/src/agent_cmds.rs` (`agent_registry_list/save/get/remove/duplicate/set_disabled` over `everyaios-agents::AgentRegistry`; ids re-derived via the crate `slug`, invalid TOML fail-closed) registered in the invoke handler; `ui/src/lib/agent-registry.ts` bridge (demo localStorage fallback outside the webview) + the builder panel now reads/writes the real registry (live list, save/remove, error surface; demo mirror only in browser preview). **(3) bundleToToml schema-drift bug (real, found by the round-trip probe)** — the TS exporter emitted `[engine]`/`[model]` mid-file; in TOML a table header absorbs every key after it, so `mcp_servers/connectors/skills/blueprints/automations` silently landed inside the `[model]` table and `AgentBundle::from_toml` dropped them (parse succeeded, fields empty). Fixed: arrays now precede all table headers — byte-identical in spirit to the Rust `toml::to_string` layout — proven by a serde probe parsing the ACTUAL bun-emitted bytes (engine Acp + model pin + mcp + tools all preserved) + 5 coordinator regression tests locking the layout. **Verified:** workspace cargo **1808 passed / 0 failed** (was 1597, +211 across the parallel P28/P29/P37/P16/P30/P32/P33/P38–P41 landings; this pass +8 storage) · coordinator **266 tests** (was 261, +5) + tsc clean · UI tsc clean · clippy 0 on changed crates · P38 Dynamic Chief re-verified 7/7 end-to-end (acp-10 + coordinator chief-10 tests, config/Tauri/UI wired — zero open). The historical count at that point was **1120 = 952 done + 168 open**; current audit snapshot (2026-08-25) is **1120 = 958 done + 162 open**. (no checkbox flips — the seams were already `[x]` with gated annotations; annotations updated in place).

> **Then 2026-08-24 (P36 Kernel Core + P31 Custom Agent Builder 9/9 landed):** the last two big engine/UI queues closed. **P36 v3.39 named types/fields** — all 21 `[NOT DONE]` items landed as tested modules: `everyaios-core::resources` (`ManagedResource` lifecycle state machine: discover→validate→install→enable→start→health→observe→remove, install ≠ enable ≠ running ≠ healthy, illegal-transition + terminal-removed guards, `ResourceManager` registry with audit sink; not Office/providers/deployments), `everyaios-core::routing` (`RouteDecision` + `ProviderObservation` — cached-token-aware billable tokens, cost/latency/health scorer with ARCH/03 weights, honest capability-filter-cost-sort), `everyaios-core::hooks` (I6 executor hooks — PreToolUse deny-only, Allow degrades to Record so a ticket is never skipped, post-hooks record-only, capability-scoped, denials audited), `everyaios-core::git-commit` (I8/K2 atomic commit — verification gate before stage/commit, literal pathspecs, nothing-else-staged isolation, empty-diff refusal), `everyaios-memory::graph` +Edge `confidence` (EXTRACTED/INFERRED) + `SourceSpan` (file/line) + `add_edge_with_evidence`, `everyaios-memory::branch` (C2/C7 lineage tree: fork at turn, recall branch = ancestry-only, siblings list, closed-branch refusal), `everyaios-memory::maintain` (C `maintain()`-class: analyze references/orphans/near-dupes, update graph from shared terms, ACT-R decay sweep with importance-≥8 floor), `everyaios-memory::abort` (C3 abortable retrieval: CancellationToken + fuse that cancels between signals), `everyaios-blueprint::subagent` (+derive_child_permissions parent∩deny∩explicit with task/todo default-deny, TerminationReason/Event, ScoutSpec read-only clone-into-cache), `everyaios-blueprint::helpers` (E14 persistent learned helpers under agent-workspace/helpers — versioned, run-counting, survives reinstantiation), `everyaios-browser::acquisition` (E10 engine-picks acquisition: intent→HTTP/Light/CDP with escalation, stealth contractually gated, cloud never bundled), `everyaios-browser::diagnostics` (E2 console/network/perf collectors over CDP events), `everyaios-browser::har` (E5 optional HAR 1.2 beside NDJSON, sticky `has_gap`), `everyaios-mcp::record` (MCPServerRecord: id/registry/version/transport/provenance/digest/capabilities/trust/per-consumer enable/health/config_hash — running Agent X never loads Agent Y's servers) + `everyaios-mcp::protocol` (resources = bounded C10, elicitation `InputRequired` → Guard-2 shapes, sampling broker-only contract, roots path-floor + traversal rejection), `everyaios-office::provenance` (DocumentAsset: source_uri/media_type/converter/versions/source+extracted hashes, ingest≠mutate≠render) + `everyaios-office::pdf::storage` (D4 annotationStorage persist: storage map → AcroForm /V, never invents fields), `everyaios-storage::hash_cache` (D10 fclones `--cache` pattern: persishable hash cache + stale cleanup), `everyaios-storage::trigram` (G7 FTS5 trigram filename index + <3-char LIKE fallback), `everyaios-storage::usn` (G7 USN journal typed contract + monotonic cursor + gap-abort + cross-platform debounced fallback; raw Windows journal read stays platform-gated). **P31 Custom Agent Builder (9/9)** — new `everyaios-agents` crate: `AgentBundle` (agent.toml, schema v1, I6-compatible: persona/system-prompt, EngineBinding Inbuilt|Acp|ModelOnly, ModelPin inherited-or-pinned, ToolScope allow/deny, mcp/connectors/skills/blueprints/automations), `AgentRegistry` (~/.everyaios/agents/, list/load/save/duplicate/disable/export), `AgentTemplates` (the 8 wizard templates), `AgentScopes` (exact-subset MCP/connector/skill + capability-grant tool computation), `AgentRuns` (P31.9 agent-owned workflow runs). UI: `ui/src/lib/agent-builder.ts` (template catalog + TOML export) + `agent-builder-panel.tsx` (4-step wizard Identity→Brain→Capabilities→Workflows, registry list/duplicate/remove, export/copy agent.toml, localStorage mirror until the Tauri command drop) wired into the left rail (Agents nav) + center-column. **Verified:** workspace cargo **1597 passed / 0 failed** (was 1510, +87) · coordinator 158 tests + tsc clean · UI tsc clean + vite build green · clippy clean on all new modules. Counts: **1120 = 841 done + 279 open** (P36 v3.39 12/12 + v3.40 10/10 summary rows; P31 10/10). Remaining open: QA/testing, GTM research, post-v1 pillars (voice/images/WASM/always-on node), steal queues, P11.6 user research, P41.2–4, P43 ledger, P38 chief (7).

> **Then 2026-08-24 (doc/codebase re-verification — count correction + P11.5 split):** full re-check of TODO census, SUMMARY table, README, ARCH/09 row count, and spec references against the codebase. **Found + fixed:** (1) the census was inflated by +2 — the SUMMARY counting-rule note (line 1742) contains literal `[x]`/`[ ]` inside backticks, so naive greps counted 1122 = 812 + 310; the strict bullet census is **1120 = 811 done + 309 open** — TODO header + SUMMARY TOTAL row corrected to match. (2) **P11.5 SUMMARY row drifted** — it claimed 75/75/0, but the P11.5 section (incl. the P11.6 User Research & Feedback Loops subsection: beta feedback, NPS, user-testing plan, UX metrics, opt-in session recording) is 70 done + 5 open; row corrected to 75/70/5 with the P11.6 items named. (3) **README TODO.md row was badly stale** (1120 = 722 + 398, P5 = 68 + 4) → corrected to 1120 = 811 + 309, P5 = 71 + 1 (LadybugDB FFI the only P5 open). (4) ARCH/09 row count re-verified: 151 unique IDs, no duplicates — the spec's "151 rows" claim holds. No code changes; docs-only reconciliation.

> **Then 2026-08-23 (six gated P11.5 follow-ons closed — the "documented, none claimed as built" list is now built):** every follow-on flagged in the P11.5 rest-landing note is implemented + tested. **(1) Warp merkle embedding sync** — `everyaios-codeintel::warp`: merkle content-hash incremental sync (`WarpIndex` file→chunk-hash state), per-chunk re-embed only on change (top-level hash fast path), chunking modes (lines/bytes), injectable `Embedder` seam (shape-identical to `everyaios-memory`), 4 tests. **(2) notify-crate watcher glue** — `everyaios-core::watcher_glue`: the storage `watch_events` (notify-kind classified `FileEvent` batches) → `memory/ghost` `FsEvent` hop (delete→tombstone, rename→re-path, modify→no-op) + `WatcherBridge` (affected-ref count) + ai!-marker auto-submit path filter + `start_watcher` convenience; 5 tests. **(3) Heavy-graph SCIP backend** — `everyaios-codeintel::scip_watch`: dir scan for `*.scip`, content-hash keyed incremental re-ingest into the merged `SemanticIndex`, drop-on-delete, recursive mode, `symbol_heat`; 2 tests. **(4) Live ACP CLI spawn** — `everyaios-acp::bin::mock_acp_agent` (a real ACP-speaking process) + `tests/live_spawn.rs` E2E: spawn → initialize → session/new → prompt (echo update) → shutdown, CI-green + an `#[ignore]` env-gated live test against the real `@agentclientprotocol/claude-agent-acp` CLI (initialize needs no subscription). **(5) AG-UI live transport** — Rust twin `everyaios-core::agui` (`AguiEnvelope` codec matching `agui.ts` byte-for-byte + `AguiRelay` sink) wired into `ChatRelay` (`agui/event` notification → `agui-event` Tauri emit; `send_agui` UI→coordinator), Tauri `agui_send`/`agui_listen` commands, coordinator `notifyAgui`/`onAguiEvent`/`dispatchAguiLine` + `agui/event` request case, chat.ts emits `tool_call_created`/`tool_call_result`/`artifact_created`/`done` envelopes during turns; 5 Rust + 3 TS tests. **(6) RepoMap SQLite cache** — `everyaios-codeintel::repo_cache` (`RepoMapCache`, rusqlite, content-hash keyed, TTL prune, 7 tests). **Verified:** workspace cargo **1510 passed / 0 failed** (was 1490, +20) · coordinator **158 tests** (was 154, +4) + tsc clean · UI tsc clean + vite build green (Monaco chunk intact) · clippy 0 on all new code (pre-existing email/guard/blueprint/gmail/connector_hub/acp/xlsx warnings only). All six TODO rows updated in place (follow-on annotations → closed). Counts unchanged: **1122 = 812 done + 310 open**.

> **Then 2026-08-23 (P11.5 rest landed — 29/29, P11.5 now 75/75):** the remaining UI-implementation items went from open to done with live backends + tested coordinator libraries. **P11.5.7 Guard panel (2)** — `guard_activity` (J5 NDJSON session log, receipts fallback) + `guard_permissions_matrix` (live `PermissionsPolicy` 5×5); guard-panel ACTIONS/MATRIX no longer hardcoded. **P11.5.8 Connector hub (1)** — `mcp_servers` (built-in catalog + user-attached) + `mcp_attach` (spawn stdio server, reconcile tools, native-wins); attach form in the panel. **P11.5.9 Aider (8)** — `repomap_build` (dir → tags + PageRank, SQLite cache follow-on), `file_outline` (DeepWiki over SCIP/semantic; Warp merkle sync stays C5-gated), coordinator `edit-strategies.ts` (SEARCH/REPLACE fuzzy: whitespace-flex + ellipsis + exact-preferred, + ApplyPatch as 4th format), `everyaios-core::ai_marker` (`// ai!`/`#`/`--`/`;`/`<!-- -->` scan + `ai_markers_scan`; notify glue = storage→core bridge follow-on), `reflection.ts` (lint → retry ×3 escalating), `Config.model_aliases` + `resolve_model_alias` + command, SCIP/crux library half (heavy-graph backend documented gated). **P11.5.10 Agent patterns (10)** — coordinator `decideMode` (Plan/Act, Cline gate), `context-providers.ts` (@Codebase/@Docs/@URL), `architectSplit` (two-pass I9), `oracleChecks` (reviewer gate), `classifyIntent` (Agent/Edit/Ask/Terminal/Build), `detectPrematureStop` (autopilot), `parseApplyPatch`, `composePrompt` (Prompt TSX + budget), `applyDistro`/`findDistro` (custom distribution), `acpAuthMode` (doc-57 seam: drive CLIs with user login, harvest blocked) + `kanban-view.tsx` (worktree-per-branch, wired to rail). **P11.5.11 Generative UI (5)** — `agui.ts` codec (16-event envelope over framed IPC; transport follow-on), `SandboxedArtifact` (strict CSP iframe, no scripts/network), `DescriptorRenderer` (JSON→local UI), `ArtifactCard` (make-live + version selector), `MermaidBlock` (lazy mermaid, strict) — surfaced in `generative-view.tsx` (rail). **P11.5.12 Resumable streams (3)** — `resumable.ts` StreamRegistry (last-token/id, resume cursor, stale sweep), `classifyIdempotency`/`canAutoRetry` (ARCH/03 classes), 🔄 Reconnecting chip (store + chat-panel + sidebar). **Verified:** coordinator 154 tests (was 110, +44) · workspace cargo 1490 passed / 0 failed (was 1483, +7 ai_marker/config) · UI tsc clean + vite build green (mermaid added) · clippy clean on all new code (pre-existing acp/xlsx/chat_stream warnings only). Honest ceilings noted inline: Warp merkle embedding sync, notify-crate watcher glue, heavy-graph SCIP backend, live ACP CLI spawn, AG-UI live transport, RepoMap SQLite cache — all documented gated follow-ons. Counts: **1122 = 812 done + 310 open** (P11.5 75/75/0; header + TOTAL in lockstep). No spec/ARCH row changes.

> **Then 2026-08-23 (P41.1 editor-core landed — Monaco embed + VS Code-style workbench):** research pass over the requested repos (Zed = GPL-3.0 core, pattern-only; Lapce/floem-editor-core = Apache-2.0 Rust editor machinery, separable crates; terax-ai = Apache-2.0 Tauri2+Rust+React workbench reference, exactly our stack; IntelliJ community = Apache-2.0 open core, Fleet/AI-Assistant proprietary; VS Code = MIT) → **DECISION: Monaco embed** (the actual MIT editor component VS Code ships) inside the existing React/Tauri shell, Rust machinery as the backend. Landed: `ide-workbench.tsx` (VS Code-style workbench: activity bar · Explorer over the real FS · SCM over real git · Problems over real LSP diagnostics · editor tabs · bottom panel · status bar) + `monaco-pane.tsx` (offline `?worker` bundling — all 5 workers emitted, own 3.9MB cacheable chunk; `NODE_OPTIONS=--max-old-space-size=4096` persisted in the build script via cross-env — the initial OOM was a shell-scoping bug, `VAR=x a && b` only applied the bump to `a`) + `git_cmds.rs` + `lsp_cmds.rs` (Tauri) consuming `everyaios-codeintel::lsp_runner::LspRunner` (real `publishDiagnostics` → Problems panel). Two real bugs fixed: Monaco ≥0.52's exports map (`"./*": "./esm/vs/*.js"`) means `esm/vs/`-prefixed worker imports double-prefix and fail to resolve — correct subpaths are `monaco-editor/editor/editor.worker?worker` etc.; `cross-env` was added for the heap bump (was not installed). License record: all-MIT/Apache-2.0, zero GPL code imported; floem-editor-core/gpui documented as the future native-path reserves (Apache-2.0) if webview editor perf ever demands it. P41.2 worktree-first parallelism / P41.3 ticketed writes / P41.4 receipts-in-editor stay open. **Doc reconciliation pass (same session):** the CodeMirror-6 lock was superseded in all five homes — DESKTOP-APP-SPEC.md (I12 row + §Workspace-tabs Editor bullet), ARCH/09 I12 row (Monaco landed note + floem-editor-core added beside gpui as a native-core reserve), ARCH/12 §4.5 Code View (workbench description), ARCH/10 v3.40 note, and TODO.md's "Code view = CodeMirror 6" checkbox (flipped to done-as-superseded). Counts: **1122 = 783 done + 339 open** (P41 4/1/3; header + TOTAL in lockstep). No ARCH/09 row-count change (151 rows).

> **Then 2026-08-23 (P11 UI push landed — 41/41: P11.1–P11.4 24/24 + P11.5 17/17):** the entire UI/UX spec block (P11.1 design system → P11.5.6 memory browser) went from open to implemented, with four new Rust command modules + the views wired over real backends. **P11.1** — `ui/DESIGN-SYSTEM.md` (design-file surrogate: tokens/components/layouts/states keyed to code; a real Figma artifact stays a design-tool deliverable). **P11.2 flows (10)** — `onboarding-modal.tsx` (4-step, non-dismissible, gated), shared `empty-state`/`error-state` (5 typed kinds + `classifyError`)/`loading-state` (ttft/compaction/tool/agent) components, multi-agent cockpit view (parallel agent cards + wired `interrupt_respond`), blueprint live status (renders real `pendingPlan`), office track-changes toggle (Word-style modified badges), cockpit quiet↔expanded framer-motion transitions, MCQ urgency badges. **P11.3 A11y/i18n (6)** — WCAG focus-visible + high-contrast mode + font scaling (`html.font-scale-*`) + `lib/i18n.ts` (en/ar/he dicts, RTL dir binding, Settings switcher). **P11.4 perf UX (7)** — `lib/ux.ts` (`useDebouncedValue`/`useVirtualList`/`useElementSize`), skeletons on async views, virtualized session timeline, debounced memory+audit search, `lib/perf.ts` LCP/TTI measurement surfaced in the status bar. **P11.5 real views (17)** — `fs_cmds.rs` (real `fs_list_dir`/`fs_read_file`/`fs_write_file`/`fs_undo_list`) → folder view over the live disk + code editor (save + Open-in-Cursor + selection→Edit-with-AI) + diff view over real agent undo snapshots; `shell_cmds.rs` (real `sh -i`/`cmd` piped process, `shell-event` streaming) → live shell view; `browser_cmds.rs` (real headless Chrome via everyaios-cdp: start/navigate/snapshot/read/click/type/stop) → browse view over CDP; `memory_cmds.rs` (MemoryService `memory/*` passthrough) → memory browser over live RPC with folder org + bulk enable; per-session layout persistence (`saveSessionLayout`/`restoreSessionLayout`); first-run welcome; child-session sidebar indentation; takeover/resume (per-agent pause, ● Live/⏸ Paused status pill, mandatory describe-changes resume); automation template→real-job + deterministic NL→trigger parser (11 templates). **Verified:** `tsc --noEmit` clean + `vite build` green (manualChunks intact); `cargo check` + `cargo clippy` clean on all four new cmds + lib.rs wiring (only pre-existing core warning remains). Honest ceilings noted inline: no real PTY (piped stdio), no LSP in the code editor, diff = undo-snapshot vs live file (not unified patch), memory store is the in-process service. Counts: **1122 = 781 done + 341 open** (P11 31/31/0 · P11.5 75/41/34; header + TOTAL in lockstep). No spec/ARCH row changes.

> **Then 2026-08-23 (P9.1 Desktop Computer-Use landed 8/8 — the last required gap, E9):** new workspace crate **`everyaios-desktop`** implements the full ChatGPT/Claude computer-use contract for native windows. **See** — `SeeMethod` (WGC occluded-window seam + `PrintWindow` + screen-DC on Windows; `GetImage` on X11; `screencapture -l` on macOS) + `Region` zoom (`is_full`/`clamp_to`); **Read** — `WinUia` UIA tree with indexes + click-by-name, X11 EWMH `_NET_CLIENT_LIST` **+ WM-independent XQueryTree fallback** (headless sessions), DPI scale, `list_windows`/`launch_app`/`activate_window`; **Act** — UIA Invoke/SetValue **first**, SendInput fallback (Windows), XTEST click/type/scroll/drag (Linux), osascript (macOS); **Vision fallback** — `TesseractCli` word boxes (TSV) + `locate_phrase`/`VisionHit` click-point math, `NotFound` never guesses; **Verify** — `Verifier::satisfied`/`verify` cascade with `Locator::{UiName,UiGone,OcrText}` → `Confirm`/`ConfirmedAfterRetry`/`Halt` (halt-over-guess); **Layer-1 first** — `router::route_action` (API > CDP-browser > desktop-GUI); **Guard-2** — `policy::PolicyEngine` (app allow-list, confirm-classes delete/money/install/CAPTCHA/transmit, hard-denies, kill switch, rate limit, safe zones, audit hook); **macOS twin** — Screen Recording + Accessibility surface. **Verified:** live Linux E2E under Xvfb — list → capture (PNG magic) → OCR → locate GO → XTEST click → label flips to CLICKED → verify satisfied (fixture = tkinter app, no xterm/fontset dependency); **27 unit tests**; Windows backend cross-compiles clean (`cargo check --target x86_64-pc-windows-msvc`, UIA + SendInput + PrintWindow + screen-DC, windows-0.58 API — `PrintWindow` lives in `Win32::Storage::Xps`, `VIRTUAL_KEY`/`BSTR` wrappers); clippy 0 on both targets. Two real bugs fixed while wiring the live test: `see()` used screen-relative offsets (GetImage is window-relative — mis-captured windows off the origin) and `list_windows` required a WM (added XQueryTree fallback). Honest follow-ons flagged in `capabilities()`: WGC occluded capture (WinRT seam), AT-SPI tree, Tauri wiring. Counts: **1120 = 739 done + 381 open** (P9 28/8/20; header + TOTAL in lockstep). No spec/ARCH row changes.

> **Then 2026-08-23 (P6.7 external-client E2E landed — supersedes the straggler-close note's "stays open"):** a REAL third-party MCP client now connects to our MCP endpoint and calls `snapshot` — no credential-gated binary needed. Zed/ACP research grounded the shape (Zed's ACP registry + `zed-industries/codex-acp` "Client MCP servers": connect over stdio → `tools/list` → `tools/call` — exactly our surface), and the client is the **official `@modelcontextprotocol/inspector` CLI** (v2, Node ≥ 22.19, CI-scriptable, no auth). Landed: (1) **`mcp-standalone-server` bin** (real 42-tool catalog over newline stdio + loopback HTTP, deterministic host-seam handler); (2) **`McpServer` protocol fixes real clients exposed**: `initialize` compat handshake (echo client protocolVersion, no session state) + `ping` + JSON-RPC **notifications never answered** (a response to one corrupts client message routing); (3) **wire-shape fix — `tools/list` is now camelCase** (`inputSchema`/`readOnlyHint`/`openWorldHint`, `ttlMs`): the SDK drops snake_case entries, which is exactly the bug the CLI surfaced (empty catalog); (4) **`tests/external_client.rs` 4/4 green** — initialize probe, `tools/list` (42 tools + camelCase `inputSchema`), `tools/call snapshot` (exit 0 + structuredContent), loopback-HTTP `tools/list` — gated `EVERYAIOS_MCP_EXT_CLIENT=1` (npx fetch on first run; npx cache corruption is a red herring, `rm -rf ~/.npm/_npx` fixes). `tests/attach.rs` 3/3 stays green (our client → external server). Counts: **1120 = 731 done + 389 open** (P6 99/98/1 — only the Signal adapter stays deferred; header + TOTAL in lockstep). No spec/ARCH row changes.

> **Then 2026-08-23 (straggler-close pass — TODO only, no spec change):** closed the last small stragglers inside the done phases, all code-verified + tested (workspace **1456 passed / 0 failed**, was 1432). **(1) P5.12 LazyGraphRAG landed (3/3):** `everyaios-memory::lazy_graph` — deterministic NLP concept extraction (`extract_concepts`: capitalized sequences + content-word runs, stopword-filtered) + `LazyConceptGraph` co-occurrence store built **only on query** (zero up-front LLM); `LazyGraphRag::retrieve` = best-forward chunk ranking (`SimilarityScorer` seam, default lexical) → BFS co-occurrence/community traversal → LLM `RelevanceAssessor` seam behind one `relevance_budget` knob (default 5 = MSR 3–5 subqueries); without an assessor, traversal is budget-capped and every report says `assessed:false` — the honest flag (`capability_status()`, `enabled:false` until wired); regression benchmark `benchmark_lazy_graph_finds_bridge_when_lexical_cannot` proves the global-query class (zero shared terms, graph bridges the answer while lexical top-1 cannot). P8.0-corpus run + UI surfacing stay gated on the P8.0 eval corpus (post-v1). **(2) LadybugDB seam (P5.2):** `graph::GraphBackend` trait + native `GraphStore` impl + swap-in test — the C++ FFI itself stays open/gated on the native lib (doc 54). **(3) P27 backend landed (5/5, UI wiring follow-on):** `everyaios-core::models` — `hf` (live Hub client, zero hardcoded repo ids, resumable `Range` + `.gguf.part`, sha256 via `.gguf.sha256` **and** LFS `oid`, byte progress, disk preflight, live-RAM quant recommendation), `store` (doc-79 layout + `index.json` registry, atomic save), `local_url` (`local://hf|ollama|llamafile` mint/parse + resolver derived from registry + installed runtimes, fail-closed), `probe` (CPU/RAM/disk/VRAM probes + /proc process scan + TTL-cached OpenAI-endpoint probe), `ModelsRuntime` (`serve_gguf` via managed llamafile + `/health` wait; `bind_ollama` via `ollama create` Modelfile; fail-closed `NoRuntime`). **(4) P6 verified, not force-closed:** MCP external-binary E2E stays open (real CLI is credential/install-gated) — the local stdio/loopback attach E2E re-verified green (`everyaios-mcp/tests/attach.rs` 3/3); Signal adapter stays `[DEFERRED post-v1]`. Counts: **1120 = 722 + 398 → 730 done + 390 open** (P5 72/71/1 · P27 6/6/0; header + TOTAL in lockstep). No spec/ARCH row changes.

> **Then 2026-08-23 (v3.55 — final line-pass; micro-fixes only):** re-read of the spec intro + affected rows after v3.54. Fixed: missing blank line before "Continuity and trust"; doubled space inside the H18 v3.54 clause; the competitive-stance line attributed lid-closed continuation to H18 — corrected to H33 (always-on node), with H18 noted as the view/steer-from-phone surface; P10 pillar (Remote & Cross-Device) now names H33 explicitly beside H18 with the never-a-founder-fleet boundary. ARCH/09: G8's cross-reference spacing cleaned. No capability change, no count change (**1120 = 722 done + 398 open**, 151 rows). Headers → v3.55.

> **Then 2026-08-23 (v3.54 — de-mishmash / de-duplication pass; every section re-read for purpose):** line-by-line read of the full spec + TODO + ARCH to remove repetition and stale clutter, keeping every row and its provenance intact. **1. G1/G8 were ~85% duplicate** (both claimed searxng + public instances + circuit breaker + BM25 + 5-min TTL + parallel cascade). Split into two purposeful contracts: **G1 = the no-key user-facing search surface** (health-gated instances + cache + `search.query`), **G8 = the cascade engine** (all tiers + parallel fetch + rerank) — cross-referenced on both rows and in ARCH/09 (its G1 row rewritten, G8 annotated). **2. The intro's "Casual/Power users" scenario lists duplicated UC-1…UC-5/UC-14 verbatim** — the 1–4 enumerations were removed and replaced by a pointer to the UCs (keeping the four-ask empty-state framing + the 4 casual / 3 power count the UI chrome follows); the "Continuity and trust" prose stays. **3. Stale count line fixed** — §5 (Engine Module Map) still said "the 149-row matrix"; now 151 (capstone + scope line were already correct). **4. H18 vs H33 drift made explicit** — H18 = remote view/steer surface, H33 = second user-owned execution node; complementary, no shared machinery (prevents future mash). **5. TODO cross-refs added to make non-overlap explicit:** P18 (inbuilt *directory*) vs P22 (runtime *manager*); P33 (render/UX tier) vs P42 (data connectors). No capability loss, no row count change (151), no checkbox change (**1120 = 722 done + 398 open**); headers → v3.54; README/ARCH-00/ARCH-09/DIAGRAMS/TODO in lockstep; `git diff --check` clean.

> **Then 2026-08-23 (v3.53 — IDE task + gpui reuse path + developer-tasks ledger; OpenClaw + Zed source research):** user prompted: does the IDE have a mode (Cursor-style chat ⇄ IDE), can we reuse Zed itself, can we add the cloud equivalents (Cowork lid-closed, Codex cloud envs, Cursor VMs), and is long-running work covered. **Live research, then edits.** (1) **IDE mode — yes, now spelled out:** Cursor's shape verified (mode picker Ask/Plan/Agent/Auto + Chat sidebar + Composer + Agent panel + ⌘I / Shift+Tab). The cockpit already has the machinery (H20 multi-view viewport + Code rail + H4 + composer); the spec now adds an **explicit Chat ⇄ Code mode switch** — ⌘⇧E / rail icon lifts the Code rail to the primary surface (file tree + multi-buffer editor + terminal + diff + LSP + plan docked; composer pinned; agent/chat one keystroke away) — a layout of the same workspace, never a separate product. (2) **“copy or reuse Zed” — license truth corrected:** verified from Zed's own open-sourcing announcement: `gpui` (their UI framework) is **Apache-2.0** — the reuse-relevant piece, explicitly licensed for building and redistributing desktop apps under any license; the Zed *editor application* layer is GPL-3.0. **I12 corrected:** my earlier row wrongly said “never a fork of gpui” — the honest v3.53 line is: gpui = the **v2 native-core candidate** (own editor core on gpui, direct dependency allowed), editor application layer = pattern-only, CodeMirror 6 stays the v1 lock; the ledger records one Apache-2.0 crate reused, zero editor GPL. (3) **Cloud equivalents analyzed — answer: user-operated or user-picked, never replicated:** OpenClaw's “cloud” is exactly H33's shape (source-verified: `Dockerfile`/`docker-compose`/`fly.toml` one-click + per-VPS guides — the user runs the daemon on *their* hosted VPS; no founder fleet exists there either); Cowork/Codex/Cursor hosted compute are *not* rebuilt — they are inherited by composition when the user picks that agent (F12/ACP), consistent with the frozen §8 non-goals. (4) **Long-running tasks — the real delta found and added:** OpenClaw's `tasks` ledger (docs.openclaw.ai/automation/tasks, read) shows exactly what our detached-work story was missing: an explicit task record `queued → running → terminal {succeeded, failed, timed_out, cancelled, lost}`, **push-driven completion** (wake the requester; polling is the wrong shape), **execution ≠ delivery** (blocked completions retry on a capped fenced generation, `blocked` ≠ `failed`), `lost` = no live authority + no durable evidence after a 5-min grace, 7-day retention, `tasks list/show/cancel/retry/audit`. We already have the deep half (B7 scheduler: 30s heartbeat lease + missed-heartbeat reassignment + checkpoint resume; ExecutionKernel `PendingApproval` — approvals persist inside the checkpoint; session reawakening) — the contract now lands as **BackgroundTaskRecord on B7** + the H19 timeline binding + **TODO P43 (4 tasks)**. Counts: rows unchanged **151**; TODO **1116 → 1120 = 722 done + 398 open** (P43 ×4). Headers → v3.53; ARCH/09 B7/H19/H20/I12 updated with the same contracts; README/ARCH-00/DIAGRAMS in lockstep; `git diff --check` clean.

> **Then 2026-08-23 (v3.52 — competitor-gap reconciliation: user-operated always-on node + Zed-class IDE capability + M365/Google connectors v2; OpenClaw + Zed source research):** a third-party review listed 10 areas where final EveryAIOS still loses: cloud execution while the machine is off, a 24/7 cloud fleet, no bundled frontier brain, zero-setup UX, enterprise identity/compliance, M365/Google graph depth, hosted compute capacity, mobile execution, cloud artifact collaboration, specialist IDE depth. **Live-web verified (no assumptions):** all ten are accurate today — Claude Cowork runs in the cloud with scheduled tasks continuing while the laptop is closed and sessions moving across surfaces (Anthropic Help Center + press, Jul 2026); the Codex app ships native worktrees + parallel agent threads + background/cloud environments (OpenAI); Cursor cloud agents run isolated VMs, test their own code, and attach screenshots/videos/logs to PRs (Cursor docs). **And every one is already a named, deliberate trade-off in §8/§9** (founder servers ❌, in-app M365/Gemini presence ❌, no bundled brain, competitive stance: "loses on default brain, habit/brand, cloud-continue-when-lid-closed"). Verdict: no founder cloud, ever — the one conversion that would delete the product's reason for existing. **The review's blind spot is the mirror:** competitors chain the user to their own brain + cloud + data and their artifacts carry no evidence; our existing counter-position = Chief-slot swap (P38), user-owned vault + audit, and K1 receipts — none of which they can offer. **Three counter-moves landed as new contracts, decided on 2026-08-23:** (1) **H33 — user-operated always-on executor node (post-v1, matrix row #150)** — the user's own hardware (mini-PC / old laptop / Raspberry Pi-class / home server, or a VPS under *their own* AWS/GCP/DigitalOcean/Hetzner/Fly credentials) runs scheduled/background work 24/7 while the control laptop is off; attaches via the landed P8.9 E2E sync transport (X25519 + ChaCha20-Poly1305, ledger reconcile + tombstones); Guard-2 never leaves the control surface (approval steps park on the node); a "rented fleet" product was researched (incl. OpenClaw's deploy stack — `Dockerfile`/`docker-compose`/`fly.toml` one-click + per-VPS guides, source-read as the pattern) and **explicitly not adopted** — plugins/skills ecosystem + hosted-agent composition via F12/ACP covers fleet-heavy asks; the row's boundary note records that. (2) **I12 — new row (#151): Zed-class Rust IDE capability, post-v1** — one capability behind the Code rail, not a replacement product: base = tree-sitter + multi-buffer + terminal/diff + tasks over existing stacks (LSP = I11, index = I7); distinctives = **worktree-first parallelism** (per-sub-agent `git worktree`, Codex-app pattern — never a shared dirty tree, K2-reversible), **all writes ticketed** (I8 → Guard-2, no silent autosaves), **receipts-in-editor** (K1), **any-brain** (the editor owns no model; Zed is the GPL-3 pattern-only reference, never a fork). (3) **F14/F15 v2 — Microsoft Graph + Google Workspace connectors** (mail, calendar, Drive, Teams/OneDrive, Docs/Sheets) via official APIs with user-owned OAuth in the vault, read-first — "our app accesses *their* apps" as data surfaces, staying above them. Counts: capability-matrix **149 → 151 rows**; TODO **1106 → 1116 = 722 done + 394 open** (P40 ×3 · P41 ×4 · P42 ×3, all post-v1). Headers → v3.52; ARCH/09 mirrors H33/I12; README + ARCH/00-INDEX + ARCH/DIAGRAMS in lockstep; the competitive-stance paragraph, UC-10, and ship order updated to name H33 vs the founder-cloud non-goal; `git diff --check` clean.

> **Then 2026-08-23 (v3.51 — algorithm & memory research pass):** re-read the algorithm code + re-searched the 2025–26 algorithm/memory landscape (web-verified, no assumptions). **Algorithm Index 33 → 34:** FSRS (C13) was implemented and landed (P5.11, `everyaios-memory::fsrs`) but never numbered — promoted to **Algorithm #34**. **Homes note corrected** (it listed #13/#15/#16/#18/#19/#20/#21/#29 as lineage-only, but the code-verified homes are in-repo `everyaios-memory`: #13 `::embedding`, #15 breaker (`everyaios-core` supervisor + `::blueprint::iteration`), #16 `::usage`, #18 `::fusion`, #19 `::rerank`, #20 `::paging`, #21 `::compaction`, #29 in `::fusion`, plus #34 `::fsrs`+`::reinforce`); the lineage-only set is now accurate (#1, #2, #5, #7, #9, #12, #14, #17, #22). **New C6 REF — LazyGraphRAG (MSR 2025, search-verified from the MSR blog):** query-time concept graph (NLP noun-phrase concepts + co-occurrence, iterative-deepening best-first/breadth-first, one `relevance_budget` knob); indexing cost ≈ plain vector RAG = ~0.1% of full GraphRAG's LLM index cost; global-query-quality answers at a fraction of the query cost; fits local-first token budgets. → added to C6 as an optional lazy mode + **TODO P5.12 (3 tasks)**. **C12 note-object memory (REF, A-MEM — NeurIPS 2025, 1K+ citations):** agent-constructed atomic notes + typed link generation + evolution (link/merge/archive) — a third memory shape (facts/vectors + KG + evolving notes) over the same store/export; no second database; tracked as a REF/pointer (no new task yet). **C13 — FSRS-7 upstream line (2025) is *not* yet in the permissive `fsrs-rs` v6.x releases (verified against the repo's releases/README today); FSRS-6 lands as built (P5.11), FSRS-7 tracked as an adopt-when-shipped watch item.** **#21 Acon REF (arXiv 2510.00615):** Agent Context Optimization — the 2025–26 state of the art for summary-compression of long-horizon histories — recorded as a REF around the compaction row without adopting (our compaction pipeline already matches its shape at a smaller scale). **Counts — TODO 1103 → 1106 (722 done + 384 open):** P5.12 adds 3 tasks (+3 open); a real bug found in SUMMARY (P4/P5 rows were joined on one line, hiding P5's open count) is fixed. Also (research-index follow-up, not an edit this pass): the RESEARCH corpus has **no** GraphRAG/A-MEM/Zettelkasten/SMEM/semantic-memory coverage — nearest residual homes are docs 05 (GraphRAG/D2R) and 34 (SOTA/final-pass); verified links recorded here for future index work: A-MEM (arXiv 2502.12110), LazyGraphRAG (MSR blog), Acon (arXiv 2510.00615). Counts: **1106 = 722 done + 384 open**. Headers → v3.51. Full diff at the working tree (7 files).

> **Then 2026-08-23 (v3.50 — deep re-audit pass: every claim re-read against code + live registries, no prior bias):** first-principles re-read of the entire spec + ARCH + code before any edit. **Live-verified again, all four endpoints return exactly the spec'd shapes:** MCP registry `registry.modelcontextprotocol.io/v0/servers` → `servers[]` with `_meta.io.modelcontextprotocol.registry/official` + `isLatest` (field confirmed present); ACP CDN `cdn.agentclientprotocol.com/registry/v1/latest/registry.json` → `version: 1.0.0` + `agents[]` (matches `registry_client::REGISTRY_URL` byte-for-byte); `models.dev/models.json` → live JSON map (note: `/api/models.json` now 302s to the HTML site — plain `/models.json` is the endpoint the spec cites); `searx.space/data/instances.json` → live. **Code-verified claims:** supervisor backoff `min(2^n, 60s)` + circuit breaker 5 crashes/10min; `MAX_FRAME_LEN = 16MiB` in Rust `frame.rs` + TS mirror `frame.ts:19`; `broker.rs DEFAULT_BASE_URLS` carries the chatgpt-pro backend-api URL (inert until an OAuth token exists — user opt-in); vault-key order env → passphrase+Argon2id keyfile → generated (never a silent first-boot key); IPC socket is Unix-only (`#[cfg(unix)]`) — the Windows named-pipe gap is real and stays an honest queued item (R4/P39.1); coordinator is **Bun-compiled** (`bun build --compile`, `--smol` dev) with the `@personal-ai/core-*` lineage imported via `../APP/packages/*` workspace glob (sidecar engine is in-repo plus lineage). **Spec corrected (all small, all real):** F8 harness-set count 9 → **10 named CLIs** (F12 lists 10; Cursor caveated as IDE); §4.2.4 flow label `(E1–E14)` → `(E1–E17)`; "NODE SIDECAR" → Bun-runtime sidecar in the §4 diagram, §4.0 intro, and the coordinator row; J13 heap flag annotated (V8 `--max-old-space-size` is the Node-lineage default; on the Bun runtime the 512MB budget + 80% self-restart lives in `heap.ts`); **Algorithm-Index implementation-homes note rewritten to match code** — native in-repo cores verified: #3 ghost, #4 graph+actr, #6 graph, #8 graph+cognee, #10/#32 actr, #11 core::memory_service+graph, #21 compaction; #1 (Forgetting-to-Remember), #2 (Risk Compass), #9 (correction-detector) have **no in-repo core** (lineage packages only) — the old note claimed 1–4/6/8–11 all native in Rust, which was false for 1/2/9; capstone "(A–J + EV)" rephrased (EV1 = the cross-cutting evidence subsystem, not a matrix family); **§7 Build Order completed** — added the missing Stage 0 row (landed 2026-08-20, the marketing gate) + P10 (E2E/QA), P11 (UI/UX), P12 (GTM) parallel rows (they exist in ARCH/10 and TODO but were absent from the spec table). Counts unchanged **(1103 = 722 done + 381 open**, no checkbox edits — this pass touched the spec/changelog only). Headers → v3.50.

> **Then 2026-08-23 (v3.49 — version moves here; use cases + subsystem map enter the spec):** per user directive, `DESKTOP-APP-SPEC.md` now carries **zero version metadata** — this file is the only home of version history. Removed from the spec: the `Version:`/`Date:`/`Supersedes:` header block and every version tag in section titles (Dynamic Chief §4.2.5a, frozen control plane §4.3 + its "as of v3.47" line, perf queue §9.3, K-Pillar §10 — all now plain titles; the frozen-state note points here). **Added — §Real-World Use Cases at the top** (UC-1…UC-14, detailed end-to-end): Downloads triage/dedupe, interview prep (calendar + memory + research), company research → deck, fix-this-repo with receipts, Q3 refresh + exec-summary patch (surgical OOXML, NOT_RECALCULATED honesty), tool-capability comparison sheet, draft-14-emails queue with per-send approval, quarterly-report orchestration, logged-in vendor comparison (Session Vault), scheduled Monday competitor digest (B7, quiet-hours), internal-dashboard run-log pull, mid-task resume after crash/Chief swap (Work survives), turning three sessions into a sourced report (memory maintain + conflict check), and Claude-subscription-as-Chief (UC-14, the Dynamic Chief from the user's seat). Each case shows the same loop: state the job → plan → Guard-2 approval → execute → visible/undoable/resumable. **Added: §4.0 Subsystem Map — modules, responsibilities & interfaces:** the three-process runtime (UI ↔ Rust core ↔ coordinator + spawned children), the wire contracts (Tauri command IPC · JSON-RPC 2.0 over stdio with `everyaios-ipc` framing `PROTOCOL_VERSION=1`/16MiB cap · CDP loopback · ACP/MCP stdio), and the complete per-crate contract table for all 17 crates (public types/traits/interfaces + boundaries — e.g. `ProcessSupervisor`, `ExecutionKernel`/`ExecutionPhase`/`PendingApproval`/`ForkLineage`, `GuardService`/`GuardDecision`, `AuditEvent`/`AuditWriter`+merkle, `ScriptSandbox` trait + `SandboxLimits`, `ToolDef`/`ToolProfile`/`BROWSER_TOOLS`/`paginate`, ACP `client` + `registry_index` + `a2a` card model, `SearchTransport`/`G8Cascade`/`DeepResearch`, memory `embedding`/`bm25`/`graph`/`fsrs`/`reference`/`compaction`, blueprint `spec`/`subagent`/`surgical`/`plugin`/`skill_store`, office `zip`/`xml`/`conformance`, storage `dedup`/`events`/`treemap`, codeintel LSP/SCIP — as implemented in `crates/`) plus the coordinator and UI rows and the three cross-cutting contracts (Execution · AuditEvent · IPC frame). Section numbers unchanged (cross-referenced); 149 rows + 33 algorithms intact; counts unchanged **1103 = 722 done + 381 open** (TODO untouched this pass). README + ARCH/00-INDEX + DIAGRAMS + TODO spec ref → v3.49.

> **Then 2026-08-23 (v3.48 — spec de-status pass):** per user directive, `DESKTOP-APP-SPEC.md` is now *literally spec* — explanation, use cases, extremely detailed architecture — with **zero app build status** (status is represented only in `TODO.md`). Removed: the P6/P7 header status blockquotes (97/99, 64/64, landing lists — they live in this changelog); the §0 status legend + the Status column from all 149 matrix rows (A–J); inline status annotations (✅ / landed / `P4.x ✅` / "not built" / "NOT DONE" / 🔁 / dates-as-status); the Algorithm Index Tests + Retest columns (now a single 33-row contract table); the §5 "What's ALREADY BUILT" status table (retitled **Engine Module Map** — implementation homes only); the §6 landing-log blockquote (retitled **The New-Build Surface**; the full landing log remains here); the §7 retest-gate line; and the "Current-state honesty" status paragraph in §9.1 (replaced with a pointer to this file). **Bug fixed:** the §4.2.5 flow diagram still carried the superseded v3.45 claim ("omit fs/terminal → native tools UNSUPPORTED") — corrected to the v3.46 governance modes (Governed-Mediated | Self-contained | NotGoverned). Risk-verdict markers (R1–R7 "✅ True / ⚠️ Partially") and decision provenance dates are retained — they are audit conclusions and decisions, not build status. Section numbers unchanged (cross-referenced from TODO/ARCH/README); 149 rows intact; counts unchanged (**1103 = 722 done + 381 open**); ARCH/00-INDEX + DIAGRAMS + TODO spec refs → v3.48.

> **Then 2026-08-23 (v3.47 — FINAL ARCHITECTURE FREEZE):** the agent control plane is frozen and consolidated into **spec §4.3** (single authoritative contract) after the 8-repo deep-dive + protocol verification. **A2A verified against the official spec** (a2aproject/A2A v1.0.0, Apache-2.0, Linux Foundation; fetched the live docs — the repo JSON schema path 404'd but the published spec + site confirmed): Agent Card = JSON metadata (identity/capabilities/skills/endpoint/auth), ops = Get Agent Card / Send+Stream Message / Get+List / Cancel Task, bindings = JSON-RPC/gRPC/HTTP, push notifications for long-running tasks; **MCP = agent-to-tool, A2A = agent-to-agent, explicitly complementary; A2A is NOT a sub-agent or tool-call protocol** → our B3 subagents are internal primitives (never A2A), our `everyaios-acp::a2a` card/trust model matches the official card model, remote task execution stays post-v1. §4.3 also consolidates: ACP v1-stable = local harness-drive (v3.46-corrected GovernedSession: Mediated | Self-contained | NotGoverned; v2 draft removes the fs/terminal client surface — monitored); MCP = the tool surface, Channel B the only fully-ticketed path; Chief model = P38 (§4.2.5a); perf queue = P39 (§9.3); invariants re-affirmed. J17 row updated with the verified A2A detail. **Header v3.47, supersedes v3.46.** Counts **1103 = 722 done + 381 open** (no checkbox change). ARCH/00-INDEX + ARCH/DIAGRAMS version refs → v3.47.

> **Then 2026-08-23 (v3.46 amendment — ACP/A2A deep-dive: GovernedSession mechanism CORRECTED + F8 quarantine):** deep-read 8 real ACP/agent-orchestrator repos (agentclientprotocol/registry, Enderfga/claw-orchestrator, formulahendry/vscode-acp, iOfficeAI/AionUi, xintaofei/codeg, Mng-dev-ai/agentrove, farion1231/cc-switch, KadenMc/work-buddy — full file trees + key source, GitHub-API verified) and the ACP spec + v2 RFD. **CORRECTION (the important one):** v3.45 claimed omitting `fs`/`terminal` from `clientCapabilities` forces all mutations through MCP Channel B. **Wrong.** Verified: (a) codeg `host_tools_policy.rs` (issue #436) — omitting makes the agent fall back to its **own in-process backends** (its own sandbox governs; those ops never cross the ACP wire so our guard/audit cannot see them), while **advertising** fs/terminal:true makes sandbox-aware agents (Grok) **delegate** file/shell ops to the client; (b) the ACP **v2 RFD** (`docs/rfds/v2/client-filesystem-terminal-capabilities.mdx`) — v2 **removes** the client fs/terminal surface entirely ("not widely adopted") and points to exposing an MCP server + future agent-sandbox config as the way to offer tooling. → Spec §4.2.5a §3 + J17/F12 rows + §6 + ARCH/01 + ARCH/09 + TODO P38 (tasks 2 + 7) rewritten: per-agent **Mediated** (advertise+service through Guard — vscode-acp/codeg pattern) vs **Self-contained** (withhold → agent's own sandbox, honest "self-contained" claim) vs **NotGoverned**, with Channel B as the only fully-ticketed path (never forced by omission). Our `client.rs` already sends `{fs:false, terminal:false}` = the withhold path — that is now correctly labeled. **Confirmed-good against the wild:** our F8 `registry_index` matches the official `agent.schema.json` (untagged binary/npx/uvx, per-platform BinaryTarget archive+sha256+cmd/args/env, package name+args+env) almost exactly; A2A `SignedAgentCard`/`CardTrust` matches the A2A card model; B3 subagent depth/budget + delegation lineage matches codeg `delegation/broker` (depth_limit, per-parent byte budget, parent_tool_use_id lineage) and claw `run-ledger`'s honesty rule ("cost from an estimate is an estimate" — same as our EV1 honesty). **Genuinely-new from the deep-dive:** (1) the official registry ships **`quarantine.json`** (8 agents, per-agent reasons) — F8 should consume it (new sub-step in TODO P38); (2) vscode-acp's FileSystemHandler reads **unsaved editor buffers** via the workspace API (UX affordance for our Mediated path); (3) v2-draft monitoring (J17) is now concrete — the adapter must not depend on the v1 mediated surface long-term. Counts **1103 = 722 done + 381 open** (no checkbox change — corrections + sub-steps only). Spec §4.2.5a + §0 J17/F12 + §6 + ARCH/01 + ARCH/09 + TODO P38 updated; header stays v3.46.

> **Then 2026-08-23 (v3.46 — Performance & Footprint Queue):** from the perf-review priority table (P0/P1/P2 items) + live source verification. No new matrix rows — five small measurement-gated tasks on existing rows (TODO **P39**). **(1) IPC payload-budget enforcement** (doc-42 §1.4 — per-message-type max sizes: tool result 50KB → truncate+ref, scraped page → ref+2KB extract; the 16MiB `MAX_FRAME_LEN` cap already lives in `frame.ts`/Rust; spec R4's "open action item" is now a queued task). **(2) Semantic context-distillation tier** — token-level pruning (LLMLingua-2 pattern, microsoft/LLMLingua **MIT** verified — up to 20× compression, 3–6× inference speedup; native-Rust `semantic-sift`-class crate for Tauri — <20MB, sub-100ms CPU, verified via the tauri-apps discussion #15363) as an **optional** tier behind the landed ratio-based compaction (P5.7), gated on profiling, never into mutation paths. **(3) MCP loopback keep-alive + pooling** — today `serve_http_once` opens a fresh TCP per request (verified in `everyaios-mcp/src/server.rs`). **(4) Local-model KV-cache type knob** (A5/P27) — llama.cpp `-ctk`/`-ctv` (F32/F16/Q8_0…, verified in `llama.cpp/common/arg.cpp`) for memory-constrained local runs. **(5) Lazy-load enforcement pass** (R6 fix #2 — first-use-only mount + measured RSS delta via the `rss_measure` harness). **Confirmed already-covered, explicitly not re-queued:** native-Rust sidecar = P29 (the sift-core <20MB native binary validates the ~15MB target); MessagePack/binary serialization = R4 "measure first" (this spec and the perf review conclude identically); SSE for long-running = MRTR already landed in `everyaios-mcp::server`; ACP-default for agent communication = v3.45 Dynamic Chief (P38). Spec §9.1 R4/R6 updated (open action item → queued; sift-core evidence noted), A5 + F6 rows annotated, header → v3.46. **Detail pass (same day):** new **spec §4.2.5a** (full Dynamic Chief contract — `primary_chief` schema/resolution, `ChiefAdapter` trait signatures + both impls, `GovernedSession` wire payload + Governed/NotGoverned states, Work-survives-Chief resumption, delegation + permission derivation) and new **spec §9.3** (Performance & Footprint Queue — each of the 5 P39 items with what-exists/what's-missing/acceptance/measurement-gate, plus the explicit already-covered list). TODO P38 + P39 tasks expanded with sub-steps + acceptance criteria (non-checkbox bullets — count unchanged). Counts **1103 = 722 done + 381 open** (+5 P39). ARCH/09 (A5/F6 rows) + ARCH/00-INDEX + ARCH/10 + DIAGRAMS + README updated.

> **Then 2026-08-23 (v3.45 — Dynamic Chief: the top brain is a configurable slot):** the surgical hierarchy (doc 52 §1, F12 §6) is re-anchored — **`primary_chief` = inbuilt | ACP-installed agent (Claude Code/Claude Agent via official `claude-agent-acp`, Codex via official `codex-acp`)** is a session-level setting, not a hardwired inbuilt default. No new matrix rows; contracts land on **B9** (any registered agent can be Chief; subagents may be other agents), **F12** (brain tier is a swappable slot; when external occupies it, EveryAIOS is the governed workspace shell), **J17** (generic `ChiefAdapter` trait — `initialize`/`start_session`/`send_message`/`stream_events`/`cancel`/`request_permission`/`update` — with inbuilt + ACP impls; **`GovernedSession`** boundary). **Live-verified against the ACP spec + GitHub org (2026-08-23):** `initialize` negotiates protocol version + capabilities **bidirectionally** (client sends `clientCapabilities` — fs.readTextFile/writeTextFile, terminal, elicitation; agent answers `agentCapabilities` — loadSession, promptCapabilities, mcpCapabilities, auth); the spec mandates **capabilities omitted from `initialize` are UNSUPPORTED** — that is the protocol-level deny-list: a governed client omits `fs`/`terminal`, so the Chief's native FS/Shell tools are unsupported and all mutations must flow through MCP Channel B. Official adapters confirmed in the `agentclientprotocol` org: `claude-agent-acp` (Claude Agent SDK, tool calls with permission requests, client MCP servers, nested subagent transcripts, permission extension), `codex-acp` (ChatGPT/API-key/custom-gateway auth, **subagent launches as standard ACP tool calls** — validates Chief-delegates-to-subagents), `acpr` (registry-fed meta launcher — npx/uvx/binary, mirrors our F8 install machinery), plus the official ACP agent registry. **Work survives Chief death** via the landed event-sourced session + `config_hash` — swap `primary_chief` mid-Work, resume the same intent→plan→checkpoints→receipt chain. Honesty invariant: **if the governance boundary can't be established (capability negotiation refused/absent), don't claim governance.** Code = TODO **P38** (7 tasks, open). Spec §4.2.5 flow diagram updated (CHIEF DISPATCHER node). Counts **1098 = 722 done + 376 open** (+7 P38). ARCH/00-INDEX + ARCH/09 (B9/F12/J17 rows) + DIAGRAMS + README updated.

> **Then 2026-08-22 (v3.44 — P8.8/P8.9/v3.39-fields landing versioned + Office ON HOLD):** versioned the work that landed since v3.42. **P8.8 packaging/updater/CI (7/7)** — `tauri.conf.json` NSIS/WiX/deb/rpm/AppImage/dmg targets + updater config (minisign pubkey, endpoints, passive install, `createUpdaterArtifacts`); `tauri-plugin-updater` + `updater_cmds.rs` (check/install/relaunch); `.github/workflows/release.yml` 3-platform build matrix (deb/rpm/AppImage · NSIS/MSI · dmg) + `tauri-action` publish of updater-signed artifacts with the coordinator sidecar injected as a bundle resource via `TAURI_CONFIG`; signing keypair gitignored in `.tauri/`. **P8.9 E2E-encrypted sync (3/3) — protocol + live TCP transport** — `everyaios-core::sync` (ChaCha20-Poly1305 AEAD envelope, header-as-AAD MAC binding, X25519 key exchange + confirm handshake, `KeyPair`/`KeyExchange`, version vectors + tombstones, 3-way `reconcile` → `SyncDiff`, `SyncBundle` export/import, `SyncTransport` seam; 19 tests incl. transport E2E server↔client convergence, tamper/MAC, tombstone propagation) + `sync_transport.rs` (length-prefixed TCP framing, ECDH handshake, `SyncServer` on 47615, `sync_with_peer`, LAN/Tailscale) + 8 Tauri commands + Settings → Connections UI (`SyncSection`). **v3.39 ExecutionKernel fields (4/4)** — `config_hash`/`RuntimeManifest` per run, `PendingApproval` inside the checkpoint, `started_unknown_repair()`, event-sourced session SoT + `fork_at_turn` lineage. **Office ⏸ ON HOLD (user directive)** — D1–D8 engine + D9–D12 storage stay landed; no further Office work (UI follow-ups, LOKit/Google-Docs tier, perfectness-gap surface) until the hold is lifted; H5 follow-ups frozen. Verification: full workspace serial **1432 passed / 0 failed**, coordinator **110/0** + TS clean, UI `tsc --noEmit` clean, rustfmt + `git diff --check` clean. Counts **1091 = 722 done + 369 open**; P8 = 45 done + 0 open; P10 = 1 done + 49 open; Research Tasks = 54 done + 0 open; P4 (Office) = 56/56 landed, ON HOLD.

> **Then 2026-08-21 (v3.42 — casual center + spec front door):** Home/empty chat dropped Code/Research/Browse/Work-mode chips; outcome examples only. Spec §top is coworker + 4 casual / 3 power examples. TODO P32.8. Counts **1091 = 686 done + 405 open**.

> **Then 2026-08-21 (v3.41 — work-first casual nav):** Sidebar answers “where do I continue my work?” — Home · Activity · Projects · Files · Automations · New work · Recent-as-work-state. Memory/Guard/Connectors/Skills/Marketplace off primary nav (Settings Control Center + title-bar Guard chip). Home is a launchpad, not a metrics dashboard. Chat ⊂ Work. TODO P32.7. Counts **1090 = 685 done + 405 open**.

> **Then 2026-08-21 (v3.41 — inspiration UI chrome, layout reference only):** Screenshots from Cursor / Qoder / Ollama / TRAE TraeWork+TraeCode / ZCode / Claude Cowork / LM Studio used as **layout reference**, not a brand clone. `ui/` settings became grouped+searchable (Voice, Mobile, Browser & Network, Indexing & LSP, Permissions, MCP, Marketplace, Skills, Hooks, Worktree, Rules, Import, Usage, Launch CLI, …). Composer: permission chip + Agent/Experts/Spec. Empty chat: folder chips + Work/Code/Design. Spec Q&A lettered MCQ. Summary view Artifacts/References. Memory Repo wiki tab. Automations Daily Brief / Weekly Review / Project Monitor. Destructive MCP toast. Prefs = `localStorage`. Executor/Tauri wiring of auto-run, voice, QR, MCP attach, migrate parsers **not** claimed. `tsc --noEmit` green. **Not** live-clicked in Tauri. TODO P37. Counts **1089 = 684 done + 405 open**.

> **Then 2026-08-21 (v3.40 — E9 required, not a cut):** User contract: EveryAIOS matches **ChatGPT Desktop Computer Use + Claude Computer Use** on native windows, not browser-only. Removed the §8 non-goal “pixel CUA post-v1”. E9 is the **union**: WGC occluded HWND capture (winappCli/ChatGPT) + UIA invoke-first (sbroenne/deploymenttheory) + SendInput + OCR fallback (JeenyJAI/AtomicBot) + observe→one-act→assert (sandraschi/ChatGPT guidance) + see-pane overlay/Esc + Guard-2 allow-list/confirmations/hard-denies + Layer-1 APIs first (iyulab) + CDP kept for browsers. Status 🟡 specified, **not built**. TODO P9.1 2→8 open (+6). Counts **1072 = 678 done + 394 open**.

> **Then 2026-08-21 (v3.40 — A5 Discover UI-only, P27 backend reverted):** user required UI-folder only and **no hardcoded models**. Removed `everyaios-core::model_fetch`, `staff_picks`, Tauri `local_catalog`/`local_quants`/`local_download`/`local_prefs_*`, `hwfit::inspect`. Discover fetches live Hugging Face Hub from `ui/`. Download/store/`local://`/runtime bind remain `[NOT DONE]`. TODO **1073 = 676 done + 397 open**.

> **Then 2026-08-21 (v3.40 — capability expansions on existing rows, live-verified):** no new matrix IDs. **F6/F7** MCP tools+resources+prompts; client roots+elicitation+sampling (elicitation/MRTR `InputRequired` → Guard-2; sampling → broker; resources = C10). **I6** executor hooks (`PreToolUse` deny-only, never skip ticket; PostTool*/batch; turn/session) distinct from J18 profiles. **B3 Scout** read-only clone-to-cache (OpenCode). **Editor lock = CodeMirror 6.** **F12 honesty:** Cursor is an IDE; Gemini CLI consumer SKU sunset 2026-06-18 → Antigravity; do not hardcode names. **D4** `annotationStorage` persist. **I8/K2** one verified edit = one git commit. **E2** console/network/perf diagnostics. **E5** optional HAR. **D10** hash cache. **G7** USN + FTS5 trigram. TODO **1068 = 676 done + 392 open** (+10).

> **Then 2026-08-21 (v3.39 — kernel resource-record wording + six named-type contracts):** no new matrix IDs. **Wording:** a live-registry hit is not a capability — it becomes a durable, versioned, health-aware resource record (validate → install → inventory → enable → start → health → use → observe → update/rollback/remove); effects still cross one ticket → one executor → one event log. **`ManagedResource`** is processes only (model runner, MCP, ACP, browser child, sandbox, worker) — not Office, not vault Providers, not deployments. **Six contracts** (types/fields on existing rows): (1) ManagedResource trait; (2) P27 as already specified + LM Studio/llama.cpp process probe; (3) `RouteDecision` + `ProviderObservation` (A7 honesty: `router.ts` is capability-filter + cost-sort; 13-factor scorer not built); (4) `MCPServerRecord` + per-consumer enable; (5) ExecutionKernel — immutable `config_hash`, Guard-2 inside checkpoint, never-started vs started-unknown, event-sourced session SoT + fork lineage; (6) `DocumentAsset` + C6 EXTRACTED/INFERRED edges + B3 derived child permissions + inbuilt abort/termination events + C2/C7 branch memory + `maintain()` tools + C3 abortable retrieval + E14 learned helpers + E10 acquisition adapter. **E9 stays ⚪.** Priority: engine-attach → ExecutionKernel disk → K1 → P27, then these contracts. TODO **1058 = 676 done + 382 open** (+17). ARCH/00–11 + DIAGRAMS #25 + README + UI-DESIGN-PROMPT + research index pointer updated. No code this pass. No extra TODO rows on the follow-up sync.

> **Then 2026-08-21 (v3.38 — P3 Cockpit/Audit UI + P3.3 traceparent + P6 connector transports close-out):** **P3 cockpit/audit UI fully landed** — `audit-view.tsx` rewritten with scrubber (play/pause/skip/seek slider), session chips, per-step screenshot strip, search box, watch-mode poll (3s interval), Stop button; `cockpit-slideover.tsx` polls `cockpit_snapshot` every 2s with agent cards (status/model/tool/tokens), interrupt cards (MCQ approval), quiet badge, per-agent STOP + UNDO buttons. **P3.3 traceparent propagation** — `stream_provider` in `chat.rs` creates `TraceContext::new_root` and injects `traceparent` via `Broker::with_extra_headers` into every outbound provider HTTP request (both streaming and non-streaming paths). **P6 connector transports** — new `everyaios-core::connectors` module (4 submodules): `gmail` (`GmailConnector<T: HttpTransport, R: TokenRefresher>` — list_labels, search, get_message, send_message with 401→refresh→retry, modify_labels, trash, unread_counts; 9 tests), `calendar` (`CalendarConnector<T>` — list/get/create/update/delete events, free_busy, export_ics; 4 tests), `imap_smtp` (`ImapSmtpConnector<T: MailTransport>` — search, read, mark_read, mark_flagged, archive, trash, send, reply; 7 tests), `browser_session` (`BrowserSessionConnector<S: CdpSession>` — Gmail inbox, Notion page, Linear issues, Outlook inbox readers, click_element, type_text; 7 tests). All over injectable seams; live accounts/Chrome are runtime seams. **Counts: 1041 = 676 done + 365 open; P3 = 14/14 ✅; P6 = 97/2.**

> **Then 2026-08-21 (v3.37 — P8.4 Search & Research + P8.5 Workspace UI + P8.8 RSS close-out):** **P8.4 search & research** — new `everyaios-search` crate (17th workspace member): `G8Cascade` (cache TTL → health-gated SearXNG endpoints with failure-threshold + cooldown → DDG circuit-breaker fallback), `DeepResearch` (G2 breadth×depth tree with `all_learnings` bubble-up + `gap_check`), `Channel` adapters (G3 — arXiv/GitHub/EDGAR/Reddit query builders + payload normalizers, pure), `CitedReport` (assemble + `render_markdown` with confidence = fraction cited + sources_count), `ParallelFetchCascade` (searxng-mcp 4-tier: firecrawl→crawl4ai→raw→wayback per-page fallback), `SiteIndex` (SeekStorm-class TF-IDF inverted index), and `RepoWideScan` (G5 — `dependency_map` + transitive `blast_radius`). All over the injected `SearchTransport` seam (live fetch/discovery is runtime wiring). **P8.5 workspace UI** — `blueprint-view.tsx` (spec.md-style editor: task list with pending/ready/running/done/failed/blocked status, dep-satisfaction check, run/advance, progress bar, add-task) + `local-server-view.tsx` (start/stop, port config, endpoint URLs with copy, VS Code/Cursor integration hint). **P8.8 RSS measurement** — `everyaios-core::rss_measure` (`measure_self` /proc/self/status VmRSS + sysinfo fallback, `measure_tree` self + descendants via pgrep, serializable `RssSnapshot`, `measure_idle_and_warm` harness; the actual warm-RSS publication with the coordinator running uses this harness). Two real bugs caught by tests: channel normalizers used `unwrap_or(&[])` with mismatched array temporaries (fixed with a named `empty` Vec), and the RSS tree test assumed strict total ≥ self ordering across two separate reads (RSS fluctuates between calls — relaxed to a same-ballpark check). Verification: full workspace serial **1377 passed / 0 failed** (search 13, core 269 incl. 3 RSS), coordinator **110/0** + TS clean, UI build + `src-tauri` cargo check green, rustfmt + `git diff --check` clean. **Re-count: 1039 = 661 done + 378 open; P8 = 39 done + 6 open** (P8.4 9/9 · P8.5 2/3 · P8.8 2/7). Remaining P8: installers/auto-updater/CI (5 — release infra), E2E sync (1 — network seam), the data-analysis Python REPL runtime.


> **Then 2026-08-21 (v3.36 — P8.1 Reader + P8.2 Widget Cards close-out, 6/6):** **P8.1 universal reader** — `everyaios-core::reader::extract_text` is a zero-dependency extractor: Markdown (drops fenced code blocks, images, links, inline code, emphasis markers), plain text, and HTML (a dependency-free tag stripper that drops `<script>`/`<style>` content, decodes `&amp;`/`&lt;`/`&gt;`/`&quot;`/`&#39;`/`&nbsp;`); PDF and EPUB return explicit `ReaderError::Unsupported` — a documented seam for an install-gated external extractor (markitdown-class), matching the project convention. `ReaderIndex` is a chunked RAG over extracted content (`CHUNK_WORDS=160` with 24-word overlap), scoring chunks by deterministic query-token overlap × IDF (no embeddings, no network), with `retrieve` and `chat_overlay` that emits the top chunks with `doc/chunk` citations for the inline chat overlay. **P8.2 widget cards** — `everyaios-core::widgets`: `MathWidget::evaluate` is a safe recursive-descent calculator (`+ - * / % ^`, parentheses, unary minus, right-associative `^`; rejects division/modulo-by-zero and malformed input — no `eval`, no unsafe), plus `MathWidget::card`; `LookupWidget::card` renders ordered key/value rows + footer; `WeatherWidget`/`StockWidget` render `WeatherSnapshot`/`StockQuote` cards (condition/temp/feels-like/humidity/wind; price/currency/signed change) with a TTL cache (`should_refresh`/`store`) — live weather/stock APIs are documented runtime seams, tested with injected data. Two real bugs the tests caught: (1) the HTML script/style closer detection built its window *after* the leading `<` was consumed, so it never matched `</script`/`</style` and swallowed the rest of the document; (2) retrieval tf used `split_whitespace` (keeping `"water,"` punctuation) while df used `tokenize` — tf now tokenizes identically. Verification: full workspace serial **1361 passed / 0 failed** (core 266), coordinator **110/0** + TS clean, UI build + `src-tauri` cargo check green, rustfmt + `git diff --check` clean. **Re-count: 1039 = 648 done + 391 open; P8 = 26 done + 19 open** (P8.1 2/2 · P8.2 4/4).


> **Then 2026-08-21 (v3.35 — P8.6 WSL/POSIX Bridge close-out, 4/4):** `everyaios-core::wsl` — the WSL/POSIX bridge the spec's F10 row describes. **Path translation** — `WslPath` + `translate_windows_to_linux`/`translate_linux_to_windows` (accepts both `\\wsl.localhost\` and legacy `\\wsl$\`, normalizes to the current UNC form, first segment after the prefix is the distro per WSL UNC convention, round-trip verified) + `translate_windows_drive_to_linux` (`C:\foo\bar` → `/mnt/c/foo/bar`). **Exec detection** — `ExecEnvironment` (NativeLinux/Wsl1/Wsl2/Windows/Unknown) + pure `detect_environment(WSL_DISTRO_NAME, OS, /proc/version)` + `detect_environment_from_env` (WSL2 sets `WSL_DISTRO_NAME`, WSL1 shows `microsoft` in `/proc/version` without it, `OS=Windows_NT` = native Windows). **wsl.exe runners** — `WslRunner.build_argv` is pure (`wsl.exe -d <distro> -e <cmd>` on a Windows host vs `wsl -d <distro> -- <cmd>` inside WSL) and `run_through` executes through the injected `CommandRunner` seam so the guard still sees the full command — never a bypass. **Loopback IPC** — `WslFrame` length-prefixed JSON framing (`encode`/`decode_all` correctly reassembles a frame split across two reads; the socket/stdio transport is the documented runtime seam the coordinator wires). One test-driven correction: the legacy-prefix test originally expected `\\wsl.localhost\home\user` to mean the default distro — WSL UNC always treats the first segment as the distro name, so it's distro `home`, path `/user` (test fixed to match reality). Verification: full workspace serial **1345 passed / 0 failed** (core 250), coordinator **110/0** + TS clean, UI build + `src-tauri` cargo check green, rustfmt + `git diff --check` clean. **Re-count: 1039 = 642 done + 397 open; P8 = 20 done + 25 open** (P8.6 4/4).


> **Then 2026-08-21 (v3.34 — P8.3/7/9 close-out):** first P8 product-surface landings. **Personality system (P8.3, 3/3)** — `everyaios-blueprint::persona`: `load_persona` parses a `SOUL.md` (frontmatter `name`/`description`/`tone` + markdown body; rejects missing name/body, unknown tone); `TonePreset` (neutral/concise/warm/professional/playful/academic/custom) + `PersonaConfig::custom_override` give the user-tunable voice; `CORE_RULES` are *always* emitted first and a fail-closed override-verb × rule-domain conflict scan rejects any persona that tries to weaken a rule (`CoreRuleConflict`) — a persona shapes *how* the assistant speaks, never *what* it may do. **Telemetry (P8.7, 2/2)** — `everyaios-core::telemetry`: `Telemetry` is the single choke point, off by default; `TelemetryEventKind` is an enumerated bucket list and `TelemetrySample` has no content-bearing field (compiler-enforced — no prompts, file names, or memory text can ever be recorded); `opt_in`/`opt_out`; cold-boot tests prove no events and no requests before explicit opt-in. **Export / wipe (P8.9, 2/3)** — `everyaios-core::export`: `render_markdown_export`/`render_json_export` (round-trippable), `MemoryMirror` renders Obsidian-compatible `_index.md` + per-fact notes with `[[wiki-link]]`s to same-session/same-project facts (Obsidian-safe `mem:1`→`mem-1` targets; a *view surface*, never a second store), and `WipeScope` + `wipe_facts`/`wipe_messages` give per-scope wipe (project-scoped or global memory, session-scoped chat, connector, all) with no-silent-nuke semantics. E2E-encrypted sync stays an opt-in network seam. Verification: full workspace serial **1335 passed / 0 failed** (core 240, blueprint 100), coordinator **110/0** + TS clean, UI build + `src-tauri` cargo check green, rustfmt + `git diff --check` clean. **Re-count: 1039 = 638 done + 401 open; P8 = 16 done + 29 open** (P8.3 3/3 · P8.7 2/2 · P8.9 2/3).


> **Then 2026-08-20 (v3.33 — P7 full close-out, 64/64):** PHASE 7 COMPLETE. **P7.8 sandbox profiles + FS broker** — `everyaios-guard::sandbox` (`SandboxProfile` 3-layer model: no_new_privs → per-path allowlist (ReadOnly/ReadAndExecute/ReadWrite/AddIfExists) → syscall policy groups; Renderer (read-only fs) / Worker (rw scratch) / Network (no fs) profiles; fail-closed `validate`; `check_path` enforces layer 2 through the P7.7 path floor; `apply` is the explicit kernel seam — `UnsupportedPlatform` off-Linux, prctl/Landlock/BPF install remains runtime wiring), `path_seal` (serenity pledge/unveil doctrine — Closed→Unveiling→Sealed, canonicalized unveil refused outside the worker root, capability unveil only from the declared set, no additions after seal), `seccomp` (chromium `bpf_network_policy_linux.cc` pattern — `SyscallRule`/`Branch`/`ArgFilter::{Eq,MaskEq,Any}` nested switches with fail-closed `CrashSIGSYS`; `network_worker()` allows socket only for AF_INET/AF_INET6, connect/send/recv, everything else SIGSYS), `fs_broker` (chromium `syscall_broker` — `BrokerHost::handle` over `BrokerOp::{OpenRead,OpenWrite,Create,Stat,Access,Readlink}`: path floor → profile path rule → create-parent writability; `BrokerTransport` seam + `InProcessBroker`), and `pathfloor` 6-axis grants (chromium `BrokerFilePermission` — `PathGrant`/`GrantAxis` `read_only_recursive`/`read_write_create`/`read_write`/`create`/`temporary`/`stat` + `from_permissions_string` feeding J21 permissions.toml; 0-escape invariant test-gated). **P7.9 warm worker pool** — `everyaios-core::worker_pool::WorkerPool` (chromium zygote pattern: pre-spawn N sandboxed workers with `--profile`/`--scratch` flags over inherited stdio, ready-handshake after profile apply, round-robin acquire/release with dead-worker refill + grow; integration-tested against the real `mock-worker` bin). **P7.5 Guard-2 UX** — `everyaios-guard::diffcard` (`NativeCard::from_ticket` + `render_native_card` + nonce-bound `CardResponse` in the exact `guard_respond` shape — the native OS card shows the same facts as the webview and neither can mint a valid nonce) + the web-action confirm banner in `ui/src/components/panels/guard-panel.tsx` (Confirm & run / Block bound to the same nonce `guardRespond` path; demo `payment` web-action card in `guard.ts` demoTickets). Two design bugs caught by tests this pass: deny-list semantics (a narrow deny must *narrow* a broader allow, not invalidate it — carried into `GrantedCapabilities.denied`), and seccomp nesting (multiple `Case` arms are sibling branches, not successive levels). Verification: full workspace serial **1315 passed / 0 failed** (guard 109, core 236 + 3 worker-pool integration, blueprint 92, codeintel 41, MCP 26, ACP 46), coordinator **110/0** + TS clean, UI build + `src-tauri` cargo check green, rustfmt + `git diff --check` clean. **Re-count: 1039 = 631 done + 408 open; P7 = 64 done + 0 open — PHASE 7 COMPLETE.**


> **Then 2026-08-20 (v3.32 — P7.3 Extension/Plugin ABI close-out):** P7.3 (11/11) fully closed — the plugin ABI the spec's I6 row describes is now landed code, not design. **Security half — `everyaios-guard::granter` (deliberately dependency-free):** `CapabilityGranter` computes manifest allow-list ∧ host grant and refuses the *whole* plugin on any unlisted capability (fail-closed — partial grants are not a thing); `wildcard_match` ports the Zed `*`/`**` pattern (`*` = within-segment, no `/`; `**` = any depth; `?` = one char); fail-closed `TrustFlags` gate `network`/`shell`/`fs.write` classes and dangerous powers additionally require `sandboxed` + `approval_required` (Hermes `allowed_*` pattern); explicit agent-binding refuses empty or untrusted bindings (never global); and the deny-list *narrows* a broader allow (carried into `GrantedCapabilities.denied`, checked per concrete capability) instead of invalidating it — the initial implementation wrongly treated a narrow deny as a contradiction, which the tests caught. **Feature half — `everyaios-blueprint::plugin`:** `PluginManifest` manifest.toml schema (`ABI_VERSION=1`, `Contributes` slots loop/scheduler/sandbox/session-store + tools, `CapabilityList` allow/deny, `TrustFlagsDecl`, `AgentBinding`) with load-time `validate` (wrong ABI / bare capability / bad semver / bad name / non-TOML all rejected — `manifest_rejects_bad_bundles`); `PluginRegistry::scan` over `~/.everyaios/plugins/` at boot with lazy activation (`PluginState::Registered` → `Activated` on first use — registered-but-not-loaded is asserted); host-owned facades `HostFacades` — `ctx.llm.call`, `ctx.files.read/write` (capability-scoped over injected `LlmBackend`/`FileBackend`), `ctx.approval.request` producing the `ApprovalRequest` the host wires to the Guard-2 card (the plugin can never approve itself); and the dogfood rule — `first_party_catalog()` (office-tools / connector-hub / search-cascade) + `dogfood_rule` refusing any bundle that claims `author = "everyaios"` without being in the catalog (spoofing). P7 exit criterion is now met in code: manifest rejects bad bundles; capability blocks unlisted exec; lazy = registered-not-loaded. Verification: full workspace serial **1288 passed / 0 failed** (guard 86 incl. 10 granter, blueprint 92 incl. 6 plugin), coordinator **110/0** + TS clean, UI build + `src-tauri` cargo check green, rustfmt + `git diff --check` clean. Still open in P7: P7.5 native-OS diff card + web-action banner (2), P7.8 sandbox profiles + FS broker (5), P7.9 warm worker pool (1). **Re-count: 1039 = 623 done + 416 open; P7 = 56 done + 8 open.**


> **Then 2026-08-20 (v3.31 — P7.1–P7.2 Forge + Skills close-out):** P7.1 (10/10) and P7.2 (9/9) are fully closed with local, tested machinery. **Forge runtime** — `everyaios-core::forge::ForgeRuntime`: `iterate` (write → run test → green stop / rewrite from stderr → repeat; SHA-256 loop guard + iteration budget + per-iteration audit trace), `tdd_loop` (write test → prove RED → iterate until GREEN), `run_js` (delegates to the rquickjs `everyaios-script` `ScriptSandbox`), and `DockerSandbox` (read-only/writable mounts, network/memory/CPU caps → exact `docker run` vector through the same `CommandRunner` seam so the guard sees the full command). **Live LSP diagnostics** — `everyaios-codeintel::lsp_runner::LspRunner` spawns a configured language server (`LspServerConfig` command/args/env → `ProcessTransport::spawn_env`) and collects `publishDiagnostics`, process-level E2E against a real `mock-lsp-server` child binary; a `ProcessTransport::recv` multi-frame drop bug was also fixed (same class as the earlier ACP fix) with a regression test. **ECC guardrails** — `everyaios-guard::ecc` (`plan_before_build` verdict + `session_scan`: unplanned mutations, destructive-op cap, runaway rewrite streaks, estop, missing verify). **Loop self-audit** — `everyaios-core::self_audit::score_session` (5-dimension report; missing evidence stays explicit). **Skill registry** — `everyaios-blueprint::skill_store`: `SkillStore` scan/save/load/delete over `<root>/<name>/SKILL.md`, `SkillManifest` (name/description/tools/triggers + author/created/version ownership), `SkillIndex` (deterministic relevance scoring, `MAX_ACTIVE_SKILLS=20` cap, planner-tier `render`), restart-survival round-trip test, first-party `taste_skill` (anti-slop design, distinct from C9), and `grow_from_task` (versioned skill-tree growth with patch-version bumps). Real language-server binaries (rust-analyzer/pyright/clangd/gopls), live docker execution, P7.3 plugin ABI, and P7.8/P7.9 OS sandboxing remain open. Verification: full workspace serial **1272 passed / 0 failed** (core 231, guard 75, blueprint 86, codeintel 41 incl. 2 live-process tests), coordinator **110/0** + TS clean, UI build + `src-tauri` cargo check green, rustfmt clean. **Re-count: 1039 = 612 done + 427 open; P7 = 45 done + 19 open.**


> **Then 2026-08-20 (v3.30 — P6.6–P6.11 local-runtime close-out):** closed the locally-testable remainder of P6 rather than leaving interface-only rows. **P6.6:** `everyaios-vault::auth_bridge` (connector PKCE — no client secret, S256 challenge + single-use state, loopback token manager → key ring; full exchange loopback-tested) + `everyaios-mcp::attach` (spawn a user-supplied MCP server over stdio, negotiate `initialize`/`tools/list`, reconcile into the catalog — loopback mock-server E2E). **P6.8:** `everyaios-acp/tests/side_by_side.rs` now spawns two real `mock-agent` child processes over `ProcessTransport` (initialize→session/new→prompt→cancel, session isolation + independent teardown); `everyaios-blueprint::surgical::surgical_route` (brain→core→surgeon routing by task class + surgical weight); `everyaios-codeintel::lsp_config` (lsp-config.json parse + `recv_diagnostics` over `LspSession`). **P6.9:** Telegram (ureq, configurable base URL, update-offset dedupe), WhatsApp + email adapters over the `MessageAdapter` contract. **P6.10:** `everyaios-vault::tier::switchyard_route` (executor-vs-frontier by task class + planning weight, floor = executor, known-skill shortcuts). **P6.11:** `everyaios-core::email` (`Mailbox` trait + `InMemoryMailbox` + guard-ticketed `EmailService` — send/reply/triage gated, read/search not — + `classify_tool`) and `suggest_nudge` (email-context → cron/goal/confidence for B7). **Two real defects fixed along the way:** (1) `ProcessTransport::recv` dropped every frame after the first when several arrived in one read chunk — the ACP client could hang forever waiting for a response it had already discarded; now frames are queued (`pending: VecDeque`) + a `recv_queues_multiple_frames_from_one_chunk` regression test. (2) the broker test's one-shot `mock_server` read only the first TCP segment, making `cache_control`/body assertions flaky — now drains headers + Content-Length body. Verification: full workspace serial **1242 passed / 0 failed**; vault 99, ACP 44 + 2 side-by-side, MCP attach 3, core/blueprint green; coordinator **110/0** + TS clean; UI build + `src-tauri` cargo check green. Live OAuth/IMAP/SMTP/Gmail/Calendar transports, real external MCP/ACP binaries, live Aider SEARCH/REPLACE, real network delivery, and Switchyard routing *evaluation* remain open. **Re-count: 1039 = 596 done + 443 open; P6 = 91 done + 8 open.**


> **Then 2026-08-20 (v3.29 — final P6 status reconciliation):** the live checklist was recalculated from the current file rather than copied from historical summaries: **1,039 total = 583 done + 456 open; P6 = 78 done + 21 open**. Rechecked P6.1–P6.11 against the current Rust/coordinator consumers and tests. The local P6 contracts are complete and verified; the remaining 21 rows are real external-account, network-transport, third-party-binary, scheduler-invocation, or routing-evaluation work. Updated the current spec, README, TODO, and architecture module map to remove stale “MCP transport not wired” wording. The APP MCP client now uses the installed Streamable HTTP SDK transport rather than deprecated SSE, and the coordinator compile excludes pdfjs' optional `canvas` native module. Verification after reconciliation: targeted Rust P6 crates passed (MCP 23, core 212, blueprint 71, ACP 43) and coordinator passed **110/0** with TypeScript clean and the compiled sidecar build green. No external integration was marked complete without a live credentialed/binary-backed test.


> **Then 2026-08-20 (v3.28 — P6.6–P6.10 local-runtime close-out):** re-audited every P6.1–P6.11 row against live consumers. The coordinator now lazily consumes APP `core-connectors` through `connector/list` + guarded read/query planning, and APP `core-search` through `mcp/search` with HTTPS/loopback egress validation. `everyaios-mcp` now contains host-owned `tools/list`/`tools/call` handling over newline-delimited stdio and authenticated loopback HTTP, including origin/body limits and loopback E2E coverage; calls still route through an injected host executor rather than creating a bypass. Added deterministic `classifyTask()` routing and `MessageReminder`/`ReminderQueue` one-shot reminder semantics. The subscription-auth boundary is now explicitly marked in the active docs. Verification: MCP 23 tests, core 213 tests, coordinator **110/0** + TypeScript clean. P6 is now **78 done / 21 open**; the remaining rows are live OAuth/IMAP/SMTP/Gmail/Calendar/WhatsApp/Telegram transports, real external MCP/ACP binaries, scheduler invocation of arbitrary automation steps, and NeMo/Switchyard evaluation—not interface-only relabeling.

> **Then 2026-08-20 (v3.27 — Guard-2 nonce hardening pass):** approval cards now carry a random `approval_nonce` generated in the Rust ticket model; `TicketStore::approve_with_nonce`/`reject_with_nonce` and `GuardService` reject empty, mismatched, or stale nonces. The nonce is propagated through the Tauri command, coordinator/UI guard bridge, plan permission cards, Excel cell/batch approval cards, and `PendingGuardCard`; the Rust regression `nonce_binds_human_approval_to_the_card` passes. Added the GuardService read-only nonce accessor required by Tauri Excel commands and fixed the Office batch approval call-site. Verification: everyaios-core + everyaios-guard **280 tests passed**, UI typecheck + Vite build passed, `src-tauri` `cargo check` passed, `git diff --check` passed. The native OS diff-card surface remains open; this pass only closes nonce binding. TODO actual checkbox count: **1039 = 576 done + 463 open**.

> **Then 2026-08-20 (v3.26 — P6 orchestration core pass):** the verified P6 crate contracts were reconciled with the live code. Added `everyaios-core::automation_runtime` to bind blueprint `run_code`/`online_search`/email/calendar shapes to explicit script/search/connector adapters with fail-closed missing-engine errors and exact-run approval for connector mutations; added A2A secondary-interface types with endpoint validation and host-owned signed-card verification; confirmed crystallization detection/classification/compilation/skill storage/drift fallback, ConnectorHub routing/deduplication/metering, MCP ToolCatalog reconciliation + TTL/ETag/MRTR protocol shapes, ACP installer ownership markers, CodeWhale registry entry, messaging adapter/stub round-trip, and asymmetric tier configuration/shortest-path routing. These are crate-level, tested contracts—not claims that live G8 search, OAuth/IMAP/SMTP/Gmail/Calendar transports, a network MCP endpoint, real two-CLI ACP execution, or scheduler/coordinator invocation are complete. Verification: P6 targeted tests passed (everyaios-core 211, including 3 automation-runtime tests; blueprint 71; MCP 21; ACP 43), workspace `cargo check` passed. TODO actual checkbox count: **1039 = 575 done + 464 open**.

> **Then 2026-08-20 (v3.25 — Stage 0 landed):** the live guard-gated tool executor — the one open gate every external review named — is now implemented end-to-end and runtime-wired. **S0.1–S0.7 (29 tasks):** `ToolRegistry` + `tool/list` (42 MCP catalog + `script.run`/`file_ops.*`/`search.query`), `tool/exec` (Guard-1 pre-scan → `GuardService::evaluate` → ticket, canonical args-hash drift blocked, read-only auto-Allow), `tool/commit` (`use_ticket` + TOCTOU re-verify + dispatch + Merkle audit row + idempotency), coordinator `tools.ts` `ToolExecutor` (ask→wait→commit, never auto-consumes, sanitization, loop cap), native model function-calling (S0.3), plan-tool wiring (S0.4), UI tool chips + retry (S0.5), security invariants (S0.6 — fail-closed, TOCTOU binds + parent-dir fd, Chrome 136+ attach pairing via `ProfilePairingStore`, canonical args-hash, estop covers tools, ACP brokered `use_ticket`), and verification (S0.7 — golden tests, red-team/path/URL fuzz through the executor, ask→approve E2E, EV1 `eval/verify` at plan completion). **HARDENING H3 (8 tasks) also landed:** unified `Execution` record + state machine (`execution.rs`, `execution/*` JSON-RPC, Chat/Plan/Scheduler/ACP triggers), lease fencing tokens, per-run context snapshot (Task/Occurrence/Run), Data Egress Engine (`egress.rs`, Offline/Local/Byok/ThirdParty + `egress/*` JSON-RPC), per-capability idempotency keys, generated capability-status manifest (`capability_manifest.rs`), R0–R4 risk tiers, and the Stage-0 adapter contract + SEP-1024 exact-command consent (`adapter.rs`). **H4 (10 honesty leftovers) added + closed:** R7 vault key derivation (Argon2id keyfile, no silent first-boot key, blocking `VaultGate`), unix control dispatcher (`agent/stop`/`agent/undo`/`agent/interrupt-response`), ACP Ask-wait + Allow-`use_ticket`, UI `planExecute` read-only decompose, ACP chat-send path, selected-model pass-through, session persistence (SQLCipher `ui_sessions`), mutation audit rows, and `guard.ts` wiring into `ToolExecutor`. Verification: cargo workspace 1158 passed / 0 failed, coordinator 108 / 0, UI tsc + vite build green, `src-tauri` cargo check green. Spec updated to match: §6 Current-build-state (Stage 0 landed; remaining = engine attach at commit, P7.8/P7.9 sandbox, ExecutionKernel disk persistence, K1 receipts), capstone EV1 line (runtime-wired at plan completion), EV row, §10 gating (receipt/recovery evidence), §8 marketing gate (executor half done), §9.1 R1/R7/honesty, §9.2, legend. TODO counts re-verified: **1039 = 552 done + 487 open** (was 1014 = 470 + 544); README + ARCH/00 + ARCH/09 (J21/G1/G8 annotations) + UI-DESIGN-PROMPT badge updated to match. **Then 2026-08-20 (P1/P2/P4/P5 close-out batch):** the last open P1/P2/P4/P5 items were implemented and verified — P1.3 prompt-cache prefixing in the broker (`annotate_prompt_cache`: Anthropic `cache_control:ephemeral` on the stable system+tool prefix, OpenAI `cached_tokens` on ≥1024-token tool blocks) + semantic/result caches wired onto read-only chat turns (`MemoryService::cache_get`/`cache_put`); P2.3 file-op tools (`download_file` real HTTP GET + path-floored atomic write; `save_pdf_enhanced`/`save_screenshot_enhanced` via a `BrowserOps` adapter seam); P2.11 WebMCP HTTP server spawned from Tauri on boot; P4.8 storage tools runtime dispatch + Tauri commands (`storage_cmds.rs`, `AppState.battery` J16 gating) + storage-health dashboard UI (`storage.ts` + live `storage-view.tsx`); P5.10 `MemoryService::retest_algorithms` + `memory/retest` across the sidecar `MemoryService::handle` boundary. Verification: cargo workspace + coordinator 108 + UI tsc/build + src-tauri check all green. TODO → **1039 = 552 done + 487 open**. Spec D12 bumped 🟡→🔵 (storage tools + dashboard live) + A9 cache-wiring note; matrix totals 🟢26·🟡66·🔵60·⚪13.**
>
> **Then 2026-08-19 (v3.24 architecture-consistency pass):** reconciled the live contract after the hostile recheck. The product UI is now described consistently as a **48px activity rail plus a persistent multi-view viewport** (one active tab rendered at a time), replacing contradictory “one surface” wording in the spec, ARCH/00, ARCH/09, and ARCH/12. H13 is deferred with A8 (the local OpenAI-compatible server is not assumed in the current runtime), changing live status totals to spec **🟢26 · 🟡67 · 🔵59 · ⚪13** and ARCH/09 **🟢24 · 🟡67 · 🔵62 · ⚪13**. Memory docs now identify the Rust-native graph/SQLite surfaces as canonical, with LadybugDB optional/deferred; MCP 2026-07-28 statelessness is transport/version-specific rather than universal; Chrome 136+ default-profile CDP is explicitly refused by contract and E13 profile pairing/isolation is an open TODO; FSRS source comments no longer cite Anki rslib as the implementation. Cache percentages are reference targets, not product measurements. TODO is now **1014 = 470 done + 544 open**.
>
> **Then 2026-08-19 (UI polish + canonical UI-spec rewrite):** per a direct instruction to upgrade the shipped cockpit and make the UI doc perfect, `ui/src` gained the missing interactivity layer — (1) **Automations tabs are now real**: Active (job cards + inline editor), Templates (8 preset cards with `Use template`), History (last-7d run table with result/cost/duration) — content crossfades; (2) **Memory tabs are now real**: Episodic (time-stamped episodes), Semantic (confidence-scored facts + source), Knowledge Graph (interactive SVG node/edge map with selection ring), Skills (installed + suggested) — plus mock data for each store; (3) **Connectors tabs split**: Native (10-connector grid, Connect wired) vs MCP Servers (Connect flips the row live) vs Tool Catalog (real registry); (4) **every no-op button wired**: right-rail per-view actions (New file/terminal/tab, Diff, Inspector, Recalculate, Accept/Revert all, Clean up, …), Create automation → Templates, NL composer creates a demo job, Add knowledge, Browse MCP servers, Add native connector, Guard Allow/Deny + vault CTAs, Storage Review diff/Keep all, Settings Add key/Health/Priority/Edit, Analytics Export CSV, editor Save/Cancel; (5) **tab-content entrance animations** (180ms fade+rise via framer `AnimatePresence`) added to Automations/Memory/Connectors/Analytics/Settings. `UI-DESIGN-PROMPT.md` was then **completely rewritten** as the canonical spec of the shipped surface (12 sections: what the app is, design language, four-column layout, chat incl. composer/picker/MCQ, all six panels, all 14 viewport views, overlays, full mock-data inventory, the 20-row animation inventory, shortcuts, accessibility). Verdict: tsc + vite build green.
>
> Archived 2026-08-10. **The v3.23 historical pass (2026-08-18 — third-party audit pass: A8 🟡→⚪ (P9.5), F5 Composio/Zapier/Nango marked ⚪ removed per the Connector-platform decision (stale routing text fixed across spec §0/§2/P6/§7 + ARCH/00/01/09/10/11/12/DIAGRAMS), G8/G1/H22/E13 honesty annotations (G8 not built, G1 unwired, H22 partial, E13 unit-vs-live correction), retest-gate disposition documented (C-series memory reimplemented natively in Rust — supersedes 🔁 sidecar retest; remaining TS algorithms keep 🔁 until the coordinator consumes them), EV1 documented as a dev/test-time gate not a runtime surface, CI extended (UI typecheck+build always; coordinator bun tests when the APP_CLONE_TOKEN secret is set), ARCH/09 totals line corrected to actual counts, LICENSE (MIT OR Apache-2.0) + README added, §2 Signal/iMessage contradiction fixed, changelog header contradiction fixed). Supersedes v3.22 (2026-08-17): docs 80–82 folded in — external competitive benchmark review (doc 80), non-model moat roadmap review → new §10 K-Pillar (K1–K6) + TODO P28 (docs 81), innovation-priority decisions with Gates A–E (doc 82), the FINAL APP vision banner added at the top of the spec, and new §9.1 Known Risks & Mitigations (third-party audit: approval-fatigue, footprint, ecosystem-maintenance, IPC, OOXML/VBA, memory-vs-promise + the four "benchmark gaps" status — only Stage 0 is an implementation gap; the rest are sequenced/deliberate; footprint roadmap incl. the post-v1 native-sidecar option), doc 83 competitor batch (openworker/cc-switch/skales/deepseek-harness) → TODO P30, a new **B9 Custom Agent Builder** capability row (matrix → 149 rows) + TODO **P31** (progressive-disclosure UI — P31.1 landed in `ui/src`), doc 84 casual-vs-power-user UX research → TODO P32, and **ARCH/12 v3.0 + UI: right panel becomes a VS Code-style multi-view tabbed panel** (defaults Terminal · Folder · Browser, `+` add view, browser internal tabs, office files as tabs, PDF study-mode chat scoping, LibreOffice/LOKit "open-perfectly" + Google Docs/Sheets via browser or Drive/Sheets API) → TODO P33, then **ARCH/12 v3.1 + UI: full-fidelity tool surfaces ("nothing held back")** — Word/Excel/PowerPoint reproduce the complete Microsoft ribbon + Copilot, PDF = full viewer (nav/zoom/search/annotate/forms/sign/redact/thumbnails), Browser = full Chrome-style chrome incl. built-in AI Mode/Gemini sidebar; reusable `OfficeRibbon` + browser chrome landed in `ui/src` → TODO P34, then the **centered-composer empty-chat pattern** (lifted mid-canvas on new chat, bottom-pinned once the conversation starts) and the **full animation wiring (TODO P35)** — every row of the UI-DESIGN-PROMPT motion table is now a live implementation (approval enter, step enter/shake + new `failed` step state, surface crossfade with the horizontal slide removed, cell flash, chart crossfade, palette/panel scale-in, treemap morph, sparkline draw, trust-ladder roll, agent-switch pulse, shimmer skeleton, breathe processing).** Then **2026-08-18: the Stage-0 plan-executor seam landed (TODO P6.3 closed — the last open item in P0.1→P6.3):** `everyaios-core::PlanService` (per-plan `CircuitBreaker` owner over `plan/*` JSON-RPC), `ChatRelay` `chat/interrupt`/`chat/plan_done` forwarding + `start_plan`/`respond_plan`, coordinator `plan.ts` (live plan executor: LLM turns → `plan/step` → trip → `chat/interrupt` → await `plan/respond` → `chat/plan_done`), Tauri `plan_execute`/`plan_respond`, and the UI MCQ card path (`interrupt` event → `pushMcq` `kind:'mcq'` → Continue submits the selected option → `planRespond`). Tests: 117 core (+8) / 78 coordinator (+12).** Then the same day: **P6.4 Scheduled Tasks (B7) landed end-to-end** — `everyaios-core::scheduler_service` (5-field `CronExpr` + `TriggerSpec::{Cron,Interval,Event,Webhook}`, Gartner event kinds w/ scope+frequency policy, battery suppression, HITL pause-as-state + retry backoff+jitter+clamp (cronflow patterns), Hatchet heartbeat lease + checkpoint resume, nudge sentinels), coordinator `scheduler.ts` executor (due-ticker + loopback `Bun.serve` webhook + session reawakening), Tauri `scheduler_*` commands + tray "Run automations now", H14 UI (AutomationsPanel live + composer nudge cards). Tests: 135 core (+18) / 83 coordinator (+5). TODO P6.4 fully done — P0.1→P6.4 complete (959 = 457 done + 502 open). Then 2026-08-18: the **Tauri↔Coordinator bridge wired (adversarial-review C8)** — `ProcessSupervisor::new_with_link`/`start_supervisor_with_link` hand each spawned child's stdin/stdout to a link channel, `SidecarLink::new_with_activity` re-arms the idle watchdog per decoded frame, and `pre_spawn_coordinator` drains handoffs to (re)build the `ChatRelay` on every (re)spawn via `connect_chat_relay` (`#[allow(dead_code)]` removed) — `chat_stream`/`plan_execute`/`scheduler_*`/`usage_snapshot` now reach the live sidecar instead of returning "sidecar not connected"; plus honesty fixes from the same review (Guard-1 "100% red-team blocked" → deny-filter wording; Guard-2 "non-bypassable" → human-gated w/ nonce-hardening P7.5 item; chatgpt-pro URL "inert without an OAuth-acquired token"; vault-key dev-placeholder risk R7 added to §9.1). **Then 2026-08-18 (Stage-0 plan enumeration):** the guard-gated tool executor — the spec's one open implementation gate — is now a fully enumerated **TODO Stage 0** section (S0.1 Rust `tool/list` + `tool/exec` + `tool/commit` dispatch over the landed engines · S0.2 coordinator `tools.ts` executor · S0.3 model function-calling · S0.4 plan-tool wiring · S0.5 UI tool surface · S0.6 security invariants · S0.7 verification; 28 tasks, full 42-tool scope, no minimal slice). TODO 959 → 987 (457 done + 530 open). Entries below run v3.0 → v3.13; **v3.14 → v3.22 changes are recorded in the research notes docs (58/59/60/61/62/63/64/65/66/67/68/69/70/71/72/73/74/75/76/77/78/79/80/81/82/83) and the SPEC §0/§6/§10 text, not as versioned entries here.** (Correction 2026-08-18: the *header* of this file carries live-status notes — the v3.22 fold-in and the 2026-08-18 P6.3/P6.4 landings above — while the versioned entry list remains archived at v3.13; the earlier sentence "no new changelog entries were appended after archiving" contradicted its own header and is now clarified: no *versioned* entries were appended, header notes are maintained separately.) If a future pass re-opens the changelog, migrate the doc-58→67 deltas (OmniRoute ingest, deep-dives, ledger 227→281, cost-optimization + event-driven triggers + eval, 37-repo steal ledger → C13/E15–E17/I11/EV1, giants code-level deep-dive → G9 read-cleaner + P7.8 sandbox profiles/FS broker + P7.9 warm pool, batch-3 agent-infra/scraping/search/UI → A9 usage-parser registry + J11 efficiency metrics + G8/E14 selector/fingerprints + I2 SKILL.md anatomy + F8 skills_index.json + I7 persistent graph + I11 symbol-editing + P6 loop-pattern registry + P5 saved-vs-discovered, anomalyco org → models.dev catalog steal (A6 two-tier lab/provider schema + A9 cache pricing + A7 routing filters; ledger 274→278), capability deltas + UI/UX finalization → H29 local dashboard artifacts (bolt.diy) + B7 heartbeat automations (Hatchet lease pattern) + H20 redefined to activity rail + views contract (ARCH/12 v2.0, work cockpit) + session-open proactivity hook + durable-execution blueprint (ledger 278→281), final all-rounder market research → H30 voice-memo→report + H31 corpus-research surface + audio digest + H32 agent picker + agent-scoped model surface + two-channel capability injection (F12/J17/F7) + mobile-companion note (H18) + M365 Copilot Cowork / Gemini Notebook competitive positioning (0 new repos, ledger unchanged 281, matrix 145→148)) into entries here. **Then 2026-08-19 (security-posture + harness-pattern pass):** an adversarial 28-finding code review of the *live* runtime was verified claim-by-claim against source; the confirmed 🔴 code bugs (not already-tracked design gaps) were fixed and landed — ticket state machine gains a real `Approved` state (`use_ticket` refuses `Pending` with `NotApproved`; approval is now a hard prerequisite), `GuardDecision::Allow` carries a ticket (ticket-every-effect, no ticketless mutation path), the guard surface is split (`handle_sidecar` = evaluate/use/read only vs `handle` = UI control plane; the coordinator can no longer approve/estop/reset/profile its own actions), `min_confidence_for_auto` wired into `evaluate`, `xlsx_edit_commit`/`xlsx_batch_commit`/`acp_install_commit` require a mandatory ticket + path floor + atomic write, CSP restored (`"csp": null` → strict `default-src 'self'`), WebMCP HTTP gained bearer-token auth + body cap + timeout, and `topologicalOrder` is fail-closed (unknown deps + cycles throw). The ChatGPT/Claude agentic-harness deep-dive was also adopted as first-class **architecture patterns on their own merit** (web verification unavailable this pass): capability index / deferred tool discovery, monitoring-tasks "notify only on a meaningful delta", memory "consolidation" (background write-side synthesis), project-only memory isolation, and deterministic tool ordering for byte-stable cache prefixes — enumerated as TODO HARDENING H2. TODO 987 → 1002 (467 done + 535 open).** **Then 2026-08-19 (memory consolidation — write-side synthesis):** adopting the ChatGPT/Dreaming memory model (WRITE-side synthesis + READ-side injection; memory as a *changing belief state*, not an append-only log), `MemoryService` gained `StoredFact` temporal fields + a `FactStatus` (`Active`/`Superseded`) lifecycle, a deterministic `consolidate()` pass (a later fact with a negation marker + a shared subject supersedes an earlier `Active` fact), `core_facts()` Active-only warm-set injection, a fully-propagating `forget()` (paged + BM25 rebuild + graph bi-temporally closed + ghost), and `memory/consolidate` + `memory/status` JSON-RPC — all backward-compatible (`#[serde(default)]` + a backcompat test). The deterministic floor is conservative; scheduled/background synthesis, per-fact provenance, project-scope isolation, and model-assisted importance remain TODO HARDENING H2. TODO 1002 → 1003 (468 done + 535 open).** **Then 2026-08-19 (scheduled-task monitoring + scheduler serialization fix):** adopting the ChatGPT Scheduled-Tasks model (task-definition vs schedule vs execution; the scheduler is outside the model; monitoring = stateful polling with run-vs-notify + stop condition), the Rust `SchedulerService` gained a `MonitorConfig` (`stop_on_condition`/`last_observation`/`notifications`) on `Job`, a `monitor_evaluate()` returning a `MonitorVerdict` (`changed`/`notified`/`stopped` — first run notifies, unchanged observation is a no-notify no-op, delta notifies, `condition_met` + `stop_on_condition` disables the job), and `scheduler/monitor` + `scheduler/monitor_config` JSON-RPC. Also fixed a real cross-layer bug: `Job`/`SchedulePolicy`/`RunState` serialized snake_case (`session_id`/`suppress_on_battery`/`lease_expires_at`) against a camelCase coordinator/UI contract, so the scheduler's session reawakening read `sessionId` as `undefined` — now `#[serde(rename_all = "camelCase")]` + `RunState` field renames, with a regression test. Remaining: coordinator `scheduler/monitor` wiring, broader time windows, task↔chat association cascade (TODO HARDENING H2). TODO 1003 → 1004 (469 done + 535 open).** **Then 2026-08-19 (scheduler monitoring — coordinator wiring):** completing the "scheduler ≠ agent" split end-to-end, `scheduler.ts` `runJob` now calls `scheduler/monitor` after a monitoring run — `SchedulerJob` gains `monitor`/`MonitorConfig`/`MonitorVerdict`, the assistant's final reply is captured as the run's `observation`, `conditionMet` is derived from a documented `[MONITOR_DONE]` stop-marker convention (the monitoring prompt instructs the model to end with it when the end condition is met; the marker is stripped from the stored observation), and a new `monitor` `ChatEvent` (`changed`/`notified`/`stopped`/`current`/`notifications`) is emitted for the UI's notify-vs-silent decision. Remaining: broader time windows (morning/afternoon/evening), task↔chat association cascade, and the UI push/badge surface for the `monitor` event. Coordinator tests 84 → 87. TODO 1004 → 1005 (470 done + 535 open).** **Then 2026-08-19 (final live-registry verification pass):** the four official discovery endpoints the spec's live-discovery principle depends on were re-verified **live** (direct `read_url` fetch, not secondary sources — web search stayed unavailable this pass): (1) MCP registry `registry.modelcontextprotocol.io/v0/servers` → JSON `servers[]` with `server` (name/description/remotes/`$schema`) + `_meta.io.modelcontextprotocol.registry/official` (`status`/`publishedAt`/`updatedAt`/`isLatest` versioning — the P22 MCP manager must dedupe by name and take `isLatest`); (2) ACP registry `cdn.agentclientprotocol.com/registry/v1/latest/registry.json` → JSON `version:"1.0.0"` + `agents[]` with per-agent `distribution` (npx `package`/`args`, or binary per-platform `archive`/`cmd`/`sha256`) — byte-identical to `everyaios-acp::registry_client::REGISTRY_URL`, so the live CDN fetch is not a hardcoded catalog; (3) models.dev `models.dev/models.json` → model catalog keyed by id with `reasoning`/`tool_call`/`modalities`/`limit.context`/`benchmarks` — the exact A6 capability-hint fields, confirmed present; (4) searx.space `searx.space/data/instances.json` → `instances` + per-instance health/timing/version. **No code/doc change was required:** the `live-discovery over hardcoded catalogs` principle (§0) was already implemented correctly (live CDN fetch + curated offline fallback + version pinning).** **Then 2026-08-19 (hostile review + Agent-OS-kernel manifestos — verification pass):** an external line-by-line hostile review of the spec plus two "local Agent OS" architecture manifestos was checked claim-by-claim. **One factual defect in this file was real and fixed: the matrix totals said 🔵 58 but the rows count 59** (now corrected, with the correction annotated). The review's verdict (RED on *trust claims* until Stage 0 is live, AMBER on the architecture) matches what §9.1 already concedes; its other P0/P1 findings resolve onto already-tracked work (dev vault key R7 · Chrome 136 default-profile CDP → E13 note added — isolated profile default, opt-in pairing · ACP/MCP brokered-or-sandboxed stance → P7.8/P7.9 · npx/uvx supply chain → K6/P22 + new exact-command consent · Lightpanda AGPL already documented spawn-only · "engine-true" Office qualified to the engine's supported feature set · MCP 2026-07-28 statelessness qualified as new-transport default, not universal, with stdio/legacy-HTTP behind a per-transport adapter). The two manifestos' 17-module list maps ~95% onto the existing A–K pillars and landed crates; the genuinely-missing *seams* were added as **TODO HARDENING H3** (8 open items): unified `Execution` record, lease fencing tokens, per-run context snapshot (Task/Occurrence/Run separation), first-class Data Egress Engine + the four explicit modes (Offline/Local/BYOK-connected/Third-party), per-capability idempotency keys, generated capability-status manifest (prevents the 58→59 class of drift), named R0–R4 risk tiers, and the Stage-0 adapter contract (describe→prepare→authorize→execute→observe→receipt) + SEP-1024 exact-command install consent. TODO 1005 → 1013 (470 done + 543 open).** **Then 2026-08-19 (spec de-cluttering + final-document pass):** per a direct instruction to make the spec the *clean current contract* rather than a running log — (1) added a deep plain-words intro at the very top (what the app is: continuity + trust, one workspace, one memory, one safety model, one audit trail); (2) moved the ~8,000-word §6 "Progress (2026-08-16)" landing log out of the spec into this changelog (its detail was already mirrored in the header notes above — the spec now carries only a one-paragraph "Current build state" summary pointing here); (3) compressed the three §9.1 review-resolution paragraphs (benchmark gaps + security-posture + hostile-review) into one "Current-state honesty" paragraph; (4) compressed the header's source-ground-truth fold-in history to a changelog pointer; (5) added a new **§9.2 Edge cases & failure modes** list (14 hard cases: exactly-once, TOCTOU, lease fencing, crash-between-intent-effect-outcome, per-run context snapshot, DST/clock edge cases, memory staleness/poisoning, cache-never-serves-mutation, Chrome 136 CDP, unsupported-Office-flagging, prompt injection, secret exfiltration, cancellation propagation, restart recovery). No capability rows or TODO items changed.**

## Changelog (v3.0 → v3.13)

> **v3.13 changes:** ACP-registry + BYO-agent subscription-auth pass (doc 57 — live-verified 2026-08-10): **the official ACP agent registry is real and is now our harness-discovery source** — `agentclientprotocol/registry` (346★, Apache-2.0, daily-active; CDN `cdn.agentclientprotocol.com/registry/v1/latest/registry.json`; RFD `/rfds/acp-agent-registry`; dist types binary/npx/uvx; **38 agents live** incl. `claude-acp` (Claude Agent)/codex-acp/Gemini CLI/Qwen Code/OpenCode/Goose) → **F8 + F12 upgraded to registry-fed discovery** (CDN catalog + local cache + version pinning + curated allow-list — the F12 9-CLI set becomes the default list, not the ceiling) + **auth-mode badge** (subscription-backed / API-key-backed / local) on every harness; **Hermes generalized ACP client confirmed** (issue #5257: `copilot_acp_client.py` → generic `ACPClient` + `acp_agent_registry.py` — J17 reference impl beside cowork-forge `acp/client.rs`); ⚠️ **subscription-auth boundary (CRITICAL, doc 57 §3, live-verified): Anthropic (Apr 2026) restricts Claude Pro/Max OAuth to official surfaces — but driving Claude Code/Claude Agent via the official ACP wrapper `@agentclientprotocol/claude-agent-acp` (co-authored by Anthropic·Zed·JetBrains, runs the Claude Agent SDK with the user's own login) is FIRST-PARTY-SUPPORTED** (Zed/JetBrains/Hermes precedent = our F12/J17 path); **blocked = harvesting the subscription token to power other engines' direct calls** → never feed it into our own broker path; BYOK API keys for the broker → new **ARCH/06 §6.16** + **TODO-1010 corrected** + doc 47 annotated; TODO P6.8 +3 tasks; ledger 226 → **227 repos** (agentclientprotocol/registry, **346★**). **No matrix rows added** (maps onto F8/F12/J17); matrix unchanged (138 rows, 33 algorithms).
> **v3.12 changes:** Agentic dev-environment + workflow + closed-source-agent research (doc 56 — warp/cowork-forge/cronflow **cloned + source-read**, Copilot/Devin web-verified, live-verified 2026-08-10): **warp (64,107★, AGPL — open-sourced 2026, OpenAI sponsor)** — **W1–W8 steals**: merkle-tree incremental codebase-embedding index (tree-sitter semantic chunker + content-hash sync + search shaping = the open Rust DeepWiki → **I7 RepoMap extension + C5 embedding path**), **LSP-backed diagnostics** (rust/ts/pyright/clangd/go — Copilot's `lsp-config.json` pattern, now open-sourced; → TODO P7.1), ONNX input intent classifier (W3), isolation sandbox (W5), computer-use (W6), rmcp MCP validation (W7), spec-driven Oz workflows (W8); **cowork-forge (83★, MIT)** — **C1–C6**: config-driven stage/hook/artifact pipeline + Actor-Critic + **ACP external-coding-agent adapter** (F12/J17 reference impl) + role-prompt set + iteration inheritance; **cronflow (125★, ⚠️ no LICENSE)** — HITL-pause state-machine workflow reference (H22/B7); **Copilot CLI (11,073★, closed — custom license, binary wrapper)** → **F12 harness list += Copilot CLI** + LSP-config pattern; **Devin API v3 + MCP + DeepWiki** (DeepWiki pattern → sourced from Warp W1). 4 already-in targets re-verified (cc-switch 126,015 / rtk 75,397 / open-interpreter 67,927 / tauri 110,062 — ⭐ refresh only). **No matrix rows added** (steals map onto existing rows); **F8 harness installer re-pointed at the F12 harness set (BrowserOS's 7-harness catalog superseded — single source of truth)**; **steal-coherence map (doc 56 §8)** records layering decisions (RepoMap vs Warp index · LSP vs lint-reflection vs rtk · architect vs surgical hierarchy · intent-routing sources · one-scheduler rule) so future steals can't conflict; ledger 222 → **226 repos**.
> **v3.11 changes:** Agent-browser ecosystem verification (doc 55 — all three repos **cloned + source-read**, live-verified 2026-08-10): **Obscura confirmed as Tier-1 engine** (21K★ — ⚠️ earlier "10K+" corrected; its own CDP *server* 14 domains + `LP.getMarkdown`, embedded MCP server 32 tools, `scrape` workers, **SSRF/file:// defaults**); **E11 Session Vault extended to full storage context** (cookies + localStorage + sessionStorage + IndexedDB with Chrome leveldb decode, persist/restore — Steel pattern); **post-v1 tool candidates** (a11y_audit axe-core, annotated screenshots, find semantic locators, batch mode, `read` markdown-negotiation + llms.txt — agent-browser); **browser network containment** (WebRTC disable + worker fail-closed + SSRF-defaults → **ARCH/06 §6.15**, F11); **MCP tool profiles** + **SKILL.md skill-format alignment** (I2); agent-browser/Steel added to the design-reference (non-dependency) list. No matrix rows added (steals map onto existing rows); ledger 219 → **222 repos**.
> **v3.10 changes:** Formalization pass (doc 53 — an external architecture review's 4 correct gaps, no new rows): **credential broker** (coordinator sends provider/model/body + opaque key handle → Rust vault resolves key, injects auth headers, scrubs buffers — the sidecar never holds raw credentials; A2/A9/P3 + ARCH/06 §6.9); **authorization ticket contract** (ticket_id/agent_id/session_id/tool_id/operation/args-hash/paths/expiry/single-use/approval-source/risk/audit-seq, enforced by everyaios-guard; J21 + ARCH/06 §6.10); **durable event model + idempotency classes** (10 event types; safe-retry/unsafe/same-key/confirm-after-uncertain; J5/J19 + ARCH/06 §6.11); **shortest-path routing** (tasks take the minimal tier chain; P2 + P6.10). Third-party audit (doc 54): Kuzu archived Oct 2025 → **LadybugDB confirmed active** (⭐1,557, Rust binding `lbug` 0.19.1) → **ledger 218 → 219 repos**; ⚠️ xxhash-rust BSL-1.0 → twox-hash swap; `focus_window` verified as pattern-completion (rename-safe). Matrix unchanged (138 rows, 33 algorithms).
> **v3.9 changes:** Second gap-audit pass (doc 52 — an external v3.7 review + web-search deep-dive, all repos live-verified): **Aider added to the F12 harness list** + explicit **surgical hierarchy** (brain → core → surgeon via ACP/J17) in P2; **new J21 escalation rules & decision packages** (`permissions.toml` policy layer + decision-package contract + confidence threshold — policy above J1/J3/J17); **new D12 storage health & analytics** (folds the review's K4/K5/K8); **new G8 tiered search cascade & cache** + **Algorithm #33** (SQLite 5-min cache → WebSurfx Rust metasearch → SearXNG → circuit-breaker fallback; parallel 4-tier fetch cascade — searxng-mcp pattern) + G1 extended; E9/J14 extended with verified references (Agent-S/cua/OSWorld; agentlens/agentsight). **Rejected:** B9–B11 (duplicate I7–I10), the review's H25–H28 (ID collision), E15–E18/C13–C14/I11–I13/J21–J25-review#/B12–B13/F14-review# rows (redundant or reference-only), Algorithm 33-aider (I9 exists). **8 proposed repos unverifiable** (cyber-memory, AIO-Sandbox-AI, Special Agents, Agent Market, Nous APM, TraceVerse, devai pod, Lybic — 404/no real match; flagged, never cited). Matrix 135 → **138 rows** (🟢 27 · 🟡 58 · 🔵 59 · ⚪ 9) · **33 algorithms** · ledger 192 → **218 repos**.
> **v3.8 changes:** Gap-audit pass (docs 49–51) closes the pasted-chat gaps — **storage intelligence (D9–D11 + G7)**: new `everyaios-storage` crate (eDirStat parallel work-stealing walker + immutable arena snapshots + treemap, fclones/eDirStat 7-stage hash dedup, large-file finder, SQLite FTS5 instant filename search + notify watcher — docs 49); **generative UI via AG-UI (H25)**, **image generation (A10)** as provider endpoint, **clipboard tool (H26)**, **resumable streams (H27)**, **voice output TTS (H28)** + offline STT/wake-word extension to H15, **email/calendar connectors (F14/F15)** (doc 50); **aider claims re-verified vs primary sources (doc 51)** — edit formats ~9 (doc 46 said 6), providers 100+ (said 50+), "4.2× fewer tokens" + "71% first-pass" flagged **third-party, unverified**; matrix 124 → **135 rows** (🟢 26 · 🟡 54 · 🔵 60 · ⚪ 9); ledger 170 → **192 repos**.
> **v3.7 changes:** Full verification pass against live 2026 ecosystem — **KuzuDB → LadybugDB** (Kuzu abandoned Oct 2025, acquired by Apple; LadybugDB is the active community fork preserving the Cypher/embedded API); **MCP 2026-07-28 is now STATELESS** (no initialize handshake, no Mcp-Session-Id — every request self-contained via `_meta`; simplifies everyaios-mcp considerably); **rmcp → official `modelcontextprotocol/rust-sdk`**; **CloakBrowser ⚠️ proprietary binary warning** (Python wrapper MIT but Chromium binary is closed-source black box — added Fortress as open alternative); **cache hit rate claims corrected** (99% on DeepSeek only; Claude dropped TTL to 5 minutes → max ~87%); **IronCalc pre-1.0 risk noted** (v0.7.1, targeting 1.0 mid-2026); **Bun binary ~60MB realistic** (not 25MB); all repos re-verified live.
> **v3.6 changes:** Modularity deep-dive (doc 44) + ACP deep-dive (doc 45) wired into the whole spec — **new matrix rows I6 (Extension/plugin ABI)** + **J17 (ACP harness bridge)**; matrix **107 → 109 rows**; **F12 harness-driving now specifies ACP (Agent Client Protocol)** as its external agent-harness interface (our app = Client, GuardRail/audit between ACP messages and execution); **F9 tool registry adopts the ACP tool-kind taxonomy** (read/edit/delete/move/search/execute/think/fetch/other); **Extension ABI (6 layers)** added to P8 + §4: versioned manifest (`abi_version`), capability allow-lists with `*`/`**` arg wildcards (Zed `CapabilityGranter`, source-verified), per-extension trust flags (Hermes fail-closed), explicit agent-binding (Cherry Studio), lazy activation (VS Code), `ctx.approval()` (AnythingLLM); **`everyaios-ipc` handshake mirrors ACP `initialize`** (protocolVersion + optional-by-default capabilities — production proof of ABI versioning); §2 sovereignty table + §6 new-to-build + §7 build order updated.
> **v3.2 changes:** opencode + Hermes **full code re-read** (doc 38) → token economy 05 gains code-verified mechanisms (Hermes 3-layer tool-result persistence: preview+path, per-turn 200K, 0.15/0.30 context fractions, read_file pinned; opencode compaction: 20K buffer, tail-turns+split, PRUNE_PROTECT 40K tool-output erasure; per-message token schema + the AI-SDK-v6 cached-input normalization gotcha; Hermes IterationBudget 500/50 + execute_code refund). No matrix change (100 rows).
> **v3.5 changes:** Landmines Reinforcement Landscape (doc 43) — concrete code-verified workarounds for the 13 landmines in 4 categories; matrix gains B6 (subagent depth/cap details), J11 ($ budget), J12 (orphan-prevention), J13 (sidecar heap safety), J14 (OTel tracing), J15 (length-prefixed framing), J16 (UNIX socket + pre-spawn + warm pool). All source-cited.
> **v3.4 changes:** IPC Architecture Validation (doc 42) — code-verified OpenFang kernel.rs + ZeroClaw ABI traits; formalized the "sidecar proposes, Rust disposes" axiom with source evidence; documented 4 weaknesses + mitigations (IPC latency budget, payload serialization cost, restart latency, shared-state concurrency); added IPC payload budget table, explicit WAL-mode concurrency model, OpenFang/ZeroClaw kernel pattern references to §4.
> **v3.3 changes:** NOOA deep-dive (doc 39) — **new C10 Pass-by-reference context** (live refs + bounded previews via script-eval; never serialize what you can reference) + **algorithm #32 ACT-R activation & spontaneous recall** (NOOA memory: retention half-life × log1p(strength), importance ≥8 protected, associative semantic+keyword+recency+graph recall, typed supports/contradicts/derived-from edges, pre-turn spontaneous context block) → 07 §7.7, 05 §5.9, 06 §6.6; **F13 now informed by DeerFlow 2.0 channels** (10 IM adapters + run_policy + dedupe_store, doc 39 §B1); microsandbox hypervisor resolved = **libkrun** (doc 39 §B2). Ledger 151 → **152**; matrix 100 → **101** rows.
> **v3.1 changes (on top of v3.0):** adds **C9 Taste profile** (auto-learned coding-preferences with confidence scores — Command Code `taste-1` pattern, doc 37) + algorithm #31; ledger 147 → **151**; matrix 99 → **100** rows; doc 37 added.
> **v3.0 changes:** folds in research docs 35–36 (Open WebUI / Vane / Open WebUI Computer / Composio-community batch), the complete ARCH set (12 docs, `desktop_app/ARCH/`), the browser tiered-engine stack + Session Vault + challenge handler (08 §8.8–8.10), multi-key BYOK key-rings (03), surgical office engine (04), token economy (05), dual-guard security (06), memory + the 7 algorithms (07), and the feature matrix grown **62 → 99 rows** (E10–E14 browser, F12 harness-driving, F13 messaging bridges, H17 widgets, H18 remote handoff). **Every shipped algorithm is now marked 🔁 TO BE RETESTED on the desktop target.**

