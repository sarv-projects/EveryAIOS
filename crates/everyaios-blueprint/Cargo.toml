[package]
name = "everyaios-blueprint"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "P6 — orchestration core: spec-per-task files, verify-gated blueprint tasks, agent-frontmatter schema, multi-agent topologies, and automation tool shapes."

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
sha2 = { workspace = true }
sha1 = { workspace = true }
thiserror = { workspace = true }
toml = { workspace = true }
# P69.D32 — the runtime verification contract (`crate::verify`) lives here; the
# eval harness is *not* a dependency of this crate (it depends on us).
everyaios-guard = { path = "../everyaios-guard" }
# P71.3f — `DelegationPolicy::admit` judges the canonical `AgentReadiness`; the
# state is declared once in the leaf contract crate, so the delegation gate
# cannot drift from what the picker renders.
everyaios-types = { path = "../everyaios-types" }
