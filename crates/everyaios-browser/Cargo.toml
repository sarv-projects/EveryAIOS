[package]
name = "everyaios-browser"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "EveryAIOS browser layer — a11y snapshot/refs/diff (P2.2) + 37-tool action engine (P2.3) + tiered engines (P2.4) + tab ownership (P2.6) + session-vault cookie glue (P2.7) + behavioral realism (P2.9) + injected replay recorder (P2.10) + E15 Electron-app CDP attach + E16 slim snapshots + WebMCP handshake + HTTP transport + E17 multi-protocol action parsing (native/CUA/Anthropic/UI-TARS)"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
similar = { workspace = true }
regex = { workspace = true }
thiserror = { workspace = true }
ureq = { workspace = true }
url = { workspace = true }
html2md = "0.2"
everyaios-cdp = { path = "../everyaios-cdp" }
everyaios-audit = { path = "../everyaios-audit" }
everyaios-vault = { path = "../everyaios-vault" }
