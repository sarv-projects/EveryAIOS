[package]
name = "everyaios-eval"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "P8.0 — Verified-Completion Eval Subsystem (EV1): completion-status taxonomy, task-manifest format, deterministic verifier SDK (never trusts the agent's \"finished\"), evidence bundles + persistent store, sandbox runner (provision→inject→verify→reset), built-in 30-task adversarial suite, retrieval corpus (7 metrics), and batch run reports."

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
sha2 = { workspace = true }
sha1 = { workspace = true }
