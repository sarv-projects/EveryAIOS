[package]
name = "everyaios-office"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "P4 — surgical OOXML editing: ZIP parts index + byte-preserving rewrite; docx block tree + minimal w:t patch (D1); xlsx calamine read + IronCalc truth recalc + workbook DSL/planner + surgical sheetN.xml/sharedStrings.xml patch (D2); pptx slide shapes + minimal a:t patch + add/remove slide (D3); atomic write + snapshotBefore rollback + parts_diff + LibreOffice oracle (D6/D7); legacy .doc/.xls/.ppt conversion (D8, GenOffice pattern doc 28)."

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
zip = { workspace = true }
roxmltree = { workspace = true }
calamine = "0.30"
ironcalc = "0.8"
regex.workspace = true
lopdf = "0.36.0"
